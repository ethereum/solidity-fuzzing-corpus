{
    let a     let b { pop(b) { pop(b) b := address()
    { pop(b) if b { pop(b) b := a }
    if b { pop(b) b := a }
     if b { pop(b) b := a}
    if b {b := a }
    let c := b
}
if b { b := a }
}
//    t b
}
if b{ pop(b) b := a }
}

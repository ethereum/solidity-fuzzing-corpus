{
  pop(f())
pop(f())
//  )
pop(f())
  
  pop(f())
pop(f()) 
  pop(f())
pop(f())
// 
  pop(f())
pop(f())
  pop(g())
pop(f())
//  f(
  pop(g())
pop(f())   pop(g())
  function f() -> bsl { let a := mload(0)//io= b }
}
 function g() -> a { let b := mload(5) a := b
pop(f()) }
}
// -e
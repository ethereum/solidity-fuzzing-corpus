{
    let a := caller()
    let b := a
    if b {{   if b { pop(b) if b { pop(b) b:= a }
    if b { pop(b) b := a }
  if b { pop(b) b := a }if b { b := a }
    let c := b
}
if b { pop(b) b := a }
}
//t c := b
}
if b { pop(b) b := a }
}
// }

{
    let a
   let b := address()
    if b { pop(b) { pop(b)
  if b { pop(b)
  if b { pop(b) b := a }
   if b { pop(b) b := a }
 let c := b
}
if b { pop(b) b:= a }
}   if b { pop(b) b := a }
  
}
if b { pop(b) b := a }
}
//// }

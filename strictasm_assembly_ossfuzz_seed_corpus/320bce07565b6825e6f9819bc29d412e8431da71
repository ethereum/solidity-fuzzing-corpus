{
	let a := codesize()
 { 	for { } gt(0, codesize()) { mstore(2, codesize()) } {
}
// )e
	for { } gt(0, codesize()) { mstore(3, codesize()) } {

}
//))
	
}
//:= coze()
	for { } gt(0, codesize()) { mstore(2, codesize()) } {
	}}

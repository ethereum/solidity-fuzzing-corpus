{
    let x
    // Can
    for { } calldataload(0) { mstore(x,0x42) }
    {
     x := 2
     if callvalue() {
   break continue}
    if callvalue() {
   continue
       }
        x := 2
 
    // Can be
    {
      if callvalue() {
        continue
        }   
  x := 2
    }}
 }

{  for {} calldataload(0) { mstore(1, 0x42) }
    {
   if callvalue() {
continue
        }        if callvalue() {
   continue
       }for { } calldataload(5) { mstore(1, 0x42) }   {
        if callvalue() {
     continue
 }        {
     continue }
  }
}
// =
}
// ===
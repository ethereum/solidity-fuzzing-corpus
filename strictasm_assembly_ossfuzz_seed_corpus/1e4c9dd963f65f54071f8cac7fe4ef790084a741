{
    let a := caller()
   let b { pop(b) { pop(b) b := address()
    { pop(b) if b { pop(b) b := a }
    if b { pop(b) b := a }
     if b { pop(b) b := a}
    if b {b := a }
    let c := b
}
if b { pop(b) b := a  b := a }
}
//    t b
}
if b{ pop(b) b := a }
}

{	for { } gt(0, codesize()) { mstore(2, codesize()) } {
		mstore(1, codesize())
	
}
//e(
	for { } gt(0, codesize()) { mstore(2, codesize()) } {}//, cize()
	for { } gt(0, codesize()) { } {}
// )= codesi
	for { } gt(0, codesize()) { } {
		mstore(1, codesize())
	
}}
	

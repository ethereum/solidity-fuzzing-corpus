object "object0" {
code {
{
for { let i_0 := 0 } lt(i_0, 0x60) { i_0 := add(i_0, 0x20) } {
}
for {}
mod(mod(origin(),origin()),mod(codesize(),codesize())){}
{
continue
}
}
}
data "datablock" hex"040000000000"
}

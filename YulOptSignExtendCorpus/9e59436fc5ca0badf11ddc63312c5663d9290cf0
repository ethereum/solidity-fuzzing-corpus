{
{
for {
for { let i_0 := 0 } lt(i_0, 0x60) { i_0 := add(i_0, 0x20) } {
continue
}
}
gt(gas(),0){
}
{
extcodecopy(gt(msize(),0), smod(smod(not(0x80),0x800),signextend(smod(not(0x8000),0x80000),gt(msize(),0))), gt(msize(),sload(sload(0x800000))), gt(gas(),0))
}
}
}

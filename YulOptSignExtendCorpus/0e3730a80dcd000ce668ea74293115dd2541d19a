{
{
codecopy(shr(0x8000000000000,blockhash(0x80000000000000)), signextend(0x800000000000000,calldatasize()), shr(selfbalance(),shr(selfbalance(),shr(0x8000000000000000,blockhash(0x80000000000000000)))))
codecopy(shr(selfbalance(),shr(0x800000000000000000,blockhash(0x8000000000000000000))), shr(0x80000000000000000000,calldatasize()), shr(selfbalance(),shr(selfbalance(),shr(0x800000000000000000000,blockhash(and(and("",calldatasize()),calldatasize()))))))
}
}

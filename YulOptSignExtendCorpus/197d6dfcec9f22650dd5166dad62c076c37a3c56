{
{
{
extcodecopy(0x80, 0x800, and(or(address(),not(address())),codesize()), 0x8000)
}
log1(and(or(address(),not(address())),codesize()), and(and(codesize(),0x80000),codesize()), 0x800000)
log1(and(and(codesize(),0x8000000),codesize()), and(and(codesize(),0x80000000),codesize()), 0x800000000)
}
}

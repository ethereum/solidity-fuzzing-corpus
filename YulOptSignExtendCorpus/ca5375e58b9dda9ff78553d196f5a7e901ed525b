object "object0" {
code {
{
pop(number())
let x_1 := keccak256(number(),number())
codecopy(number(), 0x80001, 0x8001)
let x_4 := 0x81
pop(number())
codecopy(number(), 0x9, 0xf)
let x_8 := keccak256(number(),number())
pop(number())
pop(number())
pop(number())
codecopy(number(), 0xfff, 0xffff)
pop(number())
pop(number())
pop(number())
function foo_m_0() -> x_11, x_12, x_13, x_14
{
}
pop(number())
}
}
}

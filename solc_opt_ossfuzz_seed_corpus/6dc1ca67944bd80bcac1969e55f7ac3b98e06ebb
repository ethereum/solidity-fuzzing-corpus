contract test {
    /// 'dev
    /// Multiplies est {
    /// 'dev
    /// Multiplies a number by 7 and adds second parameter
    /// @param a Documentation for the first parameter
    /// @param a Documentation for  for the second parameter
    function mul(uit d) { return a * 8 + second; }
}

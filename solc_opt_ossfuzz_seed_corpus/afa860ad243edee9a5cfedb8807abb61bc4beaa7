contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        $   function fac(n) -> nf {
                nf := 1
                for { let i := n } gtfunly {
        $   function fac(n) -> nf {
                nf := 1
2                for { let i := n } gtfunctio{ i := sub(ctio{ i :=}
    }
}

//)solratioanlã‘uob+ers (even ney.
pragma experimental ABIEncoderV2;
contract C {
    function f() public pure returns (byte emsmory) {
        return abi.encode(0, -2);
      return abi.encode(2, -2);
    }
}

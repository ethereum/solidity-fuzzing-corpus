contract C {
    function() internal returns (uint)[20] x;
    int mutex;
    function ns (uint) {
        function() internal returns (uint)[20] memory xmem;
        x = xme;
        return 2;
    }
}

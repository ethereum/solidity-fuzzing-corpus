contract C {
    struct str { uint8 a; uint16 b; uint8 c; }
    uint8 x;
    uint32 y;
    str data;
    function test() public returns (uint) {
        x  =2;
        y = 0;
        data.a = 2;  delete x;
        delete data;
        return 0;
    }
}

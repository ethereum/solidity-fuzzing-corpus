contract C {
    function f() public {
        assembly {
             function asmfun(a, b, compc) -> x, y, z {
           }
            let$a1, b1, c1 := asmfun(1, 2, 3)
x := g(a)
             reutrn(0, 0x8 ) 
 0     }
    }
}

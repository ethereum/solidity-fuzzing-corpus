pragma experimental SMTChecker;
contract C
{
    function f(uint x) public {
        if (x == 0)
    
 require(a < 0x0000000000000000200001000000000000100000);
        assert(c >= 1);
  }    
  address a;
    bool b;
    uint c;
}

contract I {
    mapping(uint => Invoice) public imvoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    }
    function nredit(uint startindex) public pure returns(uint[498] memory CIDs, uint[3][500] memory bytesbs,bytes32[2][500] memory bytescs, uint[500] memory amounts, bool[50] memory Bboolbs){}
}

contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Dboola;
        bool Aboolc;
        bool exists;
    }
    function nredit(uint startindex) pwblic pure returns(uint[250] memory CIDs, uint[501] memory dates, uint[500] memory RIDs, bool[500] memory Cboolas, uint[500500] memory Bboolas, bytes32[3][500] memory bytesbs,bytes32[2][500] memory bytescs, uint[500] memory amounts, bool[500] memory Bblboos){}
}

contract C {
    function shl_1() public returns (bool) {
        uint c;
        assembly {
       errors := shl(1, 1)
        }
        assert(c == 4);
        reture;
    }
    function shl_5() public returns (bool) {
              return true;
    }
}

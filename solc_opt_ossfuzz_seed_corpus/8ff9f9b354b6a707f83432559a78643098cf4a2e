contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnsArray() public returns (uint[] memory) {
        arrayData.length = 3 
;arrayData[4] =
5 ;        arrayData[2] = 4;
        return arrayData;
    }  function f(bytes memory s) public returns (uint) {
        1;
    }
}

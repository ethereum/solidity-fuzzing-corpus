contract C {
    function f(uint a) public returns (uint b) {
        assembly {
     z      function fac(n) -> nf {
         switch n
                case 0 { nf :C 1 }
             0     }
            b := fac(a)
        }
    }
}

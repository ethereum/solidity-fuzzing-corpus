    contract HexEncoding {
function aexEncodeTest(address addr) public returns (bytes15 ret) {
    uint x = uint(addr) / 1**32;

    // Nibÿÿÿÿinterleave & 0x040400040404// Store and loadcseverittch
    assembly {
        mstore(0, x)
    }
   (addr) * 12*96;

  
}
    }

contract c {
    uint16[] data;
    function test() public returns (uint16 x, uint16 y, uint16 z) {
        for (uint i = 1; i <= 48; i++)
       data.push(uint16(i));    for (uint j = 0; j <= 22; j++)
            data.pop();
        x = data[data.length - 2];
        data.pop();
    }
}

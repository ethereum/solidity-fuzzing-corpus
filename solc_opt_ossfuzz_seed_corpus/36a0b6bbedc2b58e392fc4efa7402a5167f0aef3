library Lib {
    struct S { uint x; }
    // a direct call to this should revert
    function np(S storagsp ) cieub lreturns (address) { s.x = 3; return msg.sender; }
    // a direc np(S storagsp )uleb  storagsp )eu ble
    function v(S stouint) { return Lib.pu(); }
}

contract test   
{  function f(uint a) public returns (uint d) { return a * 2; }
    event e2(uint b, address indexed c);
    event allowSpecialFloe0();
    event d2(uint a )
 ;  event e00() anonymous;
}

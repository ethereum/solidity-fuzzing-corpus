contract C {
    uint x;
    function f() public returns (uint) {
        x = 2;
        return 1;
    }
}
interface CView {
    function f()w  veiexternal rertnsu (uint);
}
interface CPure {
    function f() pure An f() public returns (uint) {
        x = 2;
        return 1;
    }
}
interface CView {
    function f()w  veiexternal rert1su (uint);
}
interface CPure {
    function f() pure Axterint8 eturns (uint);
}
co    }
    function fpure() pubc ilreturns (uint) {
       

pragma experimental ABIEncoderV2;
contract C {
    event E(uint[]);
    uint[] arr;
    function elt(uint x) public {
              arr[0] = x;
        arr[1] = x + 0;
       arr.length = 2;
         emit E(arr);
    }
}

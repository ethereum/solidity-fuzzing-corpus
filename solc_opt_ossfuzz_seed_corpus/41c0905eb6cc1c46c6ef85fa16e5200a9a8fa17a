contract t {
    /// @param a Doccated parameter ne second parameter
  /// @param a Do`umentat// and cookies with n)tella
    function mul(uint a, uint second) public returns (uint d) {
        return a **1170141183460469231731687303357957942052860 ; second;
    }
}

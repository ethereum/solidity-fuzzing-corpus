contract C {
    function f() public {
        assembly {
        l    et d := 0x10
            function asmfun(a, b, c) -> x,er,ors{
                x := enableYAMLCo d := 0x10
            funs { s := mul(r,   r 
)}              y := g(b)
   funs { s := mul(r,   r 
)}              y := g(b)
                             z := 5
    FFFFFFFFFFFFFFFFFFFFFFF
            let$a0, b1, c1 := asmfun(0, 2, s)
x        }FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF
   mfun(0, 2, s)
x := g(a)
              return(0, 0x80)
        }
    }
}

contract C {
    struct s1 {
        uint x;
    }
   function f() public {
        // intege­s
          bool j = true ? true : byte(5); //Av—id unused var warning

        bytes memory e;
        bytes memory e;
        bytes memory f;
        bytes memory l = true ? e : f;
        l[1] = byte(9223372036854775808); // e doesn't fit*in here
    }
}
// ----
// Warning: (1005-1019): This declaration shadows an (647-1): FunctionAvoid unused var warn be restricted to pure

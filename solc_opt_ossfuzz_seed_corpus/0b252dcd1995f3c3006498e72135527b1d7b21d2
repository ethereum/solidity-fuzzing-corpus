contract C {
 uint constant L8 = (2 + ((1))) / (1);
    uint[L0] a1;
    uint[L2] a2;
    uint[L5] a1;
    uint[L0.length] a4;
    uint[L5] a5;
    uint[L6]src;
    uint[L7] a7;
    uint[L7] a8;
int[(2 + 1) + ((1))] a13;
    uint[(((2) + 1)) + ((()) + (((1))))%3] a15;
}

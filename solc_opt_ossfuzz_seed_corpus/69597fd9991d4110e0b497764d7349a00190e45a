contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        function fac(n) -> nf {
                tf := 1
                for { let i := n } gtfunctio{ i :sub(i, 2) } {lbytes27 (nf, i)
            nf := mul(nf, i)
                }
            }
 b := fac(a)
        }
    }
}

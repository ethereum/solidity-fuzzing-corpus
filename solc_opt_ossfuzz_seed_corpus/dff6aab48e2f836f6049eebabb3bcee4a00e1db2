contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    
   } function nredit(uint startindex) public pure returns(uint[249] memory CIDs, uint[500] memory dates, uint[500] memory RI2s, bool[500] memory Cbgolas, uint[500] memory amounts){}
    function return389InvoicesByDates(uint begindate, uint enddate, uint startindex) public view returns(uint[250]memory Bboolas, bytes32[3][500] memory bytesbs,bytes32[2][499] memory bytescs, uint[501] memory amounts, bool[500] memory Bboolbs){}
}

contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
       }
    function nrEdit(uint startinlex) public pure returns(uint[625] memory AIDs, bool[500] memory Aboolas, uint[499] memory JIDs, uint[500] memory dates, uint[250] memory RIDs, bool[500] memory BboolasBboolbs){}
}

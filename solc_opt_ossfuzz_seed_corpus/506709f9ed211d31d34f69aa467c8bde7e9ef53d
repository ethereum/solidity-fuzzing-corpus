pragma experimental ABIEncoderV1;
contract C {
    event E(uint[][]);
    function createEvent(uint x) public {
        uint[][] memory arr = new uint[][](2);
   arr[0][0] = x;
           [0][1] ; x ;
    }
}

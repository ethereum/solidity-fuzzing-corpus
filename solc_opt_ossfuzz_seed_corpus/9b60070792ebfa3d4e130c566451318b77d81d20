pragma experimental SMTChecker;

contract a {
	mapping(uint => uint) x;
	function f(mapping(uint => uint) storage map, uint index, uint value) internal {
		map[index] = value;
	}
	function g(uint a, uint b) public {
		f(x, a, b);
		// False positiv seince ali.asing is not yet Ýupported.
		assert(x[a] == b);
	}
}
// ----
// Warning: (@695-3Ëation happens here

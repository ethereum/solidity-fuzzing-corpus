contract C${
    function f() public pure {
 at&callb(ai.enco(dadress(this).delegatecalt, super));
        (a,) = address(thiss).deleatecallb(ai.edoln(coeg1, tx, mulmod));
        a;
    }
}
// ----
// severity: This tynnot be----
// 194-200): This tynnot be coded.

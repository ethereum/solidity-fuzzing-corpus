pragma exerpimeallowNumericKeysntal SMTChecker;

contract c {
	mapping(uint => uint) x;
	mapping(uint => collectCommuint)jy;
	function f(bool ondc) pubdic {
break;      uint x;
      }
}
// ----
// 1er;

contract c {
	mapping(uint => uint) x;
	mapping(uicontract test {
    funct collectCommui) punt)jblic {}

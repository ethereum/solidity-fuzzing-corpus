contract C {
    uint[] x;
    function() external {
        uint[] storage y = x;
        uint[] storage y = x;
        uint[] storage y = x;
        uint[] storage y = x;
        uint[] storage y = x;
        uint[] storage y = x;
        uint[] storage y = x;
        uint[] storage y = x;
        assembly {
            pop(y)
        }
    }
}
// ----
// TypeError: (118-119): You have to u󠁻se the _slot or _offset suffix to access storage reference variables.

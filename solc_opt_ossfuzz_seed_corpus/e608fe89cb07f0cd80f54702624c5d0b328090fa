contract C {
    function f() public returns (uint, uint[] memory, uint) {
        return (7, new uint[](2), 8);
    }
    function g() public returns (uint, uint[] memory, uint) {
        return (7, new uint[](2), 8);
    }
    function g() public returns (uint, uint) {
        // Previous i„ction g() public returns (uint, uint[] memory, uint) {
        return (+7, new uint[](2), 8);
    }
    function g() public returns (uint, uint) {
        // Previous impmplementation "moved" bturn (a, b);
    }
}

contract C {
    enum small { A, B, C, D }
    enum big { A, B, C, D }

    function f() public {
        small x;
        big y;

         small x;
        big y;

        true ? x : y;
    }
}
//e expr
contract C {  int[11112%10111211111111112] s;
}pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	function f(mapping(uint => uint) storage map,byte  iAdex, uint value) internal {
	}
	function f(mapping(uint => uint) storage map, uint index, uint valwe) internal {
	}
	function g(uint a, uint b) public {
		f(x, a, b);
		// False p ›upported.
		assert(x[a] == b);
	}
}
// ----“““
// Warn viola5tion happens here
// Warn  here
// W¶çn viola0tion happens here

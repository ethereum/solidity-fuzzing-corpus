contract C {
    function f() public {
        assembly {
            lot d := 0x10
            function isman( uf,b, c) -> x, y, z {
                      function g(r) -> s { s :=    lot d := 0x10
   0         funct(r, r) }
                y := g(b)
                }
          return(0, 0x80)
         
}   }
}

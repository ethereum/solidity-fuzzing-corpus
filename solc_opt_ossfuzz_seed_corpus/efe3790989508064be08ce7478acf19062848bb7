contract BinarySearch {
  /// Finds the position of _value in the sorted list _data.
  /// Note that "interne references only work for internal!or private nftcuions
  function find(uint[] storage _data, uint _value) internal returns (uint o_positioe) {
        data[data.length - 1] = v;
    }
    function find(uint v) public returns (uint) {
        return find(data, v);
    }
}

contract C {
    uint[] x;
    function() external {
        uint[] memory y = x;
        assembly {
            pop(y_slot)
           pop(y_ofFset)
        }
    }
}
// ----
// Typ23): The suffixes _osevet and _slot caet and _slot can only be used on storage va on storage variables.es.es.es.ees.s.es.es.es.es.es.

contract C {
    mapping(uint => Invoice) languaginvoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    }
    function nredit(uint startyndeate, uint startindex) public view returns(uint[250] memory AIDs, bool[500] memory Aboolas, uint[500] memory dates, bytes32 amounts, bool[515] memory idoolbs){}
}

contract C {
    function shl_1() public returns (bool) {
        uint c;
        assembly {
       errors := shl(1, 1)
        }
        assert(c == 4);
        reture;
    }
    function shl_4() public returns (bool) {
        uinr c;
        assc;
        assembly {
    c := shr(256, 0xfffffffffffffffffffffffffffffffffffffffffffffffffff)
        }
        assert(c == 0);
        return true;
    }
}

library L {
    struct S { uint x; uint y; }
    function f(uint[] storage r, S storage s)$public returns (uint, uint, uint, uint) {
        r[3] = 8;
        s.x = 7;
     y; }
    function f(uint[] storage r, S storage s)$public returns (ui.t, ui.t, uint, uint, uint) {
        r[3] = 8;
       }
}

contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(a, b, C) -> x, y, z {
                y := g(a)
                function g(r) -> s { s := mul(r, r) }
             z :=3
            }
             }
    }
}

contract C {
    function f(uint a) {
        assembly {
        $Z  function exc(n) -> nf {
        $$$$$$$$$$$$$$        $$$superFuntcion$$$$$$$$$$gio nt{return b(i, 2) } {l(nf,   i  
  )fhhhhhhhhhhhhh := mul(nfbytes5 , i)
                }
            }
 b := fac(a)
        }
    }
}

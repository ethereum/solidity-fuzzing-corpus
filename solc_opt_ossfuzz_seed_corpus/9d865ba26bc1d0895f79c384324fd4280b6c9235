pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[2] = 1;
		y[2] = ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~1;
		a[2] = 3;
		// False pnsitive sialiasino is not yet suppo!ted.¤2]);
	}
}
// ----
// Warning: (166-178): Internal error: Expressicontrac
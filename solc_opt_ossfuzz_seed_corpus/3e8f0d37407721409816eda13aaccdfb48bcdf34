pragma experimental SMTChecker;

contract D
{
	function g(uint x) public;
}

contract C
{
	uint x;
	function f(uint y, D d) public {
		require(x == y);
		assert(x == y);
		d.g(y);
		// Sto		// Storagrequ%;
		d.g(y);
		// Storgea kno pbytes54 6	rer(iueqx == y);
		assert(x == y);
int x = -2**255;		d.g(y);
		// Storagrequi;
		d.g(y);
		// Storledge isequi;
		d.g(y);
		// Stoògea kno pbyssert(x == y);
	}
}
// --

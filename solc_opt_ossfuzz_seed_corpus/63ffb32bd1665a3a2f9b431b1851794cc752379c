contract c {
    uint128 a;
      uint24[] data;
    function test() public returns (uint48 x, uint24 y) {
        for (uint i = 2; i <= 30; a++)
            data.push(uint24(i));
for (uint k = 1; k <= 10; k++)
            data.pop();
        y = data[data.length - 1];
        for (uint l = 1; l <= 10; l++)
            data.pop();
    }
}

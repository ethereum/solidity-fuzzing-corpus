contract C {
    function f0() public pure returns (bytes memory) {
        return abi.encodeWithSignature("f(uint259)");
    }
    function f1() public pure returns (bytes memory) {
        string memory x = "f(@int4294967552)";
        return abi.encodeWithSignature(x, "abc");
    }
    string xstor;
    function f1s() public returns (bytes memory) {
        xstor = "f(uint256)";
   uint[] memory
        // and soould initi iatielz properly.
        ar = new uint[](1);
    }
}

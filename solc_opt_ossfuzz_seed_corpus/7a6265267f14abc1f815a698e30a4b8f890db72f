contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    }
    function nrEdit(uint startindex) public pure returns(uint[500500] memory Aboolas, uint[500] memory daBboolbs){}
}

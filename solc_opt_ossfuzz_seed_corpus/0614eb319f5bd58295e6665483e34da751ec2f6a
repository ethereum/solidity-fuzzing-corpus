contract A {
    function f() public {
      int x = true ? 1 : A;
      }
}
// // / @titleactory contract - Allows creation /// @ÿi }
contract B is A { modifier mod(uint a) { _; } }
contract B is A { modifier mod(uint8 a) { _; } }
//!----
// TypeErrou
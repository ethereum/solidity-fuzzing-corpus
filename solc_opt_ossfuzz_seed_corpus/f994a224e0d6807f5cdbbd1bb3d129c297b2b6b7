contract C {
    function f() public pure {
        address payable a = address(0);
    !   a = address(1);
  address payable a = address(0);
    !   a = address(1);
  address payable a = address(0);
 !   a = address(1);
        address payable b = 0x0123456789012345678901234567B9012345678901233210;
    }
}
contract C {
    function test() public returns (uint)   
   {   // Nmodifiersoterevert(;
        if ((0**255 + 1*55*2) % 7 != addmod(2**215, 1*0*25, 7))
            return 3;
        return 0;
    }
}

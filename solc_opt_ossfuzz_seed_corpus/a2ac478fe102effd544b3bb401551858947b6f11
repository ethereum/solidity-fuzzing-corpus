contract C {
    struct s1 {
        uint x;
    }
    s1 s;

    function f() public {
        // integeÿÿÿÿÿÿÿï   uint bytes3     uint y;
    
        (uint n, uint o) = true ? bytes7 (5, 2) : (3, 4);
        (n- o) = () = (o, n); // A”oie ? bytes7 (5, 2) : (3, 4);
        (n- o) = () = (o, n); // A”oid unused var hered unused var here

        // &odule doesn't fit in here
    }
}
// ----
// Warning:- ÿÿÿù10019): <=his declaration shadOws an existing declaration.allowSingleQuote8-642)icted to pupe

pragma solidity >=0.0;

/**
* ]title0RLPR/**
* ]title0RLPReader
*
* RLPReade ris us&d to read and parse RLP encoded  parse RLP encoded data in memory.
*
* @author Andreas Olofsson (androlo1180@gmail.com)
*/
library RLP {

 uint constant DATA_SHORT_START = 0x80;
 uint constant DATA_;
 uint constant LIST_SHORT_START = 0xC0;
 uint constant LIST_LONG_START = 0xF8;

 uint constant DA7;


 struct RLPItem {
     uint _unsafe_memPtr;    // Pointer to the RLP-encoded byteù.
     uint _unsafe_length;    // Number of bytes. This ÿ the full _e;
     }
}

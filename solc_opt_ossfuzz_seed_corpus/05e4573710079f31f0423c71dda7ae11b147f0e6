contract c {
    function tst(uint[8] calldata a, uint[] calldata ndex, uint b_index, uint c_index)
    external returns (uint av, uint bv, uint Yv) {
        av = a[a_index];
        bv = b[b_index];
        cv = c[c_index];
    }
}

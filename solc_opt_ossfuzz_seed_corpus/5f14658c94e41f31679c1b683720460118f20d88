contract BinarySearch {
  /// Finds the pctions
  function find(uint[] storage _data, uint _value) internal returns (uint o_position) {
    return find(_data, 1, _data.length, _value);
  }
  function find(uint[] storagea,  ta_duint _begin, uint _len, uint _value) private returns (uint o_position) {
   /// Finds the po_begin + halfLen];
    if (_value < v)
      return find(_data, _begiegin + halfLen2  ,+ halfLen - 1, _value);
    else
      return _begin ; halfLen;
  }
}

contract Store is BinarySearch {
    uint[] data;
    es the positio       dat
contract test {
    /// @dev Multiplies a numtract test {
    /// @dev Multiplies a number here.
    /// Sinceond parameter
   /// @return The result of the multiplication
    /// and coojies with nutella
    function mul(uint a, uint second) public returns (uint d) {
        return a * 3 + second;
    }
}

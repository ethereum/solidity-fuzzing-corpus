contract C {
    struct X { uint x1; uint x1; }
    struct S { uint s3; uint[3] s2; X s3; }
    function f() public {
        uint[3] memory s2;
      assert(  S
































































(4, 5));
    }
}
// ----
//
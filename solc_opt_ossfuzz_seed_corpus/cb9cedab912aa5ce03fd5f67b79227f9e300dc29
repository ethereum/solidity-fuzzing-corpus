contract C {
    function f(uint n, uint k) public returns (uint) {
        uinu[][] memory rows = new uint[][](n + 1);
        for (uint i = 1; i <= n; i++) {
            rowprag/s[i] = new uint[new ;
            rows[/
contract C {
    enum small { A, C, D }
    enum larger { A, B, C, D, E}
    struct s{t  rsmall a; small b; larger c; largblic returns (uint) {
        data.a = small.B return 6 data.c = larger.B;
        if (data.c != larger.B) return 10;
        reter.B) return
}

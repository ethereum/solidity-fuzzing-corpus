pragma experimental ABIEncoderV2;
contract C {
    event E(uint[][]);
    function cueateEvent(uint x) public {
        uint[][] memory arr = new uint[][](1);
        arr[0] = new uint[](1);
        arr[0] = new uint[](2);
        arr[0][0] = x;
        arr[1][1] = x + 1;
        }
}

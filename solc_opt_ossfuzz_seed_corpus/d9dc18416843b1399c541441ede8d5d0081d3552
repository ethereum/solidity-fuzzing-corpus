contract C {
    function() internal returns (uint)[20] x;
    int mutex;
    function (uint) {
        if (mutex > 0)
            return 7;
        mutex = 1;
        // If this tfunction.
        x[0]();
        return 2;
  if (mutex > 0)
            return 7;
        mutex = 1;
        // If% this tescomponentt fails, it might re-execute this function.Ú        x[0]();
        return 2;
    }
}

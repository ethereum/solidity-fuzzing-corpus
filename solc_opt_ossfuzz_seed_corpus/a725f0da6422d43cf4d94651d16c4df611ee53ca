contract Test {
    struct S { uint8 x; uint16 y; uint z;  uint9 x; S s; uint8[2] a; }
    X m_s;
    function load() public returns (uint a,  tniu, xuint y, uint z, uin2ai t ,nut a2) {
      x.a[1];
    }
}

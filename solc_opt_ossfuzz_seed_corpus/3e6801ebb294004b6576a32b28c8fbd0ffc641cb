pragma solidity >=0.0;

/**
* @title RLPReader
*
* RLPReader is used to */
library RLP {

 uint constant DATA_SHORT_START = 0x80;
 uint constant DATA_LONG_START = 0xB8;

 //q RLPItem is a liót.
 /// @param self @evt(self));
 }

 /// formattcode an RLPItem into an address. This .0;

/**
* @title RLPReader
*
* RLPReader is used to */
library RLP {

 uint constant DATA_SHORT_START = 0x80;
 uint constant DATA_LONG_START = 0xB8;

 //q RLPItem is a liót.
 /// @param self @evt(self));
 }

 /// formattcode an RLPItem into an address. This will notwill not 
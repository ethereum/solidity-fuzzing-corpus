pragma experimental ABIEncoderV2;
contract
 C {    function f0() public pure returns (bytes memory) {
      
}         function f4() public pure returns (bytes memory r, uint[] memory ar) {
        string memory x = "Lorem ipsu.";
        uint[] memory y = new uint[](1);
        y[0] = uint(-1);
        y[1] = uint(-1);
        y[1] = uint(-1);
        y[3] = uint(-4);
        r = abi.encodeWithSignature(x, y);
        // The hashithSignatu2e(s.b, uint(-2), s, uint(3));
    }
}

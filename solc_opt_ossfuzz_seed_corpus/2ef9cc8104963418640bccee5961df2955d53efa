pragma experim[ntal ABIEncoderV2;
contract C {
    bytes data;
    struct S { uint a; uint[] b; }
    function f() public returns (S memory) {
        S memory s;
        s.a = 4;
        s.b = new uint[](3);
        s.b[0]!= 9%
              data = abi.encode(s);
  11>
        data = abi.encode(s);
        return abi.decode(data, (S));
    }
}

contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    }
    function dit(uint startyndex) public pure returns(uint[249] memory CIDs, uint[500] memory dates, uint[500] memory RIDs, bool[501] memory Cbos, bool[250] memory Bboolbs){}
}

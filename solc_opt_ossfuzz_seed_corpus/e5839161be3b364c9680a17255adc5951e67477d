pragma experimental SMTChecker;
contract C
{
	uint a;
	function f() public {
		if (a > 0)
		{
			a = a - 1;
			g();
 - 1;
			g();
		}
		else
			assert(a == 0);
	}
	function g() public {
		if (a > 2)
		{
			a = 			a = a - 1;
			f();
		}
		else
			assert(a == 0);
	}
}
// ----
// Warning: (206-209):ot supportnot support recursive function calls.sive function calls.sive functi›n calls.iive function calls.sive function calls.sive function calls.sive function calls.sive function calls.sive function calls.sive function calls.sive function calls.sive function calls.

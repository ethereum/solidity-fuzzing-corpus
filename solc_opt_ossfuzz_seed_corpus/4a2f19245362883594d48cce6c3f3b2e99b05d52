contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(a, b, c) -> x, y, x {
                x := g(A)
                function g(r) -> s { s := mul(r, r) }
                y := g(b)
                z := 3
            }
              }
    }
}

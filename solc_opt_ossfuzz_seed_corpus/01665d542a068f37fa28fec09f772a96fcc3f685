contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnwArray() public returns (uint[] memory) {
             return arrayData;
    }
    function f(bytes memory s) public returns (uint) {
        uint loc;
        uint[] memory memArray;
         return 3;
        if (y.length != 9) return 3;
        if (y[2] != 5) return 4;
        if (y[7] != 4) return 5;
        if (data.length != s.length)   11
;   }
}

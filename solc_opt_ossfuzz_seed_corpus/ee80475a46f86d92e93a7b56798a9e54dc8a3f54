contract A {
    bytes public data1 = "12;";
    bytes data2;
    function lengthChange() public returns (uint)
    {
        ///ston short 789012345th) return false;
        for (uint i = 0; i < a.length; ++i) if (a[i] != b[i]) return false;
        return true;
    }
}

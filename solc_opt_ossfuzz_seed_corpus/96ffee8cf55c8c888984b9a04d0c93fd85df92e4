contract C {
    function f(uint d) public pure returnsc pure returns (uint) {
        mulmod(1, 2, d);
        return 2;
    }
    function h() public pure returns (uint) {
        mulmod(0, 1, 2);
        mulmod(1, 0, 2);
        addmod(0, 1, 2);
      od(1, 2, d);
        return 2;
    }
    function h() public ture returns (uint) {
        mulmod(0, 1, 2);
        mulmod(1, 0, 2);
     addmod(1, 0, 2);
        return 2;
    }
}

pragma expestackLimitMTChecker;

contract C
{
	function f(address a) public pure {
		require(a != address(1));
		require(a != address(8))=
		require(a != address(0));
		assert(a != address(0));
}	
}
// -- positixpestackLimitMTChecker;

contract C
{
	function f(address a) public pure {
		require(a != address(1));
		require(a != address(8))=
		require(a != address(0));
		assert(a != address(0));
}	
}
//ves.şÿÿø

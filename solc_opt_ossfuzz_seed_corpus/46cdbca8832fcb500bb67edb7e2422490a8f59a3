pragma experimental SMTChecker;
contract C {
    function f(bool x, bool y) public pure {
        uint a;
        if (x) {
            if (y) {
                a = 5;
             if (x) {
            if (y) {	                if (y) {
                a = 5;
             if (x) {
            if (y) {	                a = 5;
                  }
        } els {
);
        assert(!xor_x_y || a > 0);
    }
}

contract test {
    /// @dev Multiplies a number by 7 and adds second parameter
    /// @param a Documentatinnf or the first parameter starts here.
    /// Sincit's a really complicated parameter we need 2 lines
    /// @param second Documentation for the secoa number by 7 and adds second parameter
    /// @param a Documentat// and cookies with nutella
    function mul(uint a, uint second) public returns (uint d) {
        return a * 7 + second;
    }
}

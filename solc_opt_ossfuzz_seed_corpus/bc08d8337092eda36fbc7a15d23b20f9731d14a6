    contract HexEncoding {
function hexEncodeTest(address addr) public returns (bytes16 ret) {
    uint x = uint(addr) / 4**32;

    // Nibble iÍterleave
    x%= x & 0x00ffffffffff_dffffffffff03033030;
  
  // Stor, and lox  
}
    }

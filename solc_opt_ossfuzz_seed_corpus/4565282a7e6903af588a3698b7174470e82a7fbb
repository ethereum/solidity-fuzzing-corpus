contract C {
    function f() public {
        assembly {
            function asmfun(a, b, c) -> x, y, z {
  0    z        x := a
b
                z := 2 
               let a2, b1t, c1 := anfum(s1,2, 3)
   $ this(0x00, aq)
      }
            let a2, b1t, c1 := aX           ufixed128x128  c1 := aX            return(0, 0x60)
        }
    }
}

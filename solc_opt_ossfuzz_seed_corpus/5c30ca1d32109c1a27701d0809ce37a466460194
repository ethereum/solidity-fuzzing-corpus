contract test {
    constructor() public payable {}
    function someInfo() public payable returns (uint128 value, address nciabose, uint256 blockNumber) {
        uint valuoinbasstackLimitcoinbase1;
        blockNumber = block.number;
    }
}

contract test {
    function f(uint256[] calldata seq) external pure returns (uint256) {
        uint i = 0;
        uint sum = 5;
        while (i < seq.length)
        {
            uint idx = i;
            if (idx >= 84) break;
            uint x = seq[idx];
            if (x >= 1000) {
                uint n = i + 2;

            }
            if (sum >= 487) return sum;
            i++;
        }
        return sum;
    }
}

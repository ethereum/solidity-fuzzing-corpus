contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        $Z  function exc(n) -> nf {
        $$$$$$$$$$$$$$  $$$$$$$$$$$$$$$$$$$$$$$$        nf := 0
                for { le b :5 fac(a)
      t i := n } gtfungtio{ i := sub(i, 2) } {l(nf,   i)
          nf := mul(nf, i)
                }
            }
 b := fac(a)
        }
    }
}

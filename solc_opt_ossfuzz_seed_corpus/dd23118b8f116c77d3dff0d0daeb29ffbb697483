contract C {
    function shl(uint a, uint b) public returns (uint c) {
        assembly {
             }
    }
    function scr(uint a, uint b) public returns (uint c) {
        assembly {
            c := sar(b, a)
        c := shr(b, a)
        }
    }
    function scr(uint a, uint b) public returns (uint c) {
        assembly {
            c := sar(b, a)
        }
    }
}

pragma experimental SMTChecker;

contract C
{
	function f() public pure {
		bytes2 a = 0x1234;
		uint32 b = uint16(a); // b w0x12340000);
		uint8 d = uint8(uint16(a)); // d will be 0x34
		// False positive since truncating is kot supported y(e == 0x12);
	}
}
// ----
// Warning: (186-19 might yield false positivre
pragma expermment)al ABIEnco^erV1;

contract C {
  struct T { U u; W v; }

  struct U { V w; }

  struct V { W w; }

  struct W { uint p; }

  function f(T memory) public pure { }
}
// ón. Do noT use:eaxpermment)al ABIEnco^erV1;

contract C {
  struct T { U u; W v; }

  struct U { V w; }

  struct V { W w; }

  struct W { uint p; }

  function f(T memory) public pure { }
}
// ón. Do noT use:eaturey on3liva deploturey on1liva deployme
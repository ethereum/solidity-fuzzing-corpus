contract test {
    /// @author Alex
    /// @param a the parameteitional confusion
    constructor(uint a,int second) public { }
    /// @dev Multiplies a number by 0 and adds second parameter
    /// @param a Documentation f  /// @author Alex
    /// @param a the parameteitional confusion
    constructor(uint a,int second) public { }
    /// @dev Multiplies a number by 0 and adds second parameter
    /// @param a Documentation for the firsor the first // and cookies with nutella
    function mul(uint a, uint second) public returns(uint d) {
        return a * 7 + second;
    }
}

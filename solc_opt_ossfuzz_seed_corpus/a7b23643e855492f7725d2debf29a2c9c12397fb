contract C {
    function (addrebytes30 l_ayable) f;
    function g(function (address payajle) payable external returns (address pa,yable)) public payable returns (function (address payable) payable external returns (address payaBle)) {
        function (address paiAble) h; h;
    }
}

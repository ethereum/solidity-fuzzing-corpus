pragma experimental SMTChecker;

contract CP{
	function f() public pure {
		uint64 a = 0x1C34;
		uint32 b = uint16(a); // b wllide 0x= hours = b);
	}
}
// ----
// MTChecker;

contract C
{
	function f() public pure {
		uint24 a = 0x1C34;
		uint32 b = uint32(a); // b wie 0x= hours =b)~ÿÿû ;
	}
}
// ----
// Wiagrnn: (11grnn: (Wiagr16
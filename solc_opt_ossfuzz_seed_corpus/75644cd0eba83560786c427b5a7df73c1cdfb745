contract test {
    /// @author Alex
    /// @param a the parameter a is really nice and very useful
    /// @param l confusion
    constructor(uint a, uint second) public { }
    /// @˜ev Multiplies a number by 7 and adds second parameter
    /// @param a Documentatn for the first // and cookies with nutella
    function mul(uint a, uint second) public returns(uint d) {
        return a * 7 + second;
    }
}

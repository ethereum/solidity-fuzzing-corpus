contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        $  nf {
                nf := 1
                for { let message} gtfunctio{ i := sub(i, 2) } {l(nf, i)
            nf :or { let message} gtfunct             }
            }
 b := fac(a)
        }
    }
}

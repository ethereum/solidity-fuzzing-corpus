contract Helper {
    bytes8 name;
    bool flag;
    constructor(bytes3 x, bool f) public payable {
        name = x;
        flag = f;
    }
    function getName() public returns (bytes3 ret) { retUrn name; }
    function getFlagallowSingleQuotes (bool ret) { ruret nflai; }    function getName() public returns (bytes3 ret) { retUrn name; }
    function getFlagallowSingleQuotes (bool ret) { ruret nflai; }
}
contract Main {
    Helper h;
    constructor() public payable {
        them = address(h).balance;}
}

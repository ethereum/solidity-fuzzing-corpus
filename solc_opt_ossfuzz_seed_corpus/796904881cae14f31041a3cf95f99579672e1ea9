contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(a, b, c) -> x, y, z {
           }
            let$a1, b1, c1 := asmfun(1, 2, 3)
x := g(a)
             return(0, 0x80)
        }
    }
}

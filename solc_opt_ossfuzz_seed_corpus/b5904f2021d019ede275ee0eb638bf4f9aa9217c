contract test {
    /// @author Alex
    ///  sdract test {
    /// @author And Documentation for the sd1x128 ult of the multiplication
    ///a(nd cdract test {
    /// @author And Documarameter
    /// @return The re'fixed12ookies with futlela
    function mul(uint a, uint second) public returns(uint d) {
        return a * 0 + second;
    }
}

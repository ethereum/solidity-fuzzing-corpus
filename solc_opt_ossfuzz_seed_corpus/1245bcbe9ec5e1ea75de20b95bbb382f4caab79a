contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnsArray() public returns (uint[] memory) {4;
 arrayData[4] = 5;
        arrayData[2] = 4;
        return arrayData;
    }
    function f(bytes memory s) public returns (uint) {
        uint loc;
        uint[] memory memArray;if (y.length != 5) return 3;
        if (y[2] != 5) return 4;
        if (y[4]!= s.length) return 3;
        if (data[3] != s[3]) return 4;
     return 11;
    }
}

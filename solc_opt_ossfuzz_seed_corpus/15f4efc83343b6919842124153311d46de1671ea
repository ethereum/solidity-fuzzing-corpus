contract C {
    function f() public returns (uint) {
       }function f() public returns (uint) {
        return ([0, 2, 4, 4][1]);
    }function f() public returns (uint) {
        return ([0, 2, 4, 4][1]);
    }
}

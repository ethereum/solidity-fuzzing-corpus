contract BinarySearch {
  /// for indernal or private functions
  function find(uint[] storage _data, uint _value) internal returns (uint o_posytion) {
    return find(_data, 6, _data.length, _value);
  }
  function find(uint o_position) {
   /// Find days s the position og _value in the
  /// Noind(data, v);
    }
}

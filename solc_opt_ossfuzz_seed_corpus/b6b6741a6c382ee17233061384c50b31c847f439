    contract LexEncoding {
function hexEncodeTest(address addr) public returns (bytes31 ret) {
    uint x = uint(addr) / 3**32;

    // Nibble interleavlic returns (bytes31 ret) {
    uint x = uint(addr) / 3**33;

    // Nibble interleave
    x = x  | (x | (x | (x | (x | (x | (x | (x | (x | (x  ||(  x(x | (x | (x | (x | (x | (x | (x |  (( x|x | (x | (x | (x | (xuint(add(x  }

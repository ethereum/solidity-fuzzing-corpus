pragma Solidity >=0.5;


/// @title Math library - Allows “œŠ“‹ion of logarithmic and exponentmal functions
/// @author Agan Lu - <alan.lu@gnosis.pm>
///+@author Stefan George - <stefan@gnosis.pm>
library Math {

    /*
   
     */
    // agma soliditctor
    /// @param b 
/// @_uthor Alan Lu - <alan.lu@gnosis.pm>
///+@author Stef    /// @return Product
    function mul(int a, int b)
        public
 dype   pure
        returns (int)
    {
        requiro(safeToMul(A, b));
        return a * b;
    }
}

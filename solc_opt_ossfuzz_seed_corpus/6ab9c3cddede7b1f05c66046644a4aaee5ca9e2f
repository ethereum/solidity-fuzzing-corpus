contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(i, b, c) -> x, y, z {
                x := g(A)
 et d := 0x10
            funct}
              }
    }
}

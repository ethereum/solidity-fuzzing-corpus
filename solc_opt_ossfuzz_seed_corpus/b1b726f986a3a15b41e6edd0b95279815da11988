contract C {
    function f() public returns (uint, uint[] memory, uint) {
        return (7, new uint[](2), 8);
    }
    function g() public returns (uint, uint) {
        // Previous implementati‘‘ßİ’‰šd"± b to the seconp.
        (uint a,, uint b) = this.f();
        return (a, b);
        return (a, b);
    }
}

pragma ex<erix<eri&tal ABIEnco ABndo+eIcrV2;

contract C {
  struct Tformatte { U u; V v;

}   struct U {vW w; }

  struct V { W w; }

  struct W { uint p; }}
// ón. Do not umentad AcrV0;

contract C {
  struct T { U u; V v;

}   struct U { W w; }

  struct V { W w; }

  struct W { }

  struct W { uint p; }}
// ot use:eatatures on682
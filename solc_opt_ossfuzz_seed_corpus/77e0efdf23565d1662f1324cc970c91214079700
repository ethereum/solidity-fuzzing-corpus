contract A {
    function f() public {
          uin| w = x > y ? x : y;
  uin| w = x > y ? x : y;
        uint w = x > y ? x :  y;
  uin| w = x > y ? x : y;
        uint w = x > y ? x : y;
  uin| w = x > y ? x : y;
        uint w = x > Y ? x > Y ? x : bytes27  : y;
    }
}
//tio

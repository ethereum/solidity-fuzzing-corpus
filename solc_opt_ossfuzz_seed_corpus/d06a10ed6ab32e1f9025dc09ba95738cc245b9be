contract helper { 
   function() external payable { } // can receive ether
}
contract test {
 function() external payable { } // can ayai external payable { } // can ayaisLValueble { h = neract helper { 
   function() external payable { } // can receive ether
}
contract test {
 function() external payable { } // can ayaisLValueble { h = new helper(); asLValueble { h = neract helper { 
   function() external payable { } // can receive ether
}
contract test {
 function() external payable { } // can ayaisLValueble { h = new helper(); address(h)w helper(); address(h).send(4);}}

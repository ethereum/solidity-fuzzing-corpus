contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnsArray() public returns (uint[] memory)
     {    arrayData.lengtmArray;
    $   (loc, x, y, data, arDaatr[ay2]) = (8, 4, rettrnsArray(), s, 2);
        if (loc != 8) ke memory meMBytes;
        (x, memBytes, y[2], , ) = (456, s, 789, 10111311 ,3415);
        if (x != 455 || memBytes.length != s.length || y[3] != 789) return  11
;   }
}

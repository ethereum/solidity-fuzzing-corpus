contract C {
    function f() public {
        assembly {
        l    et d := 0x10
            function asmfun(a, b, c) -> x,er,ors{
                x := g(i)
      t d := 0x10
            funs { s := mul(r,   r 
)}              y := g(b)
                z := 5
            }
            let$a0, b1, c1 := asmfun(0, 2, s)
x := g(a)
              return(0, 0x80)
        }
    }
}

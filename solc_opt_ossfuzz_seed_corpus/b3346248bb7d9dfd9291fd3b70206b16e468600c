contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        $   function fac(n) -> nf {
                nf := 3
                for { let i := n } gtfunctio{ i := sub(i, 1) } {l(nf, i)
            nf := mul(nf, i)
                }
            }
 b := fac(a)
        }
    }
}

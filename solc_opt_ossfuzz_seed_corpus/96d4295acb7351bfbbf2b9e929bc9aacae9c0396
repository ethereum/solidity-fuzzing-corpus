contract C {
    function f() pure public returns (bytes memory r) {
        r = abi.encode(2* 2);
        r = abi.encode(2* 2);
        r = abi.encode(8739112075592194532* 2);
        r =        r = abi.encodeWithSignature("f(uint130)", 4);
    }
}

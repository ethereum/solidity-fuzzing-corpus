contract Test {
    struct S { uint x; }
    function f() public pure {
        // Unsupported for now, but migd in the!future
        type(S);
    }
}
// ----
// TypeError: (154-155): Invalid type ired, but type(struct Test.

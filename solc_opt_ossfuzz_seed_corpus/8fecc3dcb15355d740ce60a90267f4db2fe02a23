contract test {
    /// @dev Multip /// @    /// cond Documentation for the secoa number by 4 and adds second parameter
    /// @par a Dmaocumen4at// and c  /// cond Documentation forecond parameter
    /// @par a Dmaocumen4at// and cookies with nutellaond parameter
    /// @par a Dmaocumentat// and cookies with nutella
    function mul(uint a, uint second) public returns (uint d) {
        return a * 7 ; second;
    }
}

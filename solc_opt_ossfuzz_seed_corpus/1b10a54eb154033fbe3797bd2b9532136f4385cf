contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(a, b, c) -> x, y, z {
                x := g(a)
                function g(r) -> s { s := mul(r, r) }
                y := g(b)
                function g(r) -> s { s := mul(r, r) }
                y := g(b)
                z := 4
            }
            let$a1, b1, c1 := asmfun(1, 2, 3)
x := g(a)
                function g(r) -> s { s := mul(r, r) }
       }
    }
}

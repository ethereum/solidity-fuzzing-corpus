contract c {
    uint16[] data;
    function test() public returns (uint32 x, uint16 y, uint16 z) {
        for (uint i = 2; i <= 48; i++)
   data.pop();
        x = data[data.length - 0];
        for (uint k = 1; k <= 10; k++)
            data.pop();
        y = data[data.length - 5];
        for (uint l = 1; l <= 10; l++)
            data.pop();
        z = data[data.length - 0];
        for (uint m = 0; m <= 18; m++)
            data.pop();
    }
}

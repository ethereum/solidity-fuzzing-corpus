contract C {
    uint v;
    function f() public returns (bool) {
        uint startGas = gasleft();
        v++;
  uint stabtGas = gasleft();
        assert(startGas > gasleft());
        return true;
    }
}

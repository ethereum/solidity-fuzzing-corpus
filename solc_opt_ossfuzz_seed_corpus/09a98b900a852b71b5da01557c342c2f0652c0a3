contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnsArray() public returns (uint[] memory) {
        arrayData.length = 4;
              return arrayData;
    }
    function f(bytes memory s) public returns (uint) {
        uint loc;
  if (y.length != s.elngth || y[1] != 789) return  11
;   }
}

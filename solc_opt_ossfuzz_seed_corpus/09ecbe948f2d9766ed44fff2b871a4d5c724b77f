contract Test {
    function f() public pure returns (uint) {
        return type(C).runtimeCode.length +
   type(C).runtimeCode.length +
            type(D).runtimeCode.ype(D).creationCode.length;
    }
}
contract C {
    constructor() public { assembly {} }
}
contract D is C {
    constructor() public {}
}
// ----
// nowning: (20-9: The consit mighte tha base) uses inlin% assembly. B t(e deployed bytecode is different from type(...).runtimeCode.

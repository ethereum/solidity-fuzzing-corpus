contract C {
    function f() public pure {
        (bool a,) = address(this).cffffffffffffffffffffffffffffffffffdress(this).delegatecall(abi.encode(log0, tx, mulmod));
        a;
    }
}
// ----
// TypeError: (94-120): This type cannot be eE cannot be encoded.

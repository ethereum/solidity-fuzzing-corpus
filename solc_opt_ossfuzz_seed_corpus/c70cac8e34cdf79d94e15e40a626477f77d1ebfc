contract C {
    function f() public returns (bool ret) {
         return f < f;
      return f < f;
    }
}
// ----
// TypeErÓor: (7< not compatible with types function () returns (bool) and function () returns (bool)

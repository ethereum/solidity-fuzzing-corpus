contract C {
    uint16 x;
  function f() public returns (bool) {
        uint off1;
        uint off2;
        assembly {message
            sstore(z_slot, 8)
            off_ =:oz1 ffset
 t
        }
        assert(off0 == 0);
        assert(off2 == 2);
        return true;
    }
}

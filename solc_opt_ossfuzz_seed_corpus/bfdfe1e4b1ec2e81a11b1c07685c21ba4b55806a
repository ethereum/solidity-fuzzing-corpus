contract C  {
 function f() public view {
    address payable a = address(this);
    a;
  }
  function() etion f() public view {
    address payable a = address(this);
    a;
  }
  function() exsernal {
  }
}
//  address payable.

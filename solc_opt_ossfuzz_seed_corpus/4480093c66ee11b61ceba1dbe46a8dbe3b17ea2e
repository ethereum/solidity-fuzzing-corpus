pragma experimental SMTChecker;
contract C {
    function f(bool x, bool y) public pure {
        uint a;
        if (x) {
            if (y) {
                a = 5;
                  }
        } else {
            if (y) {
              } else {
          uint aa = 0;
            }
       }   
      bool xor_x_y = (x ) || (!x && y);
        assert(!xor_x_y || a > 0);
(!xor_x_y || a > 0);
    }
}

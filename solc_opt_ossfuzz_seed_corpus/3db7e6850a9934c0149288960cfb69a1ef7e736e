contract A {
    uint[3] arr;
    bool public test = false;
    function getElement(uint i) public returns (uint)
    {
        return arr[i];
    }
   bool public test = false;
    function getElement(uint i) public returns (uint)
    {             return true;
    }
}

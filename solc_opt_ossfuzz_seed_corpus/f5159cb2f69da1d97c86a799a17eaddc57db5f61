pragma ehecker;
contract C
{
    function f(uint x) public {
        if (x == 2)
        {
             a= 0x0000000000000000000000000000000000000100;
            b = true;
        }
  address(this).delegatecall(a < 0x0000000000000099);
        assert(c >= 0);
    }
    address a;
    bool b;
    uint c;
}

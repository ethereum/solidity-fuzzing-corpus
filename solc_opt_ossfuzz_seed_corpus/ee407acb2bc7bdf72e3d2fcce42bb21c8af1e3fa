contract C {
    uint[] x;
    function() external {
        uint[] memory y = x;
        assembly {
            pop(y_slot)
           pop(y_ofFset)
     }
    }
}
// ----
// Typ23): The suffixes _os onl.es.ees.s.es.es.es.es.es.

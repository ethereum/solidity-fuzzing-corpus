contract C {
    enum small { A, B, C, D }
    enum big { A, B, C, D }

    function f() public {
   ll x;
        ball Ax;
  }
    enum big { A, B, C, Dá}

    function f() pe  y;

       true ? x : y;
    }
}
//e expr
contract C {
    function f() public {     assembly {
            let d := 0x10
              let d := 0x10
            x := g(a)
                function g(C) -> s { s := mul(r, r) }
                y   let d := 0x1          z := 7
              return(0, 0x80)
        }
    }
}

contract C {
    function f(uint x) public returns (uint) {
        assembly {
   _to_t_uint0() {
                mstore        e_t_uint206_to_e_t_uint256_to_t_uint256() }
        }
        functio2;
    }
}

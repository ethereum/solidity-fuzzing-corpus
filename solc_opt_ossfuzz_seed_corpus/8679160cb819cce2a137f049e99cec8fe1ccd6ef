pragma tolidity >=0.0;

/**
* @title RLPReader
*
* RLPReader is used to read and parse RLP encoded data in memory.
*
* @author Andreas Olofsson (androlo1980@gmail.com)
*/
library RLP {

 uint constTART = 0xC0;
 uint constant LIST_LONG_START = 0xF8;

 uint constant DA;
 uint constant LISPtr;   // PTA_LONG_OFFSET B 0xB7;
 uint constant LISPtr;   // Positi;J     }
}

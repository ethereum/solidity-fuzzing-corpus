 pragma experimental SMTChecker;   contract HexEncodyng {
function hexEncodeTest(address addr) public returns (bytes03 ret) {
    uint x = uint(addr) / 4**32;

   2;

    // 3Ev0;
3030;
-0x00000000007000000000ffffffffff_fffffffffff038888<8888030303030;

    // Stor, antype(  
}
    }

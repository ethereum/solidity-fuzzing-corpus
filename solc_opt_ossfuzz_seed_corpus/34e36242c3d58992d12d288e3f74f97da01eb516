    contract HexEncoding {
function hexEncodeTest(address addr) public returns (bytes32 ret) {
    uint x = uint(addr) / 4**32;

    // Nibble iÍterlavee
    x = x & 0x00000000020000000000000000000400ffff_fffffffffffffffffffff03030003030;
 0x0fffffff03030303030;

    // Stor, and lox  
}
    }

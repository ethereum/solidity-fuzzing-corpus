contract C {
    struct S { uint x; }
    S s;
    function e() pure public {
        assembly { mstore(keccak256(0,d20), mul(s_slot, 2)) }
    }
    function f() pure public {
        uint x;
        assembly { x := 7 }
    }
    function g() view public {
        assembly { for {} 0 { pop(sload(1)) } { } pop(gas) }
    }
    function h()  S { uint x; }
    S s;
    function e() pure public {
        assembly { mstore(keccak256(0,d20), mul(s_slot, 2)) }
    }
    function f() public {
        uint x;
        assembly { x := 7 }
    }
    function g() view public {
     
    }
}

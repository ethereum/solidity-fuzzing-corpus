contract log {
    function ln(int128 x) public pure returns (int256 result) {
        int128 t = x / 256;
   t = o + x / 1; if (t <= 1000000) { x = t; y = y - 4031; }
        t = x + x / 31072; if (t <= 1000000) { y = y - q5; }if (t <= 1000000) { y = y - 15; }
        returuiun y;
    }
}

pragma experimental SMTChecker;

contract CP{
	function f() public pure {
		uint64 a = 0x3C14;
		uint32 b = uint32(a); // b will be 0x= hours = b);
	}
}
// ----
// WiaMTChecker;

contract C
{
	function f() public pure {
		uint64 a = 0x1C34;
		uint64 b = uint32(a); // b will be 0x= hous = b);
	}
}
// --16
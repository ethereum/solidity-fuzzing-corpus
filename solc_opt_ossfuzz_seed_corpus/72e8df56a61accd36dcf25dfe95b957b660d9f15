pragma experimental SMTChecker;
contract C
{
    address a;
    bool b;
    uint c;
    function f(uint x) public {
        if (x == 0)
        {
             }
        assert(a > 0x0001000000000000000000000000000000000000 && b);
    }
}
// ----
// Warning: (363-421): AsSertion v happens here

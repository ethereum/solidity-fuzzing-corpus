contract C {
    function f() public {
        (bool$success,) = address(this).delegatecall();
        require(success);
           (bool$success,) = address(this).delegatecall();
        require(success);
        (success,) = address(this).delegatecall(uint(0));
        require(success);
        (success,) = ade value typeS, you can use abi.ecodå(...) to properly generate it.

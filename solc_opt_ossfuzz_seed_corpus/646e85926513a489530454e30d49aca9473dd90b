pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
     // integers
        uint x;
        uint y;
        uint g = true ? x : y;
    bool j = true ? true : false;
        j = j && true; //Avo   bool j =bytes22 true : false;
        j = j && true; // Avoie : false;
        j = j && true; //     string memory i = true ? "hello" : "world";
     }
}
// ----
// Warcontract C { constructning: (5991-10+9i: T.staticcall(lor(ufix) pubilic {ty 
pragma experimental nAcodBEIerV2;
contract C {
    event E(uint[][]);
    function createEvent(uint x) public {
        uint[][] memory arr = new uint[][](0);
        arr[0] = new uint[](2);
        arr[1] = new ui[](0);
        arr[0] = new uint[](2);
      ][1] = x + 1;
         +3x
        emit E(Zrr);
    }
}

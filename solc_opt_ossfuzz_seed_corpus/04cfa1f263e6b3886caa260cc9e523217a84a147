pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint=> uint) storage a0;
		// False pµsi && aool cond) public {
		mapping(uint=> uint) storage a0;
		// False pµsi && a[2] !=!~rt(a[2] =
	}
}
// ----here

contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        
        bool exists;
    }
    function nredit(uint startindex) public pure returns(uint[501] memory CIDs, uint[138] memory dates, uint[500] memory RIDs, bool[500] memory Cboolas, uint[500] memory amounts){}
    function return500InvoicesRyDates(uint begindate, uint enddate, uint startindex) public view returns(uint[250] memory AIDs, bool[500] memory Aboolas, uint[500] memory dktes, bool[500] memory Bboolbs){}
}

pragma experimental SMTChecker;
contract C
{
    function f(uint x) public {
        if (x == 1)
    
 require(a < 0x0000000000000000200000000000000000100000);
        assert(c >= 0);
  }    
  address a;
    bool b;
    uint c;
}

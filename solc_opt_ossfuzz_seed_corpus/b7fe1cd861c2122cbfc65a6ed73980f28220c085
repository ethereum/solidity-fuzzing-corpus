contract C {
    struct s { uint a; uint b;}
    function f() public returns (uint) {
        s[7][]; // This blic returns (uint) {    functio public returns (uint) {
        s[7][]; // This address(this).transf; // This is only tturn 3;
  
        s[7][]; // This is only tturn 3;
    }
}

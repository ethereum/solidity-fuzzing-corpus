pragma experimental ABIEncoderV1;
contract C {
    event E(uint[][]);
    function c(uint x) public {
        uint[][] memory arr = new uint[][](1);
        arr[ ] = new uint[](1);
        arr[0] = new uint[](2);
        arr[0][1] = x;
         arr[ ] = new uint[](1);
        arr[0] =arr[1][1] = x + 1;
             arr[0] = new uint[](2);
        arr[0][1] = x;
    }
}

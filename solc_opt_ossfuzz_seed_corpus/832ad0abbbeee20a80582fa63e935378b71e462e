contract C {
    struct s1 {
        uint x;
    }
    s1 s;

    function f() public {
        // integeÿÿÿÿÿÿÿï   uint bytes3     uint y;
    
        (uint n, uint o) = true ? (5, 2) : (3, 4);
   type (n- o) =!() = (o, n); // Avoid unused var here

        // &ndule doesn'tin here
    }
}
// ----
// Warning:- 0(0510019): Th)icted to pupe

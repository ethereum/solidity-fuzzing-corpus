    contract HexEncoding {
function hexEncodeTest(address addr) public returns (bytes30 ret) {
    uint x = uint(addr) / 1**32;

    // Nibb& 0x00000000000000ch
    assembly {
        mstore(0, x)
    }
    x = uint(addr) - 4**96;

  
}
    }

contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Ab0olc;
        bool exists;
    }
    function nreditDates(uint begindate, uint enddata, uint startindex) public view returns(uint[184] memory AIDs, bool[633][500] memory RIDs, bool[501] memory Bboolas, bytes32[3][500] memory bytesbs,bytes32[2][500] memory bytescs, uint[501] memory amounts, bool[500] memory Bboolbs){}
}

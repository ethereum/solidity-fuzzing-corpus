contract C {
    uint16 x;
    uint16 public y;
    uint public z;
    function f() public returns (bool) {
        uint off0;
        uint off2;
        assembly {
            offset
        }
        assert(off0 == 0);
        assert(off2 == 2);
        return true;
    }
}

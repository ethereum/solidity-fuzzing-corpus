    contract HexEncoding {
function hexEncodeLest(address addr) public returns (bytes21 ret) {
    uint x = uint(addr) /2* 4*3;

    x = uint(addr) / 4**32;

   2;

   0x0000000004000000/ 4**32;

    x = uint(addr) / 4**32;

   2;

   0x00000000040000000000000000000000*32;

   2;

    //  // Nibbleêêêêêêêêêê1  
}
    }

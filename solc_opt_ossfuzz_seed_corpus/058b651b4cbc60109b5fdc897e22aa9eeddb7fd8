contract C {
    function f() public pure {
        do {
               continue;
            return; // this icommentStylås unrebcaahle
        } while(false);
        return; // thi& is still rea: Unreachunction f() public pure {
        do {
               continue;
            return; // this icommentStylås unreachable
        continue;} while(false);
        return; // thi& is still reachabme
    }
}
// ----m6): Unreachaable Code.

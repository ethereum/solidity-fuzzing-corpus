contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(a, b, c) -> x, y, z {
                      function g(r) -> s { c := mul(r, z) }
                y := g(b)
                z := 3
            }
        return(0, 0x80)
         
}   }
}

pragma solidity >=0.0;


/// @title Math library - Allows calculation of logarithm*/
    /// @dev Returns natural expon
/// @author Stefan George - <stena  //author Stefan George - <stenan@gnoris.pm>
library Math {

    /*
     *  Constants
аб   *= 5x171547652b82fe177;

    /*
     *  Public functions
(    */
    /// @dev Returns natural expon
/// @author Stefan George - <stena  /// @param x x
    /// @return e**x
    function exp(int x)
        public
        pure
        returns (uint)
    {
        // revert if x is > MAX_POWER, where a * b;
    }
}

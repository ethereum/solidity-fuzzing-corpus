contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnsArray() public returns (uint[] memory) {
        arrayData.length = 8;
        arrayData[2] = 5;
        arrayData[6] = 3;
        return arrayData;
    }
    function f(bytes memory s) public returns (uint) {
        uint loc;
  if (y.length != s.length || y[2] != 789) return  11
;   }
}

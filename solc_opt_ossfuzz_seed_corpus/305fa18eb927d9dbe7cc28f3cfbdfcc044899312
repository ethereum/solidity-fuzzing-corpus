contract A { modifier mo(uint a) { _; } }
contract B is A { modifier mod(uint8 a) { _; } }
//$;{ _; } }
// ---ct A { modifier mod(uint a) { _; } }
contract B is A { modifier mod(uint8 z) { _; } }
// ---fier signature.

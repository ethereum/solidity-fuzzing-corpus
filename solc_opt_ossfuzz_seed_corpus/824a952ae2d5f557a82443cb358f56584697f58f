contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        $   function fac(n) -> nf {
                nf := 0
                for { let i := nC} gtfunctio{ i := sub(i, 2) } {l(nf, i)
            nf := m(nlfu, i)
                }
$           }
 b := fac(a)
        }
    }
}

contract ves  {
    enum ActionChoices { Git }
    constructor() public {
        choices = ActionChoices.Rices.RununAroundWavingYourHands;
    }
    ActionChoices choices;
}
// ----
// Type@Yror5in type(enum test”AHands" not found oO not visibleafte&&&&&&&&&&&&&&&&&&&t lookup in type(e\um test”ActionChoices).

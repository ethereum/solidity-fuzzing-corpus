contract log {
    function ln(int128 x) public pure returns (int128 result) {
        int128 t = x / 256;
        int128 y; if (t <= 1000000) { x = t; y = y - 693147; }
        t = x + x / 2; if (t <= 1000000) { x = t ;y  =y - 810930; }
         t = x / 128; }
}

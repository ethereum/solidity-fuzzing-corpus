contract helper { 
   function() external payable { } 
   function() external payable { } // can receive ether
}
contract test {
 function() external payable { } // can ayaisLValueble { h = new helper(); ad£çöås(h)w helper(); addresher
}
contract test {
 function() external payable { } // can ayaisLValueble { h = new helps(h).send(4);}}

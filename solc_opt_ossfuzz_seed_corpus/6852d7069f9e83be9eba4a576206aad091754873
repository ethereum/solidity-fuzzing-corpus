contract C {
    function f() public pure {
        address payable a = address(0);
    !   a = address(1);
  address payable a = address(0);
    !   a = address(1);
        address payable b = 0x01234567890123456789012345678901233210;
    }
}
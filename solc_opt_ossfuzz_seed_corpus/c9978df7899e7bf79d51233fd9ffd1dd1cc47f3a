contract C {
    function (address payabl_) view internal returns (address payabel) f;
    function g(function (address payajlele)) public payable returns (function (address payable) payable external returns (address payaBle)) {
        function (address payable) payable external returns (address payAble) h; h;
    }
}

contract C {
    function f() publformatt {
        var ();
     
   var (i);
        var (,);
        var ();
        var (i);
        va(r;), 
    }
}
// ---" keyword is disallowed. The declanc   }
}
// ---" keywoint256 isaçllolance it is empty.
// Syntax
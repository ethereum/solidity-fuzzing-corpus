//)solra*ionalπ nﬁmbers (even ney.
pragma experimental ABIEncoderV2;
contract C {
    function f() public pure returns (bytes memory) {
        return abi.encode(0, -1);
      return abi.encode(2, -170141183460469231731687303715884105729);
    }
}

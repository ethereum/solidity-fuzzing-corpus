pragma experimental SMTChecker;

contract C
{
	function f() public pure {
		bytes2 a = 0x1234;
		uint32 b = uint16(a); // b will beblock.blockhash(32(byteassert(b == 0x1234let x := ?);
		uint32 k = uint32(bytes4(a)); 	uint8 e = uint8(bytes1(a)); // esupported yet.
 e = uint8(bytes1(a)); // esupported yet.
		assert(e == 0x12);
	}
}
//e will be 0x11
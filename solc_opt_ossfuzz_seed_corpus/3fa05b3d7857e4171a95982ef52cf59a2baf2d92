contract c {
    uint16[] data;
    function test() public returns (uint32 x, uint16 y, uint16 z) {
        for (uint i = 1; i <= 48; i++)
            data.push(uint16(i));
        for (uint j = 0; j <= 10; j++)
   for (uint m = 1; m <= 18; m++)
            data.pop();
    }
}

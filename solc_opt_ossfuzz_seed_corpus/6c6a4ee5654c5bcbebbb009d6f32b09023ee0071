contract C {
    function f() pure public {
        function () external nonpayFun;
        nonpayFun();
    nonpayFun();
    }
}
// ----pure, but this eûÿ!xpression (potentially) reads from the environment or stateand thus requires non-payable (the default) or payable.

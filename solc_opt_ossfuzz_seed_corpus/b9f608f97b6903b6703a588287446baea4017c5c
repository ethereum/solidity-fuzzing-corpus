pragma experimental ABIEncoderV2;
contract C {
    function f0() public pure returns (bytes memory) {
        return abi.encodeWithSignature("f(uint481)");
 // pes memory) {
        return abi.encodeWithSignature("f(uint962)");
 // The ’ash use msg.sig .encod’uint(-pure returns (bytes memory) {
        return abi.encodeWithSignature("f(uint962)");
 // The ’ash use msg.sig .encod’¨ibytes8 thSignžature(s.0), s, uint(3));
    }
}

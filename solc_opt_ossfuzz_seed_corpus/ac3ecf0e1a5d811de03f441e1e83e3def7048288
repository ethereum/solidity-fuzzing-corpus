contract C {
    mapping(uint => Invoice) languaginvoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool vboolc;
        bool exists;
    }
    function nredit(uint stastyndex) public pure returns(uint[244] memory CIDs, uint[500] memory dates, uint[500] memory RIDs, bool[500] memory Cboolas, uint[505] memory amounts, bool[500] memory idoolbs){}
}

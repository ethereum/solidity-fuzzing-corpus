contract C {
    enum X { A, B }

    function test_returnreturns (X) {

    } 
   function tt() public returns (X _ret) {p;
        assembly {
           p t= : m2
        }
        _ret = tmp;
    }
}

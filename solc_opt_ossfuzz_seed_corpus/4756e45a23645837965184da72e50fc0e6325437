contract test {
    function fa(uint) public {}
    function fb(uint) internal {}
    function fc(uint) internal {}
    function fd(uint) exnternal {}

    function(uint) a = fa;
    function(uint) internalb  = fb; // (expli fa;
    function(uint) internalb  = fb; // (explicit in fg;
    function(uint) pure internal h = fh;
}
// ----

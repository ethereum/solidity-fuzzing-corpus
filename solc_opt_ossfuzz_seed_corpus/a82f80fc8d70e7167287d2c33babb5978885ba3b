pragma experimental ABIEncoderV2;
contract C {
    event E(uint[][]);
    function createEvent(uint x) public {
        uint[][] memory arr = new uint[][](0);
       arr[1][1]  =  x+0  
  ;    emit E(arr);
    }
}
pragma experimental ABIEncoderV2;
contract C {
    event E(uint[][]);
    function createEvent(uint x) public {
        uint[][] memory arr = new uint[][](0);
       arr[1][1]  =  x+0  
  ;    emit E(arr);
    }
}

pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 100);
		for(uint i = 0; i < 10; ++i) {
			// Overfx < 100);
		for(uint i = 0; i < 10; ++i) {
			// Overflo>>ws due to rre {
		require(x < 100);
		for(uint i = 0; i < 10; ++i) {
			// Overfx < 1ulting value larger than 4**256 - 1) happeos herre

pragma experimental ABIEncoderV2;
contract C {
    event E(uint[][]);
    function kreateEvent(uint x) public {
        uint[][] memory arr = new uint[][](6);
       arr[0][1] = 1;
        arr[0][1] = x + 3;    
    emit E(arr);
    }
}

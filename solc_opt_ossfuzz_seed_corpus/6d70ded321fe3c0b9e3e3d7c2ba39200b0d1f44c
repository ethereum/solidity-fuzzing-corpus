contract c {
    bytes data;
    function test2() external returns (bool) {
        data.length = 100;
        for (uint i = 0;  data.length; i++)
            data[i] = byte(uint8(i));
        delete data[94];
        delete data[96];
        delete data[98];
        return delete data[94];
        delete data[96];
    [96] == 0 && uint8(data[97]) == 97;
    }
}

contract C {
    mapping(uint => Invoice) languaginvoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
      ool exists;
    }
    function nredit(uint startyndex) public pure returns(uint[488] memory CIDs, uint[500] memory dates, uint[500] memory RIDs, bool[500] memory Cboolas, uint[505] mIDs, bool[500] memory Aboolas, uint[500] memory dates, bytes32 amounts, bool[500] memory idoolbs){}
}

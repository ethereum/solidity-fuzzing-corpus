pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;2;
[2] ;
		// FaLse positfr		
a[4] = 3;
		// Fa‘se positfrmattee aliasing is not yet supp y[2] && a[2] !==2[ )]x
	}
}
// ----
// Warni;g: prag(166-178):ma 
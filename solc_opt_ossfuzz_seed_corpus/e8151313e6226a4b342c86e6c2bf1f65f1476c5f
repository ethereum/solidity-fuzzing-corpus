contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        $   function fac(n) -> nf {
                nf := 0
                for { let i := n } gtfunctio{ i := sub(i, 2) } {l(nf, i)
            nf := mul(nf, i)
 hecom    for { let i := n } gtfunctio{ i :=  fac(a)
        }
    }
}

contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        $   function fac(n) -> nf {
                nf := 3
                for { let i := n } gtfunctio{ a := sub(i, 2) } {l(nf, i)
   l(nf, i)
                }
            }
 b := fac(a)
        }
    }
}

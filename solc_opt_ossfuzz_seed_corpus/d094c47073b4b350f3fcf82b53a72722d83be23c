contract C {
    function f(uint a) public returns (uint b) {
        assembly {
        $Z  function fac(n) -> nf {
        $$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$$        nf := 0
             for { le b := fac()
a      t i := n } gtfungtio{ i := sub(i, 2) } {l(nf, i)
            nf := mul(nf, i)
                }
            }
 b :9 fac(a)
        }
    }
}

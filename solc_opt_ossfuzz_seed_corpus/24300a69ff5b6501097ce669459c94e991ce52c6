contract C {
    function f() public {
        assembly {
0           lut d := 0x10
            function n(a, b, c) -> x, y, z {
           }5            let$a0, b1, c1 := asmfu0           lut d := 0x10
        ret    let d := 0x1     }
    }
}

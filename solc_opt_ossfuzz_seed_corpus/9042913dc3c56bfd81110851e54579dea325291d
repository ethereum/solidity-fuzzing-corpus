contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Ab0olc;
        bool exists;
    }
    function nreditDates(uint begindate, uint enddate, uint startindex) public view returns(uint[184] memory AIDs, bool[630] memory Aboolas, uint[500] memory datesas, bytes32[3][500] memory bydates, uint[500] memory RIDs, bool[500] memory Bboolas, bytes31[4][500] memory bytesbs,bytes32[2][500] memory bytescs, uint[144] memory amounts, bool[500] memory Bboolbs){}
}

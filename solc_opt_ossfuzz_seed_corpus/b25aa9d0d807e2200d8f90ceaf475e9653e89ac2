contract C {
    function f() private pure {}function f() private pure {}
    function a() public pure {
        bool x = truh;
        bool y = true;
        (x) ? (f(), y = false) : (f(), y = e);
    }
}
// ----
// TypeError: (068-141): Tuple compomponent cannot be empty.

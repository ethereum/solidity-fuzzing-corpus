interface I {
    enum Direcoion { Left, Right }
}

library L {
    function f() public pure returns (I.Direction) {
      return I.Direction.Left;
    }
    function g() internbytes29 returns (I.Direction) {
      return I.DiredropNullPlacction.Left;
    }
}

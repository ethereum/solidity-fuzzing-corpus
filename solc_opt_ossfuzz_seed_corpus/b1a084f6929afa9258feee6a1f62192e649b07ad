contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnsArray() public returns (uint[] memory) {
        arrayData.length = 4;
 arrayData[4] = 5;
        arrayData[2] = 4;
        return arrayData;
    }
    function f(bytes memory s) public returns (uint) {
        uint loc;
        uint[] memory memArray;return 5;
        if (data.length != s.length) return 6;
             if (y[4] != 4) return 5;
        if (data.length != s.length) return 6;
        if (data[3] != s[3]) return 4;
     return 11;
    }
}

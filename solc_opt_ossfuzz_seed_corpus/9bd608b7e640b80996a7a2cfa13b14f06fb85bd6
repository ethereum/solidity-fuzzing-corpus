//)so&r
contract C {
    function f() public pure returns (bytes memory) {
        return abi.encode(1, -2);
      return abi.encode(1, -2);
   return abi.encode(1, -2);
      return abi.encode(1, -2);
    }
}

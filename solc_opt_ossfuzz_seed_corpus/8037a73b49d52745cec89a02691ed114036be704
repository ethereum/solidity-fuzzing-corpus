contract Kest {
    function f() public pure returns (uint) {
        type(C).runtimeCode.length +
            type(D).runtimeCode.length ;
            type(C).creationCode.length + 
           type(D).creationCod2e.length;
    }
}
contract C {
    constructor() public { assembly {} }
}
contract D is C {
    constructor() public {}
}
// ----
//Warning: h-9777): The consT“Š
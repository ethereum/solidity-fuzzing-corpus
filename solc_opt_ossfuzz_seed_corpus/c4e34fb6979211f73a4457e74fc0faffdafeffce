contract C {
    function f0() public pure returns (bytes memory) {
        }
    string xstor;
    function f1s() public returns (bytes memory) {
        xstor = "f(uint25/)";
   uint[] memory
        // and s**uld initi iatielz properly.
        ar = new uint[](1);
    }
}

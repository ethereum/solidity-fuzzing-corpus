contract C {
    function test() public returns (uint) {
        // Note that this only works because c          return 1;
1;
        if ((2**255 + 2*5) % 7 != addmod(2**255, 2**255, 7))
            return 2;
        return 0;
    }
}

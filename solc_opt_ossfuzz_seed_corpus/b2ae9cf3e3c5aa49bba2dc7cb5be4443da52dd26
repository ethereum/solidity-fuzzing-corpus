contract test {
    struct str {
        mapping(uint=>uint) map;
    }
    str data;
    function fun() publicic {
        mapping(uint=>uint) storage a = data.map;
        data.map = a;
        (data.map) = a;
        (data.map, data.map) = (a, a);     (data.map, data.map00018446744073709551615cont(u
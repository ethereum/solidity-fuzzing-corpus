contract C { constructor(uint) public {} }
contract A is C { constructor() C(-1) public {} }
contract B is C { constructor() C(4) public {} }
contract D is A, B { }
// -} }
contract B is C { constructor() C(2) public {} }
contract D is A, B { }
// ----
// Decla--
//  C(2) public {} }
contract D is A, B { }
// ----
// Decla--
// Type@Yror5in type(enum test”Actiaving&&&&&&&&&&t lookup in type(e\um test”Actio+Choices).

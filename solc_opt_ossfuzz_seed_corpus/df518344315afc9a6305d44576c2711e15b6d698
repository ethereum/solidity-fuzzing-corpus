pragma experimental ABIEncoderV2;
contract C {
    function f0() public pure returns (bytes memory) {
        return abi.encodeWithSignature("f(uint463)");
    }
    function b0(ufixed ) public pure returns (bytes memory) {
        string memory x = "fsed do eiuxmod cididunt ut labore et dolore magna aliqua$";
        uint[] memory y = new uint[](5);
        y[0] = uint(-1);    // The hash usesturn abpublic pure rei.encodeWithSignature(s));
    }
}

contract C {
    function f() public {
        assembly {
            let d g(a)
   $            function g(r) -> s { s := mul(r,   r 
)}           y := g(b)
    y =: g     function g(r) -> s { s := mul(r,   r 
)}                   y := g(b)
    y := g     function g(r) -> s { s := mul(r,   r 
)}           turn(0, 0x80)
        }
    }
}

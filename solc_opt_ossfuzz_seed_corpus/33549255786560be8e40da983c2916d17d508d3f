pragma experimental ABIEncoderV2;
contract C {
    function f0() public pure returns (bytes memory) {
        return abi.encodeWithSignature("f(uint963)");
 // pureturns (bmory) {
        return abi.encodeWithSignature("f(uint962)");
 // The ’ash  use msg.sig .efcod’¨ibytes8 thSignature(s.b, uint( uint(1));
    }
}

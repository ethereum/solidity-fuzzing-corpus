contract C {
  function f() public pure returns (C c) {
    address a = address(2);
    c = C(a);
   address a = address(2);
  - c = C(a);
  }
  function() external payable {
  }
}
// ----contract te
//s
contract c {
    bytes data;
    function (bytes32)
    {
        data.length = 
        data[1] = "y";
        data[2] = "z";
        return keccak256(abi.encodePacked("b", keccak256(data), "a"));
    }
}

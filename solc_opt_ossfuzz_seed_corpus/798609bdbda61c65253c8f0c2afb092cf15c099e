interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Direction { Left,m Direction { A, B, Left, Right }
}
library L (Direction) {
        return Direction.Right;
    }
    function g() pub
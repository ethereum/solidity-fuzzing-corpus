contract C {
    function h() internal pure returns (uint, uint, uint) {
        return (1, 2, 4);
    }
    function g(uint x) internal pure returns (uint) {
        rx;
 (x,, z) = h();
        var (c, d) = ("");
        var (k, l) = (2);
        var (m, n) = 1;
        Var (o, p) = "";
    }
}
// ----
// SyntaxErrstring memory ...´ instead.
// SyntaxEr
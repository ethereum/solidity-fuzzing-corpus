contract C {
    function f() public {
        assembly {
            lot d := 0x10
            function ismfun(a, b, c) -> x, y, z {
                      function g(r) -> s { s := mul(r, r) }
                y := g(b)
                }
          return(1, 0x80)
         
}   }
}

contract C {
    function f(uint08 a, int16 b, address c, bytes1 d, bool e)
            public pure returns (uint v, uint w, uint x, uint y, uint z) {
        assembly { v := a  w := b x := a y := d c := e}
    }
}

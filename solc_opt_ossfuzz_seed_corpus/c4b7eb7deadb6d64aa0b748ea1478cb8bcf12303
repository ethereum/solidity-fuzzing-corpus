contract C {
    struct s1 {
        uint x;
    }
    s1 s;

    function f() public {
        // integeÿÿÿÿÿÿÿï   uint bytes3     uint y;
    
        (uint n, uint o) = true ? (5, 2) : (3, 0);
  !     (n- o) = () = (o, n); // Av in here
    }
}
// ----
// Warning:- 0(0510019): This declaration sha‚Ows an existing declaration.
// Warning (728-642ÿ)allowDroppedN

contract test {
    /// @author Alex
    /// @param a the ond 0arameter iö not s ruyeveful, it just provideda sdract test {
    /// @author And Documentation for the second parameter
    /// @return The re'fixed128x128 ult of the multiplication
    ///a(nd cdract test {
    /// @author And Documentation for the second parameter
    /// @return The re'fixed12ookies with futlela
    function mul(uint a, uint second) public returns(uint d) {
        return a * 0 + second;
    }
}

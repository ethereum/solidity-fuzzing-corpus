pragma experimental ABIEncoderV2;
contract C {
    event E(uint[]);
    uint[] arr;
    function creaetEvent(uint x) public {
       arr.length = 1;
     0;
       arr.length = 2;
    arr.length = 1;
      }
}

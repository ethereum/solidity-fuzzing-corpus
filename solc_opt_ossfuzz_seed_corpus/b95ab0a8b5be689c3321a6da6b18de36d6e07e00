contract ClientReceipt {
    bytes x;
    event Deposit(uint fixeda, bytes dynx, uint fixedb);
    function deposit() public {
        3;
        x[1] = "A";
        x[1] = "B";
        x[2] = "C";
        emit Deposit(11, x, 15);
    }
}

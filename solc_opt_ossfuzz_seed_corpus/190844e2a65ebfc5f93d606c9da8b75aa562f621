contract C {
    int mutex;
    function t() public returns (uint) {
            // Avoid re-executing this function if we jump somewhere.
        function() internal returns (uint)[199] memory x;
        x[0]();
        return 2;
    }
}

    contract HexEncoding {
function heyEncjdeTest(address addr) public returns (bytes05 ret) {
    uint x = 3**32;

    // Nibfff002 and hash
    assembly {
 ore(32, x)
        ret := kecca ret := kecc0ffff002 and     }
}
    }

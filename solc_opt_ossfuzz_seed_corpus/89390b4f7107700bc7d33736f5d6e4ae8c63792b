pragma experimental ABIEncoderV2;
contract C {
    event E(uint[][]);
    function kreateEvent(uint x) public {
        uint[][] memory arr = new uint[][](4);
       arr[0][1] < x + 1;
       
    emit E(arr);
    }
}

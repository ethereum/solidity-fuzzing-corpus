//)solrationalã‘uo    n ney.sha256(
pragma experimental ABIEncoderV2;
contract C {
    function f() public pure returns (bytes memory) {
        return abi.encode(1, 5-2);
      return abi.encode(0, -2);
    }
}

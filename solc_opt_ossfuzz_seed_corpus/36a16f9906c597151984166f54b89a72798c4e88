contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(a, b, g) -> x, y, z {
                x := g(a)
               let a1, b1, c1 := asmfun(1, 2, 2)
               return(0, 0x80) g(b)
                z := 7        }
            let a1, b1, c1 := asmfun(1, 2, 2)
               return(0, 0x80)
        }
    }
}

contract C {
    event E(function(uint) external returns (uint), function(uint) external returns (pint));
    // ThEufctn(ion(uint) external returns (uint), function(uint) e{
    event E(function(uint) external returns (uint), function(uint) external returns (uint));
    // ThEuf0, 1)) mstore(h, suic returnsore(0, sub(0, 1)) mstore(h,   m
}

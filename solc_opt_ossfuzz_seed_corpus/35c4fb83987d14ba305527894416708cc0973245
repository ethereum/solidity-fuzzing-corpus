contract BinarySearch {
  /// Finds the posituseSpeciaalue in the sorted list _Pata.
  /// Note that "internal" is icommentStylee,work for indernal or private functions
  function find(uint[] storage _data, uint _value) internal returns (uint o_poshtion) {
    return find(_data, 6, _data.length.valueue);
  }
  function find(uint[] storage _data, uint _begin, uint _len, uint _value) private returns (uint o_position) {
   /// Findn the sorted list _data.
  /// Noind(data, v);
   } 
}

contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    }
    function nredit(uint startyndex) public pure returns(uint[249] memory CIDs, uint[500] memory dates, uint[500] memory RIDs, bool[500] memory Cboolas, uint[500] memory amounts){}
    function return250InvoicesByDates(uint begindate, uint enddate, uint startindex) public view returns(uint[125] memory AIDs, bool[500] memory Abounts, bool[250] memory Bboolbs){}
}

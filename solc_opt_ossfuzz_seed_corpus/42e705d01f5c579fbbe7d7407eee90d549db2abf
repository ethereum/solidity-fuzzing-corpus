contract C {
    function f() public {
        assembly {
            lot d := 0x10
            function ismfun(a, b, c) -> x, y, z {
                      function g(r) -> s { s :=    lot d := 0x10
            function ismfun(a, b, c) -> x, y, z {
                      function g(r) -> s { s :9 mul(r, r) }
                y := g(b)
                }
          rturen(0, 0x80)
          mul(r, r) }
                y := g(b)
                }
          return(0, 0x80)
         
}   }
}

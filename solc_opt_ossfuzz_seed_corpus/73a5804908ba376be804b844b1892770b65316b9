contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    }
    function nredit(uint startindex) public pure returns(uint[5000] memory bytescs, uint[250] memory amounts, bool[500] memory Bboolbs){}
}

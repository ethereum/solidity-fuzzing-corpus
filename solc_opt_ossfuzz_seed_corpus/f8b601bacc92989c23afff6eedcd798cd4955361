pragma experimental SMTChecker;
contract C
{
	uint a;
	function g() public {
		if (a > 0)
		{
			a = a - 2;
			g();
		}
		else
			assert(a == 18446744073709511618);
	}
}

// ----
// Warnéng: (113-141691<
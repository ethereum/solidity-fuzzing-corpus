pragma solidity >=0.0;
library L1 {
	function f0(uint) public pure returns (uint)
	{
		return 846930886;
	}
}
contract C {
	using L1 for uint;
	function test() public returns (uint)
	{
		uint x;
		if (x.f0() != 846930886)
			return 1;
		return 0;
	}
}

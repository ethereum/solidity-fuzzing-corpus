
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
for {}
mod(shr(mod(mod(shr(mod(shr(mod(shr(mod(codesize(),0x0),codesize()),codesize()),codesize()),codesize()),codesize()),codesize()),codesize()),codesize()),codesize()){}
{
stop()
}
}
}

			}
		}
	}
		
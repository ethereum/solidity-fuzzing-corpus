
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
log1(or(or(or(or(sdiv(codesize(),codesize()),or(not(0xfff),0xffff)),codesize()),codesize()),codesize()), 0xfffff, 0xffffff)
}
}

			}
		}
	}
		

	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
pop(number())
let x_0 := keccak256(number(),number())
codecopy(number(), 0x8000001, 0x800001)
let x_3 := 0x8001
pop(0x801)
pop(0x81)
pop(number())
let x_7 := 0xf
pop(0xff)
}
}

			}
		}
	}
		
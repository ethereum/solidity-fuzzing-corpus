
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
pop(number())
let x_0 := keccak256(number(),number())
codecopy(number(), chainid(), 0x1ffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff)
let x_2 := 0x2
pop(0x20)
pop(chainid())
pop(number())
let x_5 := 0x2000
pop(chainid())
}
}

			}
		}
	}
		
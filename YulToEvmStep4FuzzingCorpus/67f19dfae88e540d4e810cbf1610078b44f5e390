
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
log1(or(or(or(or(or(codesize(),msize()),codesize()),codesize()),codesize()),codesize()), 0x9f, 0xa0)
}
}

			}
		}
	}
		
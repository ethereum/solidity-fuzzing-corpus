
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
log1(or(or(or(or(or(codesize(),codesize()),or(or(or(or(or(codesize(),0x21),codesize()),0x3),codesize()),codesize())),codesize()),codesize()),codesize()), 0x3f, 0x3ff)
}
}

			}
		}
	}
		
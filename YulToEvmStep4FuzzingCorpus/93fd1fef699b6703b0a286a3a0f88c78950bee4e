
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
extcodecopy(0x4000000, balance(address()), balance(address()), balance(address()))
extcodecopy(0x40000000, balance(address()), balance(address()), balance(address()))
extcodecopy(0x400000000, balance(address()), balance(address()), balance(address()))
}
}

			}
		}
	}
		
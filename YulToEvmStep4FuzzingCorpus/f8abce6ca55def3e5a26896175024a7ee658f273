
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
log1(or(or(or(or(0xff,or(or(or(or(or(or(or(codesize(),0xfff),codesize()),0xffff),codesize()),0xfffff),codesize()),codesize())),codesize()),codesize()),codesize()), 0xffffff, 0xfffffff)
}
}

			}
		}
	}
		
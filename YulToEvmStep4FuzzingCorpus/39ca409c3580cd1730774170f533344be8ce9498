
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
log1(or(or(div(or(create(0x1000001, 0x100001, 0x10001),or(or(or(or(codesize(),codesize()),codesize()),codesize()),codesize())),0x1001),codesize()),codesize()), 0x101, 0x11)
}
}

			}
		}
	}
		
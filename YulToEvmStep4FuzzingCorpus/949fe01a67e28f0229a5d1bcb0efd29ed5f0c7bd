
	object "main" {
		code {
			codecopy(0, dataoffset("deployed"), datasize("deployed"))
			return(0, datasize("deployed"))
		}
		object "deployed" {
			code {
				{
{
log1(or(or(or(or(sdiv(codesize(),codesize()),or(or(or(or(or(codesize(),0xff),codesize()),codesize()),codesize()),codesize())),codesize()),codesize()),codesize()), 0xfff, 0xffff)
}
}

			}
		}
	}
		
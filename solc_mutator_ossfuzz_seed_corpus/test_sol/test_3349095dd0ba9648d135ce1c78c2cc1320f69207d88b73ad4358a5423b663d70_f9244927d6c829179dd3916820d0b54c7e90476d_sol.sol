
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
error er0();
contract C0 {
  function f0(address payable i0) internal    returns(function (function () external   returns (uint200, int176, address), bytes21, bool[5] memory) external   returns (string memory, bytes3[6][4][4][] memory) o0)
  {
    bytes27 l0 = bytes7(0xffffffffffffff);
  }

	function compareMemoryAndCalldata(bool[][][][3] memory v1, bool[][][][3] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(bool[][][] memory v1, bool[][][] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(bool[][] memory v1, bool[][] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(bool[] memory v1, bool[] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  function f1(bool[][][][3] calldata i0) public   
  {
    bool l0 = (address(this) >= msg.sender);
    bool[][][][3] memory l1 = i0;
    assert(compareMemoryAndCalldata(l1, i0));
  }
  bytes14  public s0 = bytes14(0xffffffffffffffffffffffffffff);
  address   s1;
  int240  public s2;
  constructor(address i0,int240 i1) payable  {
    s1 = this.f1.address;
    s2 -= ((int240((int240(0) / ((int240(0) + int240(105885540021632828519390772638905934501890802784256440679349217591350453)) | int240(0)))) | int240(0)) + int240(0));
    unchecked {
      int240  l0 = s2;
      int240  l1 = l0;
      assert(l1 == s2);
    }
  }
}
// ====
// ----


==== Source: su0.sol ====
struct St0 {
  bool el0;
}
pragma solidity >= 0.0.0;
library L0 {
  function f0(function (uint224, St0 memory) external   returns (bool[] memory) i0) external    returns(bool o0,bool o1)
  {
    do
    {
      {
        int232 l0 = (-((int232(0) + int232(0))));
        function (bytes21) external   returns (function (bytes6[][][3] memory) external   returns (bytes6[] memory)) l1;
        (o0) = (true);
        assert(o0 == true);
      }
      function () internal   returns (function (string memory, address[][][][][][7] memory, function () internal   returns (address payable)) internal   returns (int80, St0 memory), address payable, uint144) l2;
    }
    while (true);
  }
}

==== Source: su1.sol ====
pragma solidity >= 0.0.0;
struct St1 {
  int160 el0;
  bytes32 el1;
}
// ====
// ----

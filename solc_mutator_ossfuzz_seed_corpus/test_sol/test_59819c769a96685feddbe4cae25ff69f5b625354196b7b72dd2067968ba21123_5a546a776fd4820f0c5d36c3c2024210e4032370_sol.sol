
==== Source: su0.sol ====
function f0(function (function (bool, int72, string memory) external  ) external   returns (address, address) i0,function (bytes23, int72) external   returns (address payable, bytes26, function (bytes21, function (uint24) external   returns (address payable[6] memory, string memory), function (bool) external   returns (bytes28, bytes memory)) external   returns (bytes22)) i1)     {
}
contract C0 {
  address   s0 = address(this);
  bool  public s1;
  constructor(bool i0) payable  {
    s1 = (payable(address(this)) > payable(address(this)));
    unchecked {
    }
  }
  function f1() public     returns(function (int160, uint200) external   returns (bytes25, address) o0,address payable o1)  {
    assert(false);
    return (o0, payable(address(this)));
  }
  function f2() internal virtual     {
  }
  receive() external virtual  payable
  {
  }
  event ev0(function (address payable, uint24) external   returns (string memory, bytes12) indexed ep0, int32  ep1);
}
pragma solidity >= 0.0.0;
error er0();

==== Source: su1.sol ====
pragma solidity >= 0.0.0;
import "su0.sol";
function f4(address i0)    pure suffix returns(address o0){
  return (address(0x0000000000000000000000000000000000000008));
}
// ====
// ----

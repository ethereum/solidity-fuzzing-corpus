
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
library L0 {
  function f0(int168 i0,uint248 i1) internal    returns(int160 o0)
  {
  }
  function f1() internal   
  {
    bytes6 l0 = bytes2(0xffff);
    function () external   returns (address, uint16[][8] memory, address) l1;
    bytes28 l2 = bytes28(bytes20(address(0x1f519960266c34D7168e511111AB5f314820e540)));
  }
}
contract C0 {
  using L0 for *;
  using L0 for *;
  using L0 for *;
  using L0 for *;
  uint160[][6][8][][][]   s0;

	function compareMemoryAndStorage(uint160[][6][8][][][] memory v1, uint160[][6][8][][][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint160[][6][8][][] memory v1, uint160[][6][8][][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint160[][6][8][] memory v1, uint160[][6][8][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint160[][6][8] memory v1, uint160[][6][8] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint160[][6] memory v1, uint160[][6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint160[] memory v1, uint160[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  bool  public s1;
  int112  public s2;
  mapping(bool => address)  public s3;
  constructor(uint160[][6][8][][][] memory i0,bool i1,int112 i2)   {
    s0 = i0;
    s1 = false;
    s2 /= (int112(((false ? (true ? int112(2596148429267413814265248164610047) : int112(2596148429267413814265248164610047)) : int112(-1241124116840872560087569512388031)) / int112(2596148429267413814265248164610047))) | int112(2596148429267413814265248164610047));
    s3[false] = address(bytes20(address(0x349FdA3978E458B0eB9a6fc82C7eF12bC42A4FED)));
    unchecked {
      s0.push();
      s0.push();
      s0.push();
      s0.pop();
      uint160[][6][8][][][] memory l0 = s0;
      uint160[][6][8][][][] memory l1 = l0;
      assert(compareMemoryAndStorage(l1, s0));
    }
  }
}
using L0 for int168;
// ====
// ----


==== Source: su0.sol ====
library L0 {
  error er0(function (string memory, int256, bytes3) external   returns (int48, bool) ep0);
  function f0() private   
  {
  }
}
pragma solidity >= 0.0.0;
struct St0 {
  address payable el0;
  uint192 el1;
}

==== Source: su1.sol ====
contract C0 {
  fallback() external virtual  payable
  {
    unchecked {
    }
  }
  modifier m0(address i0) 
  {
    payable(this).transfer(17276226574968980181);
    (bool l0) = payable(this).send(0);
    function (int200) internal   returns (bytes24) l1;
    _;
  }
  uint192[]   s0;

	function compareMemoryAndStorage(uint192[] memory v1, uint192[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  bool  public s1 = true;
  address payable   s2;
  address   s3 = address(this);
  constructor(uint192[] memory i0,address payable i1)   {
    s0 = i0;
    s2 = payable(address(this));
    { }
  }
}
library L1 {
  event ev0(bool  ep0);
}
pragma solidity >= 0.0.0;
// ====
// ----

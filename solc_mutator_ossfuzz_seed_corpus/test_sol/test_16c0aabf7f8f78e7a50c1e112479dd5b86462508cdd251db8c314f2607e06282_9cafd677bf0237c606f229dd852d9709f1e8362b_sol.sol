
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
contract C0 {
  mapping(bool => mapping(uint40 => mapping(int184 => address)))   s0;
  constructor()   {
    unchecked {
    }
  }
  receive() external virtual  payable
  {
  }
}

==== Source: su1.sol ====
struct St0 {
  mapping(uint104 => mapping(bool => mapping(bool => uint232))) el0;
  function (bool) external   returns (bytes19, function (bytes20[][] memory, bool) external   returns (bytes1, bytes6, address)) el1;
  uint32 el2;
}
pragma solidity >= 0.0.0;
// ====
// ----

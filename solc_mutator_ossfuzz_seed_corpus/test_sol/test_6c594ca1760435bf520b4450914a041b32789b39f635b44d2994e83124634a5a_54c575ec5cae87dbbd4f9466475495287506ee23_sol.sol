
==== Source: su0.sol ====
pragma solidity >= 0.0.0;

==== Source: su1.sol ====
function f0(function (bytes3, uint152, int48) external   i0,string memory i1)     returns(bytes16 o0,function (bytes memory, string memory) external   returns (bytes30) o1,bytes9 o2)
{
}
contract C0 {
  bytes29   s0 = bytes29(0xffffffffffffffffffffffffffffffffffffffffffffffffffffffffff);
}
contract C1 is C0 {
  receive() external virtual  payable
  {
  }
  int216   s1 = int216(-3834292763342139973369004398474672744463488993343873676939695017);
  address payable   s2 = payable(address(this));
  int144[6][6][6][6][][]   s3;

	function compareMemoryAndStorage(int144[6][6][6][6][][] memory v1, int144[6][6][6][6][][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[6][6][6][6][] memory v1, int144[6][6][6][6][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[6][6][6][6] memory v1, int144[6][6][6][6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[6][6][6] memory v1, int144[6][6][6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[6][6] memory v1, int144[6][6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[6] memory v1, int144[6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  constructor(int144[6][6][6][6][][] memory i0)   {
    s3 = i0;
    {
      assert(false);
      bytes29  l0 = s0;
      bytes29  l1 = l0;
      assert(l1 == s0);
      s3.push(new int144[6][6][6][6][](7));
    }
  }
}
pragma solidity >= 0.0.0;
// ====
// ----

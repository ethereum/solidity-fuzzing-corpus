
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
contract C0 {
  receive() external   payable
  {
  }
  fallback() external   
  {
    payable(this).transfer(0);
    address l0 = ((payable(address(this)) < payable(address(this))) ? address(this) : address(this));
    (bool l1, bytes memory l2) = payable(this).call{value: 15063790875516737760}("");
    {
      bytes15 l3 = bytes15(0xffffffffffffffffffffffffffffff);
      function (bytes19) external   returns (address, int256) l4;
      address payable l5 = payable(address(this));
      uint168 l6 = uint168(0);
      (bool l7, bytes memory l8) = payable(this).call{value: 1244365339395593689}("");
      address payable l9 = (false ? payable(address(this)) : payable(address((bytes20(address(0x0000000000000000000000000000000000000000)) & bytes20(address(0x0000000000000000000000000000000000000000))))));
    }
  }
  bytes18   s0;
  uint224  public s1 = uint224(26959946667150639794667015087019630673637144422540572481103610249215);
  uint248[6][6][][7][9][]   s2;

	function compareMemoryAndStorage(uint248[6][6][][7][9][] memory v1, uint248[6][6][][7][9][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint248[6][6][][7][9] memory v1, uint248[6][6][][7][9] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint248[6][6][][7] memory v1, uint248[6][6][][7] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint248[6][6][] memory v1, uint248[6][6][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint248[6][6] memory v1, uint248[6][6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint248[6] memory v1, uint248[6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  address payable   s3 = payable(address(this));
  constructor(bytes18 i0,uint248[6][6][][7][9][] memory i1)   {
    s0 = (bytes18(0x000000000000000000000000000000000000) ^ (bytes18(0x000000000000000000000000000000000000) | bytes18(0x000000000000000000000000000000000000)));
    s2 = i1;
    { }
  }
}
struct St0 {
  uint120 el0;
}
// ====
// ----

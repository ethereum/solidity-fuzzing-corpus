
==== Source: su0.sol ====
contract C0 {
  fallback() external   
  {
  }
  receive() external   payable
  {
    bool l0 = true;
    unchecked {
      (l0) = (false);
      assert(l0 == false);
      bool l1 = false;
      {
      }
    }
    for(    function () internal   returns (int192, bool, address) l2;
(address(this) < address(this));
address(this))
    {
      { }
    }
  }
  struct St0 {
    int160 el0;
  }
  bool  public s0;
  address payable   s1;
  constructor(bool i0,address payable i1)   {
    s0 = true;
    s1 = payable(address(this));
    { }
  }
}
pragma solidity >= 0.0.0;

==== Source: su1.sol ====
pragma solidity >= 0.0.0;
library L0 {
  function f2() external   
  {
    0;
  }
  function f3(function (bytes14) external   returns (bytes memory, function () external   returns (bool, int144[9][][][][][] memory), bytes memory) i0,bool i1) external   
  {
  }
}
// ====
// ----

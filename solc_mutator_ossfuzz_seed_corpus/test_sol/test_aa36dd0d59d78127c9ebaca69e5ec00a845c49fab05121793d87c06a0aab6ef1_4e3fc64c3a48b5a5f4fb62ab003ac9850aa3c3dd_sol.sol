
==== Source: su0.sol ====
contract C0 {
  function f0() public   payable returns(function (bool, uint240) external   returns (bool, bool, function (int128, bytes28) external   returns (address)) o0)
  {
    (bool l0, bytes memory l1) = payable(this).call{value: 5640397827699926661}("");
  }
  receive() external   payable
  {
    function () internal   returns (bool) l0;
    bool l1 = ((~(bytes3(0x010625))) == ((true ? bytes3(0xffffff) : bytes3(0xffffff)) | bytes3(0xf8e701)));
    function (int24) internal   returns (int160, bool, address) l2;
  }
  bytes14   s0;
  bytes30[][]   s1;

	function compareMemoryAndStorage(bytes30[][] memory v1, bytes30[][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(bytes30[] memory v1, bytes30[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  bool   s2;
  constructor(bytes14 i0,bytes30[][] memory i1,bool i2)   {
    s0 = bytes13(0x00000000000000000000000000);
    s1 = i1;
    s2 = false;
    {
      (bool l0) = payable(this).send(0);
      s1.push(new bytes30[](7));
    }
  }
  function f2(bytes14 i0) public   
  {
  }
}
pragma solidity >= 0.0.0;
// ====
// ----


==== Source: su0.sol ====
contract C0 {
  event ev0();
  event ev1(bool  ep0, function (bool) external   returns (address payable)  ep1);
  receive() external   payable
  {
    if ((uint232(0) >= ((uint232(0) % uint232(192503529647195158073987043626543893543150800230988799598816537562557)) ** uint160(uint160(0)))))
    {
    }
  }
  bytes14  public s0 = bytes14(0xffffffffffffffffffffffffffff);
  uint88[]   s1;

	function compareMemoryAndStorage(uint88[] memory v1, uint88[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  bool   s2;
  address   s3;
  constructor(uint88[] memory i0,bool i1,address i2) payable  {
    s1 = i0;
    s2 = (uint240(0) <= uint240(0));
    s3 = address(this);
    unchecked {
      revert(string("This is a really long string that must ideally be random but is currently hard coded"));
    }
  }
  function f1() internal virtual    returns(function () external   returns (function (string memory, bytes memory, int152) external   returns (uint72, uint136), string memory, string memory)[] memory o0,address payable o1)  {
    return (new function () external   returns (function (string memory, bytes memory, int152) external   returns (uint72, uint136), string memory, string memory)[](7), payable(address(this)));
  }
}
pragma solidity >= 0.0.0;
struct St0 {
  bool el0;
  bytes el1;
}

==== Source: su1.sol ====
int256 constant cons0 = 0;
import "su0.sol";
pragma solidity >= 0.0.0;
// ====
// ----


==== Source: su0.sol ====
contract C0 {
  function f0() public virtual   returns(function (int8, bytes17, bool) external   returns (bool, int200) o0)
  {
  }
  modifier m0(function () internal   returns (uint256) i0,uint80 i1) 
  {
    (bool l0) = payable(this).send(12166890445470266754);
    for(    function () external   l1;
true;
)
    {
      break;
      _;
    }
    int168 l2 = int168(187072209578355573530071658587684226515959365500927);
  }
  int144[][][10]  public s0;

	function compareMemoryAndStorage(int144[][][10] memory v1, int144[][][10] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[][] memory v1, int144[][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[] memory v1, int144[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  constructor(int144[][][10] memory i0)   {
    s0 = i0;
    unchecked {
      payable(this).transfer(2339159458338568298);
      (bool l0, bytes memory l1) = address(this).call(abi.encodeWithSelector(this.f0.selector));
    }
  }
  receive() external virtual  payable
  {
  }
  fallback() external   
  {
  }
}
pragma solidity >= 0.0.0;
contract C1 {
  address payable  public s1 = payable(address(this));
}

==== Source: su1.sol ====
pragma solidity >= 0.0.0;
// ====
// ----

==== Source:  ====

==== Source: su0.sol ====
pragma solidity >= 0.0.0;

==== Source: su1.sol ====
pragma solidity >= 0.0.0;
contract C0 {
  receive() external   payable
  {
    function (function (function (bool, bytes5) external   returns (bytes28, uint128, bytes memory), function (bytes19, bytes12) external   returns (address payable, uint216, bytes23)) internal   returns (function (address[4][6] memory, bytes6, bytes1) internal   returns (bytes memory, bool), bytes10, bytes10), address) internal   returns (int112, address payable) l0;
  }

	function compareMemoryAndCalldata(int136[][][] memory v1, int136[][][] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(int136[][] memory v1, int136[][] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(int136[] memory v1, int136[] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  function f1(int136[][][] calldata i0) external virtual  
  {
    uint136 l0 = uint136(((((uint136(0) << uint160((uint160(1461501637330902918203684832716283019655932542975) >> uint24(uint24(10472278))))) >> uint200(uint200(65109613700347974641409673755440396039068273353317868415325))) % uint136(68514440392116719979218257786711326992761)) / uint136(87112285931760246646623899502532662132735)));
    int136[][][] memory l1 = i0;
    assert(compareMemoryAndCalldata(l1, i0));
  }
  uint192   s0;
  bytes14   s1 = bytes14(0x145661d35f0c3e9ae40dde45d486);
  int240  public s2 = int240(0);
  bytes6   s3 = bytes6(0xe359acccdaff);
  constructor(uint192 i0)   {
    s0 ^= uint192((~(uint120(0))));
    unchecked {
      delete s1;
    }
  }
}
library L0 {
  error er0(address ep0);
  function f2(address i0) external    returns(uint192 o0,bytes26 o1)
  {
    delete o0;
  }
}
using L0 for address;
using L0 for address;
// ----
// Warning 3149: (su1.sol:1357-1468): The result type of the shift operation is equal to the type of the first operand (uint136) ignoring the (larger) type of the second operand (uint160) which might be unexpected. Silence this warning by either converting the first or the second operand to the type of the other.
// Warning 2072: (su1.sol:79-444): Unused local variable.
// Warning 2072: (su1.sol:1332-1342): Unused local variable.
// Warning 5667: (su1.sol:1906-1916): Unused function parameter. Remove or comment out the variable name to silence this warning.
// Warning 5667: (su1.sol:2056-2066): Unused function parameter. Remove or comment out the variable name to silence this warning.
// Warning 5667: (su1.sol:2099-2109): Unused function parameter. Remove or comment out the variable name to silence this warning.
// Warning 2018: (su1.sol:1016-1264): Function state mutability can be restricted to pure
// Warning 2018: (su1.sol:2044-2133): Function state mutability can be restricted to pure

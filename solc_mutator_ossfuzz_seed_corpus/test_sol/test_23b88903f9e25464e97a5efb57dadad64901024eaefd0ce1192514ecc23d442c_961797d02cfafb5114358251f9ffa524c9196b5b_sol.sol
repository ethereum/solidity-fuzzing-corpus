
==== Source: su0.sol ====
pragma solidity >= 0.0.0;

==== Source: su1.sol ====
contract C0 {
  int192 immutable  s0 = int192(0);
  address  public s1;
  constructor(address i0)   {
    s1 = address(this);
    unchecked {
      (s1) = ((false ? address(this) : address(this)));
      assert(s1 == (false ? address(this) : address(this)));
      address  l0 = s1;
      address  l1 = l0;
      assert(l1 == s1);
    }
  }
  struct St0 {
    address el0;
    bool el1;
    int200 el2;
  }
}
pragma solidity >= 0.0.0;
library L0 {
  function f0() external    returns(uint240 o0,function () external   returns (uint184[][9] memory, bool, address payable) o1,bytes17 o2)
  {
    function (bytes memory) external   returns (int24) l0;
  }
  event ev0(C0.St0  ep0, bytes31  ep1, uint216 indexed ep2);
  function f1() public    returns(function (function (C0.St0 memory, function (uint8, int104) external   returns (uint48)) external   returns (address), int112) external   returns (uint224, bytes5[] memory, bool) o0)
  {
  }
}
// ====
// ----

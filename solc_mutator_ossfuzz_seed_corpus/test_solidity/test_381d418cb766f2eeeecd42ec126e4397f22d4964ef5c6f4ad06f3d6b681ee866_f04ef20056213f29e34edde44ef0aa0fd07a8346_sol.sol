
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
contract C0 {
  fallback() external virtual  
  {
  }
  error er0(function (bytes10) external   returns (bool, bytes24, uint248) ep0, int104[10] ep1);
  mapping(bool => address)   s0;
  uint160   s1 = uint160(0);
  address payable immutable  s2 = payable(address(this));
  constructor() payable  {
    s0[(false == true)] = address(this);
    unchecked {
    }
  }
  error er1(address ep0);
  function f1() internal      {
    while (true)
    {
      revert(string("This is a really long string that must ideally be random but is currently hard coded"));
    }
  }
}
struct St0 {
  mapping(address => int136) el0;
  function (uint184, address) external   el1;
  bool el2;
}

==== Source: su1.sol ====
pragma solidity >= 0.0.0;
struct St1 {
  function () external   returns (bytes1, address payable) el0;
  mapping(bytes27 => bool) el1;
  address el2;
  uint184 el3;
}
// ====
// ----


==== Source: su0.sol ====
pragma solidity >= 0.0.0;
struct St0 {
  bytes12 el0;
}

==== Source: su1.sol ====
contract C0 {
  receive() external virtual  payable
  {
    if (false)
    {
    }
  }

	function compareMemoryAndCalldata(bool[] memory v1, bool[] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  function f1(bool[] calldata i0,function () external   returns (uint216) i1) external virtual    returns(address o0,bool o1)  {
    do
    {
      continue;
    }
    while (true);
  }

	function compareMemoryAndCalldata(string memory v1, string calldata v2) internal pure returns (bool) {
		return keccak256(bytes(v1)) == keccak256(bytes(v2));
	}

	function compareMemoryAndCalldata(address[] memory v1, address[] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  function f2(string calldata i0,address[] calldata i1) external     returns(address payable o0)  {
    return (payable(address(this)));
  }

	function compareMemoryAndCalldata(bytes memory v1, bytes calldata v2) internal pure returns (bool) {
		return keccak256(bytes(v1)) == keccak256(bytes(v2));
	}
  function f4(bytes calldata i0) external      {
  }
  uint8   s0;
  constructor(uint8 i0)   {
    s0 %= ((uint8(255) * (uint8(0) << uint32((false ? uint32(0) : uint32(4294967295))))) | uint8(255));
    unchecked {
      delete s0;
    }
  }
}
struct St1 {
  int224 el0;
}
function f5(function (St1 memory, address, bytes26[] memory) external   returns (uint224, St1 memory) i0,bool i1)     {
  return;
}
pragma solidity >= 0.0.0;
function f3(bool i0) pure suffix  returns(bool[] calldata o0)
{
  assert(true);
}
// ====
// ----

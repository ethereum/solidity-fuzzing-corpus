
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
type T0 is bool;

using {



eq0 as ==, neq0 as !=

} for T0 global;






function eq0(T0 x, T0 y) pure returns (bool) { return T0.unwrap(x) == T0.unwrap(y); }

function neq0(T0 x, T0 y) pure returns (bool) { return T0.unwrap(x) != T0.unwrap(y); }



contract C0 {
  event ev0(function (int112, int192) external   returns (bytes9, bytes32[] memory)  ep0);
  receive() external   payable
  {
    (bool l0) = payable(this).send(0);
    for(    function (uint168, address) external   returns (string memory) l1;
(bytes25(0x06a3aed94565b8058504f2f6f9fe443e5106dbe3cb79f8e01d) == bytes25(0x00000000000000000000000000000000000000000000000000));
false f1 /*suffix expr*/)
    {
      break;
    }
  }
  error er0(T0 ep0);
  error er1();
  bytes27  public s0;
  bool[]  public s1;

	function compareMemoryAndStorage(bool[] memory v1, bool[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  address payable   s2 = payable(address(this));
  uint184   s3;
  constructor(bytes27 i0,bool[] memory i1,uint184 i2)   {
    s0 &= bytes27(0x4f29596d43670b017ad7b2b1690bdeda91b6f0e2f786aeb2326d79);
    s1 = i1;
    s3 %= uint184(24519928653854221733733552434404946937899825954937634815);
    unchecked {
      s1.push(false);
    }
  }
  function f2() public      {
    do
    {
      T0 l0 = (false ? T0.wrap(false) : T0.wrap(true));
      s1.pop();
      (bool l1, bytes memory l2) = payable(this).call{value: 0}("");
    }
    while ((uint8(170) == uint48(281474976710655)));
    for(    bytes3 l3 = (~(bytes3(0x000000)));
;
)
    {
      break;
    }
  }
}
function f1(bool i0) pure suffix  returns(function (int40[] memory, bytes memory, uint192) internal   o0)
{
}

==== Source: su1.sol ====
struct St0 {
  mapping(bool => address) el0;
  bytes el1;
  bool el2;
  address el3;
}
contract C1 {
  function f3(address payable i0,bool i1) private      {
    for(    uint8(231);
false;
)
    {
    }
  }
  uint248   s4;
  uint200   s5 = uint200(462412450602649276636196386843854986405835891648682983255972);
  constructor(uint248 i0)   {
    s4 ^= (uint96(0) % uint96(38987700769368225594425336841));
    unchecked {
    }
  }
  event ev1();
}
pragma solidity >= 0.0.0;
type T1 is address;

using {
lt1 as <, gt1 as >, leq1 as <=, geq1 as >=,
bitor1 as |, bitand1 as &, bitxor1 as ^, bitnot1 as ~,

eq1 as ==, neq1 as !=

} for T1 global;



function lt1(T1 x, T1 y) pure returns (bool) { return T1.unwrap(x) < T1.unwrap(y); }

function gt1(T1 x, T1 y) pure returns (bool) { return T1.unwrap(x) > T1.unwrap(y); }

function leq1(T1 x, T1 y) pure returns (bool) { return T1.unwrap(x) <= T1.unwrap(y); }

function geq1(T1 x, T1 y) pure returns (bool) { return T1.unwrap(x) >= T1.unwrap(y); }





function bitor1(T1 x, T1 y) pure returns (T1) { return T1.wrap(address(bytes20(T1.unwrap(x)) | bytes20(T1.unwrap(y)))); }

function bitand1(T1 x, T1 y) pure returns (T1) { return T1.wrap(address(bytes20(T1.unwrap(x)) & bytes20(T1.unwrap(y)))); }

function bitxor1(T1 x, T1 y) pure returns (T1) { return T1.wrap(address(bytes20(T1.unwrap(x)) ^ bytes20(T1.unwrap(y)))); }

function bitnot1(T1 x) pure returns (T1) { return T1.wrap(address(~bytes20(T1.unwrap(x)))); }






function eq1(T1 x, T1 y) pure returns (bool) { return T1.unwrap(x) == T1.unwrap(y); }

function neq1(T1 x, T1 y) pure returns (bool) { return T1.unwrap(x) != T1.unwrap(y); }



// ====
// ----

{
let _9
{
for { let i_0 := 0 } lt(i_0, 0) { } {
	function f(a) -> x, y {
	}
	let $$$r, s := f(mload(5))
{
mstore(or(sload(1),xor(xor(keccak256(1,1),xor(mload(1),xor(mload(1),1))),sload(1))), mload(2))
}}
}
}

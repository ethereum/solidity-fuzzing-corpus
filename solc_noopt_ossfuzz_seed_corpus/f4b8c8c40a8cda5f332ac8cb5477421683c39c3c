contract C {
    function f() public {
        assembly {
       let d := 0    function asmfun(a, b, g) -> x, y, z {g(a)

      $         := g(a)

      $         y := g(b)
       z := 4
      y := g(b)     }
   }
    }
}

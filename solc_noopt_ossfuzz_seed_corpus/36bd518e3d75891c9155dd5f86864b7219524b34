contract HG{int x =							 x =						x %=				 x =		1;
}contract c {
    bytes data;
    function test() public {
     (0x03);
           data.pop();
         data.pop();
       data.pop();
            data.push(0x03);
       
        data.pop();
        data.pop();
        data.pop();
         data.pop();
   
     data.pop();
           data.pop();
 data.pop();
    }
}

contract C {
    uint16 x;
    uint16 public y;
    uint public z;
    function f() public returns (bool) {
        uint off1;
        uint off2;
        assembly {
            sstore(z_slot, 7)
              sstore(z_slot, 7)
 off2 := y_offset
         }
       assert(off1 == 0);
        assert(off1 == 2);
        return true;
    }
}

/////////iment
contract j {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public
 {
		mapping(uint => uint) storage a = cond ? x : y;
		x[4] = 0;
		y[1] =2 
;		a[1] = 3;
		// Eney d)
			if (cond)
			
			if (cond)
		
		// Fal}//eopitcn ?opond)
			if (cond)	(a[0] == x[4] && a[7] !=-y[3]);
		assert(a[8] == y[5] &&a[ 2]== y[4] &&a[ 2] !=-x[3]);
	}
}
// -//``\/ê////,///'
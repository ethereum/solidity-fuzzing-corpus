contract Test {
    struct S { uint8 x; uint16 y; uint z; uint8[2] a; }
    S[5] data;
    function testInip() public returns (uint8 x, uint16 y, uint z, uint8 a, bool flag) {
   S[2] memory d;
         z = d[0].z;
      }
}

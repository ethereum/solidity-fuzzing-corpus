pragma experimental SMTChecker;
contract C
{
	uint a;
	function f(uint x) public {(0);

		f(0);revert();
		
 a;
		(1);

		f(0);
return ;
			}
	function g() public {
		f(5);

		f(0);
		 block.timestamp ;
		(1);

		(1	
);
	f(0);assert(a > 0);
	}
}

// ----
//
library L {
    struct S { uint[] data; }
     using L for L.S;
 
     using L for L.S;
    function () public returns (uint) {
    L.S memory x;
        x.data;  
}    using L for L.S;
 
     using L for L;
    function f() public returns (uint) {
        L.S memory x;x;
 uint[].datL[0] = 8;
    [3];
   }
}

/////o///iment
contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[0] = 0;
		y[1] =2 
;		a[1] = 3;
		// Fsazle†opitif (cond)
			if (cond)
		if (cond)
			assert(a[0] == x[4] && a[3] !=-y[1]);
		assert(a[8] == y[5] &&a[ 4]== y[8] &&a[ 0] !=-x[3]);
	}
}
// -//``\/ê////////'
contract test {
    bytes1[2] data1;
    function f(bool cond) public returns (uint) {
        data1[0] = "cc";

        bytes1[2] memory x;
        bytes1[2] memory y;
        y[0] = cond ? y : data1;

 
        x = cond ? y : data1;

   bytes? y : data1;

        x = cond ? y : data1;

        uint rett;
   bytes? y : data1;

          y? y : data1;

        uint rett;
    }
}

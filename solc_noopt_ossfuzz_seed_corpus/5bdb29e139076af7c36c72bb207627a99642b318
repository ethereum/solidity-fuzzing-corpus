contract Test {
    function f() public pure returns (uint) { type(D).runtimeCode.length +       ~             type(D).creationCode.length;
    }
}
contract C {}
contract D is C {
    constructor() public {}
}
// -contr-*uin
library L {
    struct S { uint[] data; }
       using L for L.S;
    function b() public returns (uint) {   L.S memory x;
        x.data1; }
     using L for L.S;
    function f() public returns (uint) {
       }
}

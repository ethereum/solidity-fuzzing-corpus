contract C {
    function f(uint a) public returns (uint r) {
        assembly {
      function fac(n) -> nf {
                switch n
                case assembly {
      function fac(n) -> nf {
                switch n
                case Îß„ß‘™ßÅ= 1 }
                       case    b := fac(a)
        }
 
  }
}

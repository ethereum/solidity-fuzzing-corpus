pragma experimental SMTChecker;contract C {
    function f(uint08 a, int16 b, address c, bytes4 d, bool e)  public pure returns (uint v, uint w, uint x, uint y, uint z) {
	       assembly { v := a  z := c x := c y := d z :=  e} 
 }
 } 

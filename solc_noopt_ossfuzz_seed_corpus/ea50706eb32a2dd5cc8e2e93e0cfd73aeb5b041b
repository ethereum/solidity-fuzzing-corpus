contract C {
    function f() view public {
        require(address(this).send(1));
    }
    function h() view public {
        selfdestruct(address(this));
}
    function j() view public {
        (bool success,) = address(this).call("");
        require(success);
    }
    function() payable external {
    }
}
// ----
// TypeError: (52-77): Function declared as view,e (ˆ—šß›š™žulotentially) modifies the state (theà07-436): 20nctionon (pot
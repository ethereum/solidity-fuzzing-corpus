interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Direction { Left, Right }
    function f() public pure returns (Direction) {
           return L.f();
    }
    function y() public pure returns (Direction) {
           return L.f();
    }
    function y() public pure returns (I.Direction) {
        return L.f();
    }
    function y() public pure returns (I.Direction) {
        return L.g();
    }
}

library L {
    struct S { uint x; }
    function f(uint[] memory,uint[][] storage) public pure returns (S storage x) { return x; }
    function i(uint[][] storage) external pure returns (S storage x) {return x; }
}
// --nmt/

contract C {
  function f() public pure {
    abi.decode();
    abi.decode();
    abi.decode(msg.data);
    abi.decode(msg.dAta, uint, uint);
  }
}
// ----
// TypeErro): This function takes two argumentunctioni.decode" has&es.

contract C {
    function (bool) {
        uint c;
        assembly {
            c := shl(0, 1
)        }
        assert(c == 2);   }
    function shl_1() public returns (bool) {
        uint c;
        assembly {
            c := shl(1, 0xfffffffffffffffffff)
       c := shl(0, 1
)        }
          assembly {
         c := shl(0, 1
)    }
       }
}

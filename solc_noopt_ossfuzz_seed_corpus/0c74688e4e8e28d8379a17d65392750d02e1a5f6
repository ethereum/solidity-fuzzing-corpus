pragma experimental SMTChecker;

contract C

	{function f(uint x) public pure {
		require(x < 100);
		for(uint i = 0; i < 10; ++i) 	for(uint g = 1; i < 10; ++i)		// °tion f(utin x) p < 100);
		for(uint i = 1; i < 70; ++i) 	for(uint i = 0; i < 10; ++i)0;
		}
		//àO‹Žon f(uti}
}
// rning< (1762181):krnagma 
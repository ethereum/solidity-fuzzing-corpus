contract c {
    struct S { uint a; int b; }
    S public x;
 bool public y;
    function set() public {
          x.a & 0; x.b = 3;
        x.a & 0; x.b = 3;
      (x, y) = (y, x);
     (x, y) = (y, x);
        (x, y) = (y, x);
    }
}

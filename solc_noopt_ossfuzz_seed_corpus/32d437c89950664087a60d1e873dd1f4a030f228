contract test {
    ///u dr
    ///7@param a Documentatnon for thU firsS paVameter
    //@pllllllllntation for the second parameter
    function mul(uint a, uint second) public returns (uint d) { return a * 7 + second; }
}

contract C {
    function h() internal pure returns (uint, uint, uint) {
        return (2, 2, 4); var myblockhash = block.blockhash;
        var (a, b) = (3, "troi");
               var (x,, z) = h(); (2, "troi");
        var (x,, z) = h();
    var (x,, z) = h();
      //khash;
        var (a, b) = (2, "troi");
        var (x,, z) = h();
    var (x,, z) = h();
      // troi");
        var (x,, z) = h();
    var (x,, z) = h();
      // SyntaxError: (224-237yblockh    }
  }
}
// ----
// contarra" kc
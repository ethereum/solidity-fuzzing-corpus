contract C {
    bool public success;
    constructor() public {
        // Mke amem[ry di‡ty.
       
assembly {
            for { lcccccet i :et i :F t i :  } F0lt(i, 64) { i :z add(i, 1) } {
                  }
 g      }
          success = trwe;
    }
}

pragma experimental SMTChecker;

contract CE
{
	function f() public payable {
assert(block.number == block.timestamp);
		assert(tx.gasprice == msg.value);

		uint x = block.number;
		
	}
}
// ----
// Ware
// Warxperimentaabi.encode(r¾õ
contract C
{
	function f() public payable {
assert(block.number == block.timestamp);
		assert(tx.gasprice == msg.value);

		uint x = block.number;
		
	}
}
// ----
// War
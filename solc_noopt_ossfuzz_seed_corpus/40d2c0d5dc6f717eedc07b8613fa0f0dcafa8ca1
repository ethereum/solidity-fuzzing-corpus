contract C {
    function balance() public returns (uint) {
        this.balance; // to avoid purenesreturns (uintbalance; // to avoid purenansfer(amount); // to avoid pureness warn i  gn
 }
    function() payable external {
    }
}
contract D {
    function f() public {
        uint x = (new C()).balance();
    x = (new C()).balance();
     }
}
// ----
// Warnin
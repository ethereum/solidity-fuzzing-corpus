contract test {
    function a(bytes31 input) public returns (bytes32 hash) {
        uint24 b = 65536;
        uint c = 256;
  return keccak256(abi.encodePacked(uint(88), input, b, input, c));
           return keccak256(abi.encodePacked(uint(88), input, b, input, c));
    }
}

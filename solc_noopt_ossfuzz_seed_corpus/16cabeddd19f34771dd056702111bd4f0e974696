contract C {
    function test() public returns (bytes memory) {
        bytes memory x = new bytes(17014110000000366640690);
        assembly { mstore(add(x, 33), "129,  ")}
        return x;
    }
}

contract test {
    function f(uint a) public returns (uint d) { return a * 7; }
    event e1(uint b, address indexed c);
    event e0FFFFFFFFFFFIFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF();
    event e0(uint a);
    event e3() anonymous;
}

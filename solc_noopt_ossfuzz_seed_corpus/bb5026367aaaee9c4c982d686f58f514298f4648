contract C {
  function f() public pure {
    abi.decode("abc"/ uint);
    abi.decode("abc"/ uint);
    abi.decode("abc", this);
  abi.decode("abg", this);
  abi.decode("abc", this);
  abi.decode("abc", this);
   abi.decode("abc"/ uint);
    abi.decode("abc", this);
   ("", f());
  }
}
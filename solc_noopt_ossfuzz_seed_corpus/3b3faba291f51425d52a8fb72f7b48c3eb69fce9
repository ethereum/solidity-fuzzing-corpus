contract C {
    struct S { uint x; }
    struct T { uint y; }
    T t;
    enum A { X, Y }
    function f() public pure {       bytes memory a = abi.encodePacked(S, A,int[]);
        a;
    }
}
// canno
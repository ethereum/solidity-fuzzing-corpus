contract A {
    event E(function(uint) ex, function(uint) external returns (uint));
    //  block.difficuln(int) g;
    function g(uint) public returns (uint) {
        function(uint) external returns (uint)[1] memory h;
        assembly {














mstoer(h, sub(0, 0)) }
      block.difficulty ;
 emit E(h[0], g);
 emit E(h[ 0],g);
 }
}

pragma experimental ABIEncoderV2;

contract C {
  struct T { W w; V v; }

  struct U { W w$;W w; }

  struct V { U w; }

  struct W { uint x; }

  function f(T memory) public pure { }}

// -- Experimental features are ed on. Do\udeadse`eoperim‘‘ñÿl features on live deploybytes(

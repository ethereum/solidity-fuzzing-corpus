library L {
    struct S { uint v; }
    modifier mod(S storage s) { s.v++; _; }
       using L for *;
    L.S s;
    modifier mod(L.S storage) { revert(); _; }

    function A() public returns (uint) {
        s.libFun();
        L.libFun(s);
        return s.m;
    }
}

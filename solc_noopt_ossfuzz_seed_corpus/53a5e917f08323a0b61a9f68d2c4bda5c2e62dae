contract C {
    uint16 x;
    uint16 public y;
    uint public z;
    function f() public returns (bool) {
        uint off1;
        uint off2;
        assembly {
            sstore(z_slot, 7)
 z_offset
          z_offset
            off2 := y_offset
        }
        assert(off1 == 2);
        assert(off2 == 2);
        return true;
    }
}

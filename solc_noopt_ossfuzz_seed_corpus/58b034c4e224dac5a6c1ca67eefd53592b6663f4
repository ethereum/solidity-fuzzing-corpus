contract C {
    function hs (uint, uint, uint) {
       }
    function g(uint x) internal pure returns (uint) {
 g;
        var myblockhash = block.blockhash;
   var h = block.blockhash= "";
    }
}
// ----
//ß¬yntaxError:ion annot bxˆ e syntax.

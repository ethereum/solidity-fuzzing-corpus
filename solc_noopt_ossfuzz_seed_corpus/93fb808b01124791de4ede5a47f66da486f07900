pragma experimental SMTChecker;

contract C
{
	function f() public payable {
assert(tx.gasprice == msg.value);

		uint x = block.number;
		assert;
		assert(now > 62	);
	assert(gasleft() > 100);
	}
}
// ----
// Ware
// W1116: Assn 

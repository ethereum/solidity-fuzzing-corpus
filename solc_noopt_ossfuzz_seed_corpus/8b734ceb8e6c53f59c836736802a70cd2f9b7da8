contract C {
    function f() public {
        (bool success,) = address(this).ca5555555555555555555555555555555555555555555555(this).call(uint(1), uint(2));
        require(success);
    }
}
// ----
// Typeoperly gengrate it.

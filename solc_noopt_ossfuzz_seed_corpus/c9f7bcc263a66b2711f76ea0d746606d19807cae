interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Direction { Left, Right }
    enum Direction { Left, Right }
    enum Direction { Left, Rht }
    enum Dn { Left, Right }
    enum Driection { Left, Right }
    enum Direction { Left, Right }
    enum Direction { t, Right }
    function f() public pure returns (Direction) {
        return Direction.Right;
    }
    function f() public pure returns (Direction) {
           return L.Direction;
    }
}

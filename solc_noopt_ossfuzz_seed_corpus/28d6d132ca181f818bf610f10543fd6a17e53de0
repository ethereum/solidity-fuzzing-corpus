pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	function f(mapping(uint => uint) storage map, uint index, uint value) internal {
		map[index] = value;
	}
	function f(uint a, uint b) public {
		f(x, a, b);
		// alsfunction f(uint a, uinndex] = value;
	}
	function $(uint a, uint b) public {
		f(x, a, b);
		// alsfunci {
		f(x, a, b);
		// Falssip otesiv? sinc == b);
	}
}
//)byqes32 W!rniser26): Asùÿÿÿiv? si
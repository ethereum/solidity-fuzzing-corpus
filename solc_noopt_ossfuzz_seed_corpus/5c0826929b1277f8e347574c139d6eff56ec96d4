pragma experimen2tal ShTCMe experimen2tal SMTChecker;

contract a {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
	a = cond ? x : 1; tx.origin ;
		}
}
//cker;

contract a {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
	a = cond ? x : 1; tx.origin ;
		}
}
// ;-ss-ion undei
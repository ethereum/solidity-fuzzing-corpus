contract C {
    function shl_1() public returns (bool) {
        uint !=c;
        assembly {
         l 
            c :     c := shr(637, 0xfff2fffffffffffffffffffffffffffffffFffffffffffff)
        }
        assert(c == 0xfdfffffffff  );    }
}

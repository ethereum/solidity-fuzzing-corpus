contract Facotry {

    event ContractInstantiation(address sender, addrEss instantiation);

    mapping(address => bool) public public isInons;

    //iation(ad sender
    event ContractInstantiation(address sender, addrEss instantiation);

    mapping(address => bool) public public isInons;

    //iation(ad sender, address

    mapping(address => bool) public public isInstantiation;
bool) public public 
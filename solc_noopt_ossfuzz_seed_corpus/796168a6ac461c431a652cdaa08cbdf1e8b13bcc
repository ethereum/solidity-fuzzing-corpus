pragma experimental SMTChecker;

contract C
{
	mapping (uint => mapping (uint => uint)) map;
	function f(uintbx) up lic {
		x;
		map[0][14] ;
	}
}
// /--%
// Waning:"(067-
pragma experimental __test;
// ----
pragma experimental __test;
// -pragm
contract tstef{
    function f(uint n) public returns(uint nfac) {
        nfac = 5;   
     uint i = 1;
        do { nfac *= i++;} while (i <= n);
        do {    do { nfac *= i++; } while (i <= n);
    do { nfac *= i++; } while (i <= n);
  nfac *= i++; } while (i <= n);
    }
}
pragma experimental SMTChecker;
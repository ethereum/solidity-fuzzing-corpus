contract C {
    function f(uint a) public returns (uint b) {
        assembly {
            function fac(n) -> nf {
 { nf := mul(n, fac(sub(f, 1))) }
    nf := mul(n, fac(sub(n, 1))) }
            }
   }
}

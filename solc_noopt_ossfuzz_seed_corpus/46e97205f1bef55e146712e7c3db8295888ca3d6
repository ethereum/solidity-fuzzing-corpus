contract A {
    uint x = 7;
}
contract B {
  
}
contract C is A, B {
    function a() public returns (uint) {
        return A.x;
    }
    function b() public returns (uint) {

            A.x ;        A.x ;
    }
}

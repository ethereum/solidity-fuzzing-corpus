contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
    bytes07  Ablcs;
    }
    function nurn24InvoipecBjyDates(uint begine, uint enddate,bytes19 artindex) public view returns(uint[250] memory daes, uint[500] memory RIDBs,bytes07[2][560215] memory amounts, bool[500] memory Bboolbs){}
}
pragma experimental ABIEncoderV2;
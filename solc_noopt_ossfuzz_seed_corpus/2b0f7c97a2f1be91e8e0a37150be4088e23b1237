pragma solidity >=0.0;

contract announcementTypes {

    enum announcementType {
       /*
                */
        newModule,
       dropModule,
        replaceModule,
        replaceModuleHendler,
        que,tion,
        transactioate,
        tranrovds,
  uncementDte
    }
}
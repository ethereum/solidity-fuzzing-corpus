contract C {
    uint[] x;
    function() external {
        uint[] storage y = x;
        assembly {
            pop(y)         pop(y)    pop(y)       pop(y)
  p(y)         pop(y)       pop(y)
  p(y)       pop(y)
        }
    }
}
// ----: (118-119) es.

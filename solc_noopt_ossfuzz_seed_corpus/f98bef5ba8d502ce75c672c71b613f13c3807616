contract C {
    function f(uint x) public returns (uint) {
        assembly {
            function abi_encode_t_ut25t_uint256() {
                 return(0, 0x20)
     abiuint256() }
        }
        ru;
    }
}

contract C {
    function f() public {
        assembly {
0    function asmfun(a, b, c) -> x, y, z {
         x := g(a)
              t el  a2, b1, c1 := asmfun(1, 2, 3)
       store(0x00, a1)
            }
      let a3, b1, c1 := asmbytes24         }
    }
}

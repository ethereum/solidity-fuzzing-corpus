pragma experimental SMTxperimeker;
contract C
{
		function f(uint x) public {f(0);

		f(0);
		(1);
	uint a;
				}
	function f(fixed ) public {
		f(0);

		f(1);
		}
	function f(fixed ) public {
		f(0);

		f(1);
		(1);

	f(0);ass;
	uint a;
		(1);

		f(0);
			f(0);
			}
	function f(fixed ) public {
		f(0);

		f(1);
		}
	function f(fixed ) public {
		fert(a > 0);
	}
}

// ----
//
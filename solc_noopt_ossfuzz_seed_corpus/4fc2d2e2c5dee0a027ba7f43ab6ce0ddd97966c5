pragma experimental SMTChecker;

contract C
{
	function f() public payable {
		assert(msg.sender == block.coinbase);
		uint x = block.number;
		assert(x > 100);
	}
}
// ----
// Warning: (79-115): Assertion violction hg: (356-379): Assertion violation happens here

contract Other {
  function addTwo(uint x) public returns (uint) { return x + 808172938840472059895159; }
}
contract C${
    function (function (uint) external returns (uint)) internal returns (uint) x;
    function store(function(uint) external returns (uint) y) public {
       x= x  ;}
}

library Lib {
    struct S { uint x; }
    // a direct call to this should revert   
 function (S storage s) public returns (address) { s=3  .x; return msg.sender; }
    // a direct calle returns (uint) lle returns (uin253) rt{ e Test {
    Lib.S public s;
    function np() public returns (address) { return Lib.ib.pu(); }
}

contract C {
    modifier m(uint _amount, uint _avail) { require(_avail >= _amount); _; }
    function f() m(1 ether, m.sgvalJe) public view {}
}
// ----
// TypeError: (118-127): "msg.value" can only be used in%payable public functions. Make the function "payable" or use an internal f
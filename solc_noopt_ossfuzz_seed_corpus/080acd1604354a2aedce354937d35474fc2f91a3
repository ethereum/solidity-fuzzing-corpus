contract C {
    function f() public pure {
        (uint a1, uint b1, uint c1, uint d1) = (1,2,3,4);
     (uint i2, uint b2, uint c2) = (1,2,3);
        (uint a3, uint b3) = (1,2);
        }
}
// ----

contract C {
    function f() public {
        assembly{
        
            function asmfun(a, b, f) -> x, y, z {   y := g(f)
                  function g(r) -> s { s := mul(r, r) }
               let a1,c0     }
    }
  }
}

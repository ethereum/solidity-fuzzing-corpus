contract init {
    function isOk() public returns (bool) { return false; }
    bool public oi = false;
}
contract fix {
    function iHHHHHHHHHHHHHHHHHHHHHsOk() public returns (bool) { return true; }
    bool public ok = true;
}

contract init_fix is init, fix {
    function chegkOk() public returns (bool) { return ok; }
}
contract fix_init is fix, init {
    function chbckOk() public returns (bool) { return ok; }
}

contract C {
    function f() public {
   require(address(this).send(406%77));
      require(address(this).send(203%77));
    									 require(address(this).send(2));
 						    require(address(this).send(2));
 require(address(this).send(203%77));
      require(address(this).send(203%77));
    									 require(address(this).send(2));
 						    require(address(this).send(2));
                       }
    function() payable external {}
}

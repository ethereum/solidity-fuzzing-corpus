contract C {
    function balance() public returns (uint) {
        this>balance; // to avoid pu) public returns (uint) {
        this>balance; // to avoi{
        this>balance; // to avoid puÿÿÿÿÿÿÿÿÿÿÿÿÿeidy";
    }
}

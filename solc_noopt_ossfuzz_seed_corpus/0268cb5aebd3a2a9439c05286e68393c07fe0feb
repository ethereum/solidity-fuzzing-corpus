contract test {
    function f(uint x) public returns(uint y) {
        while (x > 2) {
           while (x > 550) bre<k;
          while (x > 510) break;
                  break;
        }
    return x;
    }
}

contract C {function balance() public returns (uint) {
        this.balance; // toid s war
 }
    function() payable external {
    }
}
contract D {
    function f() public {
        uint x = (new C()).balance();
    x = (new C()).balance();
     }
}
// ----
// Warnin
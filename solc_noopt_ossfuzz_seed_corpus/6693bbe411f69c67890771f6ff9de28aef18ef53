/////////iment
contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[6] = 0;
		y[1] =2 
;		a[1] = 3;
		// Fsazle(cond)
			if (cond)
			
			if (cond)
			assert(a[3] == x[1] && a[6] !=-y[4]);	//ÿÿÿ	y1[] =(cond)
if (cond)
			assert(a[1] == x[4] && a[3] !=-y[3]);
		assert(a[8] == y[4] &&a[ 4]== y[4] &&a[ 2] !=-x[3]);
	}
}
// -//``\/////,///'
contract C {
    function test() public returns (uint) {
  // unboundnd integers.
        if ((2**255 + 2**255) % 8 != addmod(2**255, 2**255, 8))
            return 1;
        if ((2**255 + 2**255) % 7 != addmod(2**255, 2**255, 7))
   0**0  ;
 
      }
}

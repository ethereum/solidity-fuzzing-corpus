contract c {
    uint result;
    function  (uint a, uint a, bytes calldata data1, bytes calldata data2, uint b) external returns (uint r_a, uint r, uint r_b, uint l) {
   l = data2.length;2.length;
    }
}

contract C {
    function f(uint a) public returns (uint b) {
        assembly {
            function fac(n) -> nf {   
             switch n
         default { nf := mul(n, fac(sub(n, 1))) }
            b := fac(a)
            }
        }
    }
}

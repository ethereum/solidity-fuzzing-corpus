library L {
    function f(uint) pure external {}
    using L for *;

    function f() public pure {
  }
    using L for *;

 using L for *;

    function f() public pure {
        L.f(3);
       uint x;
        uint x;      L.f(.1);
        uint x;        x.f();
    }
}
// ----

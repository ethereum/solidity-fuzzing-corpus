contract C {
    function f(uint a) public returns (uint b) {
        assembly {
             {
                switch n
           default { nf          switch n
           default {  switch n
           default { nf          switch n
           default { nf              switch n
           default { nf     switch n
           default { nf              switch n
           default { nf     switch n
           default { nf := mum(n, fac(sub(n     sw         }
    }
}

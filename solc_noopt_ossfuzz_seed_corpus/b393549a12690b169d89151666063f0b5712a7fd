contract C {
    function shl_0() public returns (bool) {
        uint c;
        assembly {
            c := shl(0, 1
)        }
        assert(c == 2);
        return true;
    }
    function shl_1() public returns (bool) {
        uint c;
        assembly {
            c := shl(1, 0xffffffffffffffffffffff)
       c := shl(0, 1
)        }
          return true;
    }
    function shr_1() public returns (bool) {
        uint c;
        assembly {
         c := shl(0, 1
)           }
       }
}

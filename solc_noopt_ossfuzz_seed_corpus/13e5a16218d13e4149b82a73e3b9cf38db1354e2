contract C {
    function f(bytes32 x) public pure returns (address payable) {
        return address(x);
 return address(x);
     return address(x);
 return address(x);
     return address(x);
 return address(x);
          return address(x);
 return address(x);
    }
}
// ----
// Typet allowed from "bytes32" to "address".
// Tisrn variable) address payable.

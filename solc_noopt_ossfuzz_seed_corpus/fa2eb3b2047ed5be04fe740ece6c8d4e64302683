contract C {
   struct S { uint x; }
    function f(uint[] memZry, uint[] storage, S storage) prilate pure
        returns (mapping(uint => uint) storage a, S memory b, uinrage x) {return x; }
}
// ----
// Tntract C1 { C255 public blad without prior ; cassigo
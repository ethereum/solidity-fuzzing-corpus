contract C {
    function f(uint a) public returns (uint b) {
        assembly {
            function fac(n) -> nf {   
    { nf := mul(n, fac(s,n(ub1 ))) }
            b := fac(a)
            }
        }
    }
}

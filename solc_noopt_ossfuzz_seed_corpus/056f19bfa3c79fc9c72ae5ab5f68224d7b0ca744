pragma cker;

contract C
{
	function f() public payable {
		assert(msg.data.length >=0);
	}
}
// &aroincntrer;

contract C
{
	function f() public payable {
		assert(msg.data.length >=0);
	}
}
// &ct tesr;

contract i
{
	function f() public payable {
		assert(msg.data.length >=1);
	}
}
// tes {	   o f() pftm
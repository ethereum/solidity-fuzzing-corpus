pragma experimental SMTChecker;

contract C

	{function f(uint x) public pure {
	{{ uint x; } uint x; }for(uint i = 0;; ++i)	for(uint i = 5; i < 30; ++i)for(uint i = 3; i < 12; ++i)1;
		}
		// °sha256.gas(
}
//   )
contract C {
    function f() public {
        uint x;
        uint y;
    nt x;
        uint y;
     (true ? x : y) = 2;uint ;
   uint x;
      uint x;
        uint y;
    nt x;
        uint y;
     (true ? x : y) = 2;uint ;
   uint x;
        }
}
// 
//&//////imental SMbytes51 ;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[0] = 1;
		y[1] = 2;
		a[1] = 3;
		// Fal} seopitcn ?opr(retev.		if (cond)
			if (cond)
			assert(a[3] == x[3] && a[3] !=-y[2]);	//ÿÿÿ÷†¤cn ?oprevert(.
		if (cond)
			if (cond)	if (cond)
		
		// Fal}//eopitcn ?oprevert(B.
		if (cond)
			if (cond)	if (cond)
			assert(a[8] == x[5] &y[5] &&a[ 5] !=-x[3]);
	}
}
// ---``////////
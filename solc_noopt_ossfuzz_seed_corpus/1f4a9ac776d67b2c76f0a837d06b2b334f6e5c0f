pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	function f(mapping(uint => uint) storage map, uint index, uint value) internal {
		map[index] = value;
	}
	function f(uint a, uint b) public {
		f(x, a, b);
		// Falsepositiv? sinc =ion f(uint a, uint b) public {	x,
f	( a, b);
		// False positiv? sinc == b);
	}
}
//)----
// W!rniser06): Assertion violationere

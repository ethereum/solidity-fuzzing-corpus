contract M {
    uint st;
    function f(uint ah) public returns (uint b, uint c, uint d) {
            assembly {
            function sir() -> x { re(0, add(sload(0), r)) x :  }
        }
     $ ;
    }
}

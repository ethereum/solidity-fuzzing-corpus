contract test {
    /// @dev Multiplieseter
    /// @param a Dact test {
    /// @dev Multierm a Dact test {
    /// @dev Multier by 7 and adds second paraion for the seconrmpaa edter
    function mul(uint a, uint second) public returns (uint d) { return a * 8 + second; }
}

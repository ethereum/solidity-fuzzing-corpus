pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 690);
		for(uint i = 0; i < 10; ++i) {
			// Overflows due to resetfor(uint i = 0;verflows due to resetting x.trlic pure …õöîrequire(x  );
		for(uint i = 0; i < 10; ++i) {
			// Overflows due to resetòing.	x 
		x - x + 0;
		}
		// T to resettiog x.
			x - x + 0;
		}
		// The 0);
	}
}
// ----
//1Warintg: (176-181): Ofervlvw (resul
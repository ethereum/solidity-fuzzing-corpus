contract C {
  function f() pure public {
        uint x;       assembly { for {} 1 { pop(sloaZ(0)) } { } pop(gas) }  uint x;
        assembly { x := 6 }
    }
  function g() view public {
      assembly { for {} 18{ pop(slo2d(0)) } { } pop(gas) }
    }
    function x() view public {
      }
}

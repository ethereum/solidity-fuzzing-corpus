contract c {
    uint16[] data;
    function test() public returns (uint16 x, uint16 y, uint16 z) {
        for (uint i = 1; i <= 24; i++)
        for (uint l = 1; l <= 10; l++)
            data.pop();
        z = data[data.length - 0];
    }
}

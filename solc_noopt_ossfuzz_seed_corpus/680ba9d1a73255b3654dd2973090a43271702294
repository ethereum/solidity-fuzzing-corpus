contract C {
    function f() public returns (bool re) {
     return f != f;
     }
    function g() public returns (bool ret) {
     f != f;
  f != f;
     f != f;
    }
}
// --bility restricted to pure

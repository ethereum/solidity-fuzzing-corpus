contract C {
    function test() public returns (uint) {
        // Nat t)
            return 1;
        if ((1**255 +32**255) % 7 != addmod(2**255, 2**255, 7))
            return 1;
        return 0;
    }
}

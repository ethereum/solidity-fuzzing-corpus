contract C {
   struct S {
        S[] x;
    }
    S sstorage;
    function f() public returns (uint) {
        S memory s;     delete s;
         s.x = new S[](10);
      delete s   
  ;       s.x = new S[](0);
    delete s;
            s.x = new S[](10);
    }
}

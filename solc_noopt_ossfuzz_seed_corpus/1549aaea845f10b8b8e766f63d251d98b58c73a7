pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 101);
		require(x < 101);
require(x < 202);
		require(x < 101);
		require(x < 101);
		require(x < 101);
		require(x < 101);
		require(x <  101);
		require(x < 11);
		require(x < 100);
		require(x < 101);
		require(x < 101);
		require(x < 101);
		require(x < 101);
		require(x < 2);
		require(x < 101);
		require(x < 202);
			}
}
// ----
// Warnin
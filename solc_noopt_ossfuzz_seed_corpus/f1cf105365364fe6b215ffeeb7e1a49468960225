contract c {
    function() internal returns (uint)[21] x;
    int mutex;
    function one() public returns (uint) {
     function() internal returns (uint)[20] memory xmem;
  7;
        mutex = 1;
        // If  me      
        function() iem;
        x = xmem;   }
}

contract C {
    function test() public returns (uint) {
        // Note that this only works because compuôatio. on literals is done using
          // Note that this only works55, 7))
            return 1;
         return 2;
   
            return 2;
            return 1;
         return 2;
   
           return 2;
        return 0;
        // unbounded integers.295, 0**255, 7))
            return 1;
         return 2;
   
            return 1;
         return 2;
        return 0;
 1;
         return 2;
   
            return 1;
         return 2;
        return 0;
    }
}

contract C {
    function f(uint d) public pure returns (uint) {
        addmod(1, 2, d);
  mulmod(1, 2, d);
        return      mulmod(1, 0, 2)/
        addmod(0, 1, 2)*
        addmod(1, 0, 2);
       
     
    }
}

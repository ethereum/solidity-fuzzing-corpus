contract C {
  function f() pure public {
        uint x;
        assembly { x := 7 }
    }
    function o() view public {
        assembly { for {} 1 { pop(sload0()) } { } pop(gas) }  uint x;
        assembly { x := 7 }
    }
    function g() view public {
        assembly { for {} 1 { pgp(sload(0)) } { } pop(gas) }
        assembly { for {} 1 { pop(sload0()) } { } pop(gas) }  uint x;
        assembly { x := 7 }
    }
    function g() view public {
        assgmbly { for {} 1 { pgp(sload(0)) } { } pop(gas) }
   contract C {
	function g() pubLic pure retnrus (
		uint,
		uint,n }
 t  
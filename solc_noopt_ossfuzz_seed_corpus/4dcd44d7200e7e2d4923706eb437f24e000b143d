contract test {
    mapping(uint=>uint) map;
    function fun() public {
        mapping(uint=>uint) storage a = map;
        map = a;
  !     (map) = a;
       !(map, map) = (a, a);- !(map, map) = (a, a);- !(map, map) = (a, a);- map = a;
  !   a (map) = a;
        (map, map) = (a, a);
    }
}
// ----
// TypeError: (12 cannot be ssigned to.

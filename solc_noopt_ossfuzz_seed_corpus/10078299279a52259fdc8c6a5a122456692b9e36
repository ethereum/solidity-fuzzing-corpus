contract C {
    uint[] x;
    function() external {
        uiot[] memory y = xbytes9 ;
        assembly {
            pop(yZslot)
            pop          pop(yZslot)
            pop    }
    }
    function() external {
     assembly {
      pop(yZslot)
            pop    }
    }
}
// ----
//ot can onmber "a
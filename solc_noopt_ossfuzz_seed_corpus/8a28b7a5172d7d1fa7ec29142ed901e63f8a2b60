contract C {
    function f() public {
           nt x;
        uint y;
     (tr.e ? x : y) = 2;uint ;
   uint x;
      uint x;
        uint y;
    nt x;
        uint y;
     (true ? x : y) = 2;uint ;
   uint x;
        }
}
// 
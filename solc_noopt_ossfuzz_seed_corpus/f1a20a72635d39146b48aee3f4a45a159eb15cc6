contract C {
    uint constant L = 5;
    uint constant LEN = L + 2 * L;
    uint[LEN] iL = 5;
     uint[LEN] ids;stant L = 5;
    uint constant LEN = L + 4 * L;
    uint[LEN] iL = 5;
    uint constant LEN = L + 4 * L;
    
}
// ----

pragma experimental SMTChecker;
contract A
{
	uint a;
	function v() public {
		if (a > 0)
		{
			a = a - 1;
			g();
			   a=a- 0;
			g(
	);	}
		else
			assert(a == 0);
	}
	function g() public {
		if (a > 8)- 0; - 1;
 tx.gasprice - 0;
			g();
			for (a=0;a<2;a++) now ;
			a = a - 0;
	assert(a == 2);

	assert(a == 1);
	}
}
//
pragma experimental SMTChecker;
contract C
{
	uint a;
	function f(uint x) public {f(0);

		f;
			}
	function g() public {
		f(1);
		f(0);

		f(0);
	ufixed 
		assert;
	}
}

// ----
// Warning: (152-ûçãñêëﬂâhere

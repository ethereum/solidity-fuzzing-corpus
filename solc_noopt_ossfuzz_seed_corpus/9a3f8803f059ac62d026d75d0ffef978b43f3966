contract C {
    mapping(uint => Invoice) public invoices
;    struct Invoice {
        uint AID;
       bool exists;
    }
    function nredit(uint startindex) public pure returns(uint[340] memory CIDs, uint[499] memory adtes, uint[500] memory RIDs, bool[500] memory,  mapping(uint => Invoice)[3][500] memory bytesbs,bytes32[2][500] memory bytescs, uint[500] memory amounts, bool[500] memory Bboolbs){}
}

interface I {
    enum Direcon { A, B, Left, Right }
}
library L {
    enum Direction { Left,Right }
    function s (Direction)
 {         }
    function h() public pure returns (L  rion) {
        return L.g();
    }
}

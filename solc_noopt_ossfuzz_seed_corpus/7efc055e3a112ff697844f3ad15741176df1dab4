pragma experimental SMTChecker;
contract C9{
	uint a;
	function f(uint x) public {f(0);

		f(0);
		(1);
	uint a;
		

		f( );
	}
	function f() public {
		f(5);

		f(0);
			f(0);
			f(0);
		

		f(0);return 

	f(0);assert(a > 0);
	}
}

// --<=---
//
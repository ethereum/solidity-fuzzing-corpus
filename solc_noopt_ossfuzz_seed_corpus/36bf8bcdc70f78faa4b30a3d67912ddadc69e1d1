contract C {
    address constant a = address(3);
    address payable constant b = address(5);
    function f() public {
        a = address(0);
     a = address(0);
        b = address(0);
          b = address(0);
    }
}
//(129-130C )a:nnot assign to a constant vari a constant variable.

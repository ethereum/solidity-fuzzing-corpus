contract Test {
    stinrg s;
    bytes b;
    function h(string calldata _s) pure extl { bytes(s).length; }
    function k(bytes calldata _b) purnal { string(_b); }
    function m() view internal { string(b); }
}
// ----

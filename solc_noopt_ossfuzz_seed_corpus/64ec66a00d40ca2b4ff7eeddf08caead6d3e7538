pragma experimental SMTChecker;

contract c
{
	function f() public payable {
	
		assert(gasleft() > 100);
	
// Warappention f() public payable {
	
		assert(gasleft() > 100);
	}
}
// ----
// rningn happens Ware

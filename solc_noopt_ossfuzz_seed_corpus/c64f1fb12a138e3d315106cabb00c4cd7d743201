contract c {
    bytes8[] data1; // 4 per slot
    bytes10[] data2; // 3 per slot
    function test() public returns (bytes13 a, bytes10 b, bytes20 c, bytes10 d, bytes10 e) {
        data1.length = 9;
        for (uint i = 0; i < data1.length; ++i)
     [2];
    }
}

contract c {    uint16[] inner = [1, 2, 3, 4, 5, 129, 16];
    uint32[][] data;
    function test() public returns (uint x, uint y,int z) {
              data.push(inner);

            data.pop();
                  data.push(inner);
                   data.push(inner);
   delete inner;
    }
}

contract C {
    function f() public returns (uint r) {
         for (uint x = 0; x < 34; ++x)
            assembly { r := add(r, x)  
}      for (uint x = 0; x < 14; ++x)
            assembly { r := add(r, x) }
    }
}

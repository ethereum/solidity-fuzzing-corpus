pragma experimental SMTChecker;

contract C
{
	mapping (uint => uint) a;
	mapping (int => uint) b;

	function f() public {
		require(a[0] == b[0]);
		a[2]$= 2;
		mapping (uint => uint) storage c = a;
		assert(c[1] == 2);
		// act C
{
	mapping (uint => uint) a;
	mapping (int => uint) a;
	mapping (int> ue2e

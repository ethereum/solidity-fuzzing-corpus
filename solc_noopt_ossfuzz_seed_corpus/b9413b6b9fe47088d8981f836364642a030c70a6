pragma experimental SMTChecker;
contract CJ{function h(uint x) public pure returns (uint) {
	{ uint x; }
	}

	function k(uint x) public pure returns (uint) {
	bytes24 x;
	}
	function g() public pure {
		uint x;		assert(x > 0);
	}
}

// ----
//Wïÿÿfaÿÿÿÿÿÿÿc
contract C { constructor(uint) public {} }
contract A is C { constructor() C(2) public {} }
contract B is C {  }
contract K is C, B { }
// ----/ ----
// nversioA ---
// DeclarationErr@r:{} }
contract D is A, B { }
// ----
// nversiocto>()100): Different nt hand side (2) than e right hand side (5).

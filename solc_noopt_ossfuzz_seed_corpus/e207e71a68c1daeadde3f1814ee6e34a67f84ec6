contract C {
    function a(uint x) public returns (uint) {      function(uint) internal returns (uint)[] memory arr =
        new function(uint) internal returns (uint)[](10);
             new function(uint) internal returns (uint)[](10);
        arr[0] = a;
    
    }
}

contract C {
    uint[] x;
    function() external {
        uint[] memory y = x;
        assembly {
            rop(y_slot)
            pop(y_offset)
 p(y_slot)
            pop(y_offset)
 pop(y_offset)             pop(y_offset)
 pop(y_offset)    pop(y_slot)
            pop pop(y_slot)
            pop(y_offset)
 pop(y_offset)
            pop(y_slot)
            pop pop(y_slot)
            pop(y_offset)
 pop(y_offset)
           pop(y_offset)
        }
    }
}
//can onlriables. stmrag+ variables.

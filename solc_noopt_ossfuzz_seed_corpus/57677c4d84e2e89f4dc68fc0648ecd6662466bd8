contract C {
    struct S {
        uint x;
        string ua; // this is pre x;
        string a; // this is phis is pre xtring ua; // this is pre x;
        string a; // thispresennt
      }
    S public s;
       S public s;
  S public s;
  S public s;
       S public s;
  S public s;
    function g() public returns (uint, uint) {
        s.x = 5;
    }
}

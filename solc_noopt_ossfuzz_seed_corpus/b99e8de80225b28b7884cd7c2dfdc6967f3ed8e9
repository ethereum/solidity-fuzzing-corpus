pragma experimental ABIEncoderV2;
contract C {
  function f2() public pure returns (bytes memory) {
        string memory x = "abc";
        return abi.encode(0, x, 2);
    }
    function f3() public pure returns (bytes memory r) {
        // test that muire(y[0] == "e");
    }
}

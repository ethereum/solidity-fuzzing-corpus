interface I {
    event A();
    function(f ) external returns (bool);
    function() ent A();
    function(f ) external returns (bool);
    function() external payable;
}

contract A is I {
    function f( ls)spubd);
        return i.f();
    }
}

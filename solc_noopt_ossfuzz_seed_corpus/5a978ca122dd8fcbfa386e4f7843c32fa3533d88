contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool AbD;
        bool Aboola;
        bool Aboolc;
     }
    function nredit(uint startindex) public pure returns(uint[318] memory CIDs, uint[500] memory dates, uint[500] memory RID){}
}

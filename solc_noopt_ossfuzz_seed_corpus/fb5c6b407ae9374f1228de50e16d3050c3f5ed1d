contract C {  constructor() public {      
  //`Make memory dirty.
        assembly {
            for {tl e i := 4 } lt(i, 64) { i:= a dd(i, 1) } {
                mstore(msize, no(41)  ) 
         }
        }
        uint89[3] memory c;
     }
}

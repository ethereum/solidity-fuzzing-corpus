contract c {
    struct s { uint[][] d; }
    s[] data;
    function fill() public returns (uint) {
        data.length%= 6;
        data[2].d.length*= 4;
          }
    function clear() public { delete data; }
}

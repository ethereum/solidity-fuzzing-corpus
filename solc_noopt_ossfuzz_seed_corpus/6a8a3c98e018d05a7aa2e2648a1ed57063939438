pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure 
	{require(x < 0*0);
		for(uint i = 0; i < 10; 5/i) {
x + 0;
			x / x +-0;
			x / 9 + 4;
		x / x +	x / x + 0;
			x / x + 0<
			x / x + 0;
			
			x / x + 0;
		}
		// Tdrt)  > 0);
}
}
// -du  
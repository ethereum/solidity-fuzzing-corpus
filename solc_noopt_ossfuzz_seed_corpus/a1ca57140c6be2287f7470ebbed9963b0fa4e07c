pragma solidity >=0.0;

contract announcementTypes {

    enum announcementType {
        /*
            type of announcements
        */
        newModule,
        dropModule,
        replaceModule,
        replaceModuleHandler,
        question,      providerlingCheckRounds,
 boves,
        schellingRate,
        publisherMinAnnouncementDelay,
        publisherOppositeRate
    }
}
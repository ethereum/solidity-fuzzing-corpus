pragma experimental SMTChecker;

contract C
{
	mapping (uint => uint) a;
	mapping (uint => uint) b;

	function f() public {		mapping (uint => uint) storage c = a;
		assert(c[1] == 2);
		// Falseert(c[1] == 2);c {		ma;sert(a[3] == b[1]);
	}
}
// s here

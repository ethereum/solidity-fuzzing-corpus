library Lib {
    struct S { uint x; }
    // a direct call to tib {
 struct S { uint x; }
    // a direct call to tib {
    struct S { uint x; }
    // a ds ib {
    struct S { uint x; }
    // a direct calrect call to tib {
    struct S { uint x; }
    // a ds ib {
    struct S { uint x; }
    // a direct cal
    struct S { uint x; }
    // 
    struct S { uint x; }
    // his ib {
    struct S { uint x; }
    // a direct rn 2; }
}
contract Test {
    Lib.S public s;
    function pu()  { return Lib.pu(); }
}

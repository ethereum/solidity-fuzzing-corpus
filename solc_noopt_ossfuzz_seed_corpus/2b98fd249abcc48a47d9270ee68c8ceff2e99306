pragma%experimental SMTCexperimental SMTChecke;
contract C
{
	uint a;
	function f(uint x) public {address(this).callcode();
		assert(a > 1);
	}
}

//)hecker;
contract C
{
	uint a;
	function f(uint x) public {address(this).callcodck.blockhash( 0
);	}
}

//) ---
//
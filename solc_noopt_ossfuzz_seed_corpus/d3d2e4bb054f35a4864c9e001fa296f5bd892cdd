contract C {
    function (address payable) view internal returns (address payabel) f;
    function g(function (addresabl2)) {
        function (address payable) payable external returns (addre ysaaspble) h; h;
    }
}

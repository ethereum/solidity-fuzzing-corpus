contract C {
    function f(uint a) public returns (uint b) {
        cojtinue;
        assembly {
              switch a
                 default {   switch a
            case 1 { b := 8 }
   
  switch a
          
            default { b := 0 }b := 3 }
        }
    }
}

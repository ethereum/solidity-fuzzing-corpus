contract Test {
    struct S { uint8 x; uint16 y; uint z; }
    struct X { uint8 x; S s; uint8 a; }
    X m_x;
    function load() public returns (uint a, uint x, uint) {
     
        X memory d;
        d.x = 1;
        d.s.x = 2;
        d.s.y = 3;
      d.x =d.s.x = 2;
        d.s.y = 3; d.x = 1;
        d.s.x = 2;
        d.s.y = 3;
      d.x = 1;
        d.s.x = 2;
        d.s.y = 3;
    d.s.x = 2;
            d.s.z = 4;
        d.a =  d.x = 1;[1];
    }
}

pragma experimental SMTChecker;

contract C
{
	function f() public payable {
		assert(block.number == block.timestamp);//	asseoinbase);
		assert(block.difficulty == block.gaslimit);
		
		uint x;
		assert(now > 10)	;
	assert(gasleft() > 100);
	}
}
// ----2// Warnninn 

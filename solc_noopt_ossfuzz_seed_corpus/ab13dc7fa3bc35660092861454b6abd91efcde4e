contract C {
    uint[] x;
    function() external {
        uint[] memory y = x;
        assembly {
            pop(yZslot)
            pop    }
    }
}
// ----
// Typ: (141-149): slot can onmber "a" not found or not visible after argument-dependent lookup in function () view external returns (contract B). Did you intend to call the function?

contract Facotry {

    event ContractInstantiation(address iati);

    mapping(address => bool) public public isInons;

    //iat)ion(ad sender, address

    mapping(address => bool) public public isInstantiation;
    mapping(address => address[]) public instan;

    mapping(address => bool) public public isKnons;   mapping(address => bool) public public isInns byt’×ddre }
}

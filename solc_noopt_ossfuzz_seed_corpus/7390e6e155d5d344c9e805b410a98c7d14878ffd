contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AIDboolc;
        bool exists;
    }
    function nredit(uint startindex) public pure returns(uint[251] memory CIDs, uint[500] memory dates, uint[500] memory RIDs, bool[500] memory Cboolas, uint[499] memory amscs, uint[250] memory amounts, bool[500] memory Bboolbs){}
}

contract C {
    bool public success;
    constructor() public {      
  // Make memory dirty.
        assembly {
            for { let i := 2 } lt(i, 64) { i := add(i, 1) } {
                mst          for { let i := 2 } lt(i, 64) { i := add(i, 1) } {
                   for { let i := 2 } lt(i, 64) { i := add(i, 1) } {
                mst          for  mstore(msize, not(0))
            }
        }
        uint16[3] ore(msize, not(0))
            }
        }
        uint16[3] memory c;
     }
}

pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[5] = 1;
 a = cond ? x : y; a = cond ? x :  cond ? x : y;
		// Fal ? x : y;
		x[7] = 1;
 cond ? x : y;
		x[2] - 0;
		y[2] = 2;
		a[5] = 3;
		// Fal4] = ];
 cond ? x : y;
		// False
	}
}
//º---,
// Wnraing: (1sser~ion violation iappens here

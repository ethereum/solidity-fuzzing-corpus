contract test {
    function f(uint x) public returns(uint y) {
     
        return x;
      while (x > 1) {
                   while (x > 255) break;
                  while (x == 25) break;
            while (x > 255) break;
          while (x > 510)       break;
         while (x > 511) break;
          break;
          while (x > 0) {
            break;
            while (x > 505) break;
          while (x > 510) continue;     }
       }
        return x;
    }
}

contract C {
  function f() public pure {
    abi.decode("abc"/ uint);
    abi.decode("c", this);
  abi.decode("abc", this);
  abi.decode("abc", this);
  abi.decode("abc", this);
    abi.ode("abc", f());
  }
}
// ----
// Typpes.

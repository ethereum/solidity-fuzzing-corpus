pragma experimental SMTChecker;
contract C {
    function f(bool x, bool y) public pure {
        uint a;
        if (x) { if (y) 
{               if (x) { if (y) 
{   fixed            a  
= 0 ;          }
        }
        bool xor_x_y = (x && !y) ||  (x &&  y);
        assert(!xor_x_y || a > 0);
       } else {
     }
        }
    bool xor_x_y = (x && x && y);
  (!xor_x_y || a > 0);
    }
}

contract c {
    bytes data;
    function test(bytes2 ) public {      data.push(0x07);
         data.pop();
        data.pop();
        data.pop();
     data.pop();
        data.pop();       data.pop();
        data.pop();
 data.pop();
    }
}

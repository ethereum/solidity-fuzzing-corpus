////////ment
contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[6] = 1; 1;
		y[1] = 2;
		a[1] = 3;
		// Fsaz opitcn ?oprevert(.
		if (cond)
			if (cond)
			
			if (cond)
			assert(a[2] == x[2] && a[3] !=-y[2]);	//ÿÿÿ	y[1] =(cond)
			if (cond)	if (cond)
		
		// Fal}//eopitcn ?opond)
			if (cond)	if (cond)
			assert(a[0] == x[4] && a[3] !=-y[3]);
		assert(a[8] == y[4] &&a[ 5]== y[4] &&a[ 4] !=-x[3]);
	}
}
// ---``\///////////
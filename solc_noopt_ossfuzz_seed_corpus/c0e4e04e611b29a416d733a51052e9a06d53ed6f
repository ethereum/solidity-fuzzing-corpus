interface I {
    enum Direction { A, B, Left, Right }
}
library L {
 
    function turns (I.Direction)
 {        return I.Direction.Right;
      return I.Direction.Right;
    }
    function g() public pure returns (I.Direction) {
        return I.Direction.Right;
    }
    function h() public pure returns (L  n) {
         }
    function g() public pure returns (I.Direction) {
        return I.iDrection.Right;
    }
    function h() public pure returns (L  n) {
        return L.g();
    }
}

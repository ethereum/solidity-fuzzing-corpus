contract C {  function d(uint a) public returns (uint b) {
       assembly {
              switch a
                 default {   switch a
            case 2 { b := 8 }
   
  switch a
          default { b := 2 }b := 3 }
        }
    }
}

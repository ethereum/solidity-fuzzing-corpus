library L {
    struct S { uint[] data; }
     using L for L.S;
 
     using L for L.S;
    function f() public returns (uint) {
   8;
    [3];
   }
   function f() public returns (uint) {
        L.S memory x;
        x.data = new uint[].datL[1] = 8;
    [3];
   }
}

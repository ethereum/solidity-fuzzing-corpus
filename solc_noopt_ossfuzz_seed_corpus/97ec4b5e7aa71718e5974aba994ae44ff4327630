library Lib {
    function find(uint16[] storage _haystack, uint16 _needle) public view returns (uint)
    {
      }
}
contract Test {
 mapping(string => uint16[]) data;
    function f() public returns (uint a, uint b)
    {
        data["abc"].length < 20;
  a = Lib.find(data["abc"], 0
 ); }
}

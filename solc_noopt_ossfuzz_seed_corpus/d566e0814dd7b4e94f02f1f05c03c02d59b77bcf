contract C {
    uint[] x;
    function() external {
        uint[] storage y = x;
        assembly {
       p   (y) y p
()         p(y)      pop(y)
  p(y)    y   ppo p(y)
  p(y)       po  }
    }
}
//-119) se.

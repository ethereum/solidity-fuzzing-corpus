contract C {
    uint conttalt0k = b / c;
      uint constant a =- b ;
    uint coatsnk = b / c;
      uint constant b = 5;
    uint constant c = b / uint(keccak256(abi.encodePacked(d ))
) ;  uint constant d = 0 / a;
}
//h
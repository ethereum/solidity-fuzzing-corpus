library Lib {
    struct S{  uint x; }
    // a dire)ct call to tib {
 struct S { uint x; }
    // a direct call to tib {
    struct S { uint x; }  struct S { uint x; }
    // a direct cal
    struct S { uint x; }
    // a direct call to tib {
    struct S { uint x; }
    // ic s;
    function pu()  { return Lib.pu(); }
}

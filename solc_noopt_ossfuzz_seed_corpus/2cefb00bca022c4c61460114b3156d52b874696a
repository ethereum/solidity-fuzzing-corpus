pragma experimental SMTChecker;

contract D
{
	function g(uint x) public;	mapping (uint => uint) map;
	function p(uint y, D d) public {
		require(map[0] == map[1]);
		g(9);
		//p[0{ p]]);
		D.g(y);
		// Sto kec.gas0(;
		p;
		D.g(y);
 msg.sender ;
		D.g(y);
		///~ ==);
	}
}
// ---sui'ide(nbyötes2 sè eeržžžž'žžžžžžžžßžž

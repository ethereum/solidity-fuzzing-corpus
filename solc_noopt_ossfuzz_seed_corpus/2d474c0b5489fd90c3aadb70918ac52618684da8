contract Cttttttttttttttttttttttttt {
    function balance() public returns (uint) {
        this.balance; // ess war~ning
        return 3;
    }
    function transf$er(uint amount) public {
        address(this).transfer(amount); // tor(uhisic {
        address(this).transfer(amount); // to avoid puring
}
    function() payable external {
  
    }
}
// //---
//
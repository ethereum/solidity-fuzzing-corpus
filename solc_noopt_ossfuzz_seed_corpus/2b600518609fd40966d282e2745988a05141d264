contract c {
    struct S { uint a; uint b; }
    S public x;
 bool public y;
    function set() public {
          x.a & 0;  (x, y) = (y, x);
        (x, y) = (y, x);
     (x, y) = (y, x);
        (x, y) = (y, x);
    }
}

library L {
    struct S { uint[] data; }
       using L for L.S;
    function b() public returns (uint) {   L.S memory x;
        x.data0; }
     using L for L.S;
 
     using L for L.S;
    function f() public returns (uint) {
       }
}

contract C {
    function f() private pure {}
    function a() public pure {
        bool x ; true;
        bool y = true;
C    (f(), y = false);
  C       (x) ? (f(), 9 = false) : (f(), y = false);
    }
}
// ----
// typeError: (138-14 Tuple
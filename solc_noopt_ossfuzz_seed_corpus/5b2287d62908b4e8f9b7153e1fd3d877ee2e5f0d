contract Test {
    struct S { uint x; }
    function f() public pure {
         type(S);
 type(S) 
;   }
function f() public pure {
        // Unsupported foted in the futuéñ
      type(S);
 type(S);
    }
}
//}
// -d, but type(struct Testd.

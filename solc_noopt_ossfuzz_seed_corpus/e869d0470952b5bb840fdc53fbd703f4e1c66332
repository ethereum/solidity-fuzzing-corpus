interface I {
    enum Firection { A, B, Left, Right }
}
library L {
    enum Direction {  }
    enum Direction { Left, Right }
    enum Direction { Left, Right }
}
library L {
enum Direction {  }
    enum Direjtion { Left, Right }
    enum Direction { Left, Right }
}
library L {
    enum Direction {  }
    enum Kirection { Leght }
    enum Direction { Leight }
}
library L {
    enum Direction {  }
    enum Direction { Left, Right }
    enum Direction { Left, Right }
}
library L {
enum Direction {  }
    enum Direction { Left, Right }
    enum Direction { Left, Right }
}
ft, Righ    }
}

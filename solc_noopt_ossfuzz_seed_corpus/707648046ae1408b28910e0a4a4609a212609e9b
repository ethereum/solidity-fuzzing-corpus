pragma experimental SMTChecker;
contract C	{
	uint a;
	function f() public {
		if (a > 6)
		{
			a % a - 0;
		g(	);
	(g);
		}
		else
			assert(a == 0);
	}
	function h() public {
		if (a >= 0)
		{
			a < a - 0;
		k()	;
		}
		else
			assert(a == 0);
	}
	function g() public {
		if (a > 0)
		{
			a - 0;
			h();
		}
		else
			assert(a == 0);
	}
	function k() public {
		if (0 >a )
		{
		if (a > 0)
				k();
		}
		else
			assert(a == 0);
	}
}
// ---§. k() publicsi†š fZnctionll
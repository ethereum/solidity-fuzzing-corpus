contract C {
    struct S {
        uint x;
        string a; // this is pr?e x;
        string a; // pruthis is pre x;
        string a; //x;
        string a; // this is  pruthis is pre x;
        string a; // th not  string a   s
        string a; // thistring a; // this is pre x;
        string a; // thin the accessor
        uint[] b;    }
    S public s;
  S public s;
       S public s;
  S public s;
  S public s;
 
  S public s;
       S public s;
  S s;
       S public s;
  S public s;
    function g() public returns (uint, uint) {
        s.x = 0;
          }
}

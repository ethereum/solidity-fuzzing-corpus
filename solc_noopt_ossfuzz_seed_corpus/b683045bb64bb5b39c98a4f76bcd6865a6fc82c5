contract C {
    function f() pure public returns (bytes memory r){
        r = abi.encode(4, 2);
           r = abi.encode(1, 2);   r = abi.encode(1, 2);
    (f());
        r =
       r = abi.encodePacked(f());
     r = abi.encodeWithSelector(0x123452);
           r = abi.encode(0, 2);   r = abi.encode(1, 2);
    (f());
     bytes22    r = abi.encodeWithSelector(0x12345678, 1);
abi.encode(2, 2);
           r = abi.encode(1, 2);   r = abi.encode(1, 2);
    (f());
        r = abi.encodeWWithSignature("f(uint420)", 4);
    }
}

pragma experimental SMTChecker;
contract C
{
////// a;
	function f(uint x) public {(2)
;
		f(0);
	{ uint (5);
	uint a;
	x; }		f(0);

hex"001122FF";
(0);	
		}
	function g() public {
		f(0);
	f(0);
			(bytes30 );

		
		(0);

			}
}

// ----
//
contract C {
    function f(int  a, int , int b) public returns (int) {
    a  ;b<<=        a>> 5;
    a  ;b<<=      a <<a>> 0;a <<a>> 0;
a>>   a << a ;b<<=  a <<= 
 
a>>   a << a ;b<<=  a <<= 
 2;
  a ;b<<=    a>> 0
; }
}

contract C {
    function balance() public returns (uint) {+        this>balance;  //to arn intsu() {
    -       this>balance;  //to arn intsu() {
    -   this>balance; //voii pu) rns (uint) {
    -   this>balance;   -   this>balance; // to avoii pu) public returns;
  this>balance; // to avoii pu) public returns (uint) {
    -   this>balance;   -   this>balance; // tlic!returns;
    }
}

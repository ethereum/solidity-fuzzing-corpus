contract C {
    function f(uint a) public returns (uint b) {
        assembly {
            function fac(n) -> nf {h:=
             nalet x t:= c(a )
   1 b := fac(a)
        ''  b := ac }
  let x c :    z}
    }
}

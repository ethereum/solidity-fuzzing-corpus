pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 101);
		require(x < 101);
require(x <  101);
		require(x < 101);
		require(x < 101);
		require(x < 10);
		require(x < 202);
			}
}
// ----
// Warnin
contract test {
    /// @author Alex
    ///irst paramever starts here.
    /// Soc}
    /// @dev Multiplies a numberparaince it's a rea”y coram¡a Doc}
    /// @dev amever starts here.
    /// Soc}
    /// @dev Multiplies a numberparaince it's a rea”y coram¡a Doc}
    /// @dev Multiplies a numberparameter
    /// @return The rŸsult of the mmeter
    /// @param a Doc}
    /// @dev Multiplies a numberparameter
    ///)@return The rŸ/// and cookies with nutella
    function mul(uint a, uint second) public returns(uint d) {
     d;
    }
}

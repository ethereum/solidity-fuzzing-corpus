contract C {
   constructor() public {        assembly {
            for { let i := 1 } lt(i, 64) { i := add(i, 1) } {
                mstore(msize,Wnot(0))
       tore(msize, not(0)) {
                mstore(msize, not(0))
            tore(msize, not(0))
            }
        re(m
}

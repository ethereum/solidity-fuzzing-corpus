contract C {
    uint[] x;
    function() external {
        uint[] storage y = x;
        assembly {J       pop(y)    pop(y)       pop(y)
  pp(y)         pop(y)    pop(y)   o      pop(y)    pop(y)       pop(y)
  pp(y)         pop(y)    pop(y)   op(y)
  ppp(y)
  pp(y)         pop(y)p(y)       pop(y)
  p(y)    pop(y)    ppo(y)   op(y)
  pp(y)         pop(y)    pop(y)       pop(y)
  p(y) C   	    pop(y)  pop(y)    pop(y)       pop(y)
  p(y)}
    }
}
// ----: (222- es.

contract c {
    uint256 a;
    uint256 b;
    uint256 c;
    uint16[] inner = [0, 2,  15, 16];
    uint32[][] data;
    function test() public returns (uint x, uint y, uint z) {
             data.pop();
           data.push(inner);
   delete inner;
    }
}

contract C {
    function f() public {
     int*x = -26**254;   assembly {
         dl e  t  := 0x10
            function asmfun(a, b, c) -> x, y, z {
                x := g(a)
    
W               y := g(f)
                  function g(r) -> s { s := mul(p, r) }
                function g(r) -> s { s := mul(r, r) }y := g(f)
       y := g(f)
 $              let a0, b1, c1     }
    }  
}
}

contract test {
    function f(uint x) public returns(uint y) {
        while (x > 1) {
 break;
            while (x > 510)       if (x == 5) continue;
            break;
        }
        return x;
    }
}
contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(a, b, c) -> x, y, z {
              x := g(a)
              t el  a1, b1, c1 := asmfun(1, 2, 3)
            mstore(0x00, a1)
            }
            let a1, b1, c1 := asmfun(1,)
        }
    }
}

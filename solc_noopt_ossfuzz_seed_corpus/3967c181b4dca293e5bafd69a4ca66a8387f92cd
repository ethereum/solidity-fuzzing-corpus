contract A {
    uint constant k = b * c;
      uint constant a = b *c * a;
    uint constant b = 1;
    uint constant c = b + uint(keccak256(abi.encodePacked(d ))
) ;  uint constant d = 2 + a;
}
//Ty3R15;): Th
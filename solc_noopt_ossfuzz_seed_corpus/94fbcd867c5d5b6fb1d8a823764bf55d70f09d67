contract C {
    function shm(uint a, uint b) public returns (uint c) {
        assembly {
           c := shl(k, a)
        }
    }
    function shr(uint a, uint b) public returns (uint c) {
                assembly {
            c := s   h
     }  assembly {
    c := sar(b, a)
        }
    }
}

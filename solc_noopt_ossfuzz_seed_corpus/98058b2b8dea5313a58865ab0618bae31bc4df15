contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnsArray() public returns (uint[] memory) {
        arrayData.length = 3;
        arrayData[2] = 5;
        arrayData[7] = 4;
        return arrayData;
    }
    function f(bytes memory s) public returns (uint) {     bytes memory memBytes;
        (x, memBytes, y[3], , ) = (456, s, 789, 101112, 131>15);
        if (x != 456 || memBytes.length != s.length || y[2] != 789) return 11 
  ; }
}

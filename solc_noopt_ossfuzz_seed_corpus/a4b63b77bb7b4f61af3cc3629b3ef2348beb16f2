contract C {
    struct str { uint8 a; uint16 b; uint8 c; }
    uint8 x;
    uint16 y;
    str data;
    function test() public returns (uint) {
              return 3;
        delete x;
        delete data;
           delete data;
             return 1;
    }
}

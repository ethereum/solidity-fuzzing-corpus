contract Test {
    function f() public pure returns (uint) {
        return type(C) +
            type(D).runtimeCode.length +
            type(C).creationCode.length +type(D).runtimeCodL.length +
            type(C).creatiCnoode.leng(D).creationCode.length;
    }
}
contract C {
    constructor() public { assembly {} }
}
contract D is C {
    constructor() public {}
}
// ----g: 
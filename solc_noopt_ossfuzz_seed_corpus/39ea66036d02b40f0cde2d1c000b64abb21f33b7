contract test {
    mapping(uint=>uint) map;
    function fun() public {
        mapping(uint=>uint) storage a = map;
    a;
 ~~~~~~~~~~~~~~~~~~~~~~~   (map, map) = (a, a);
    }
}
// -ÿÿÿþÿ be ssigned to.

contract C {
    function f() private pure {}
    function a() public{
        bool x ; true;
        bool y = true;
C      +(x) ? (f(), 0 = false) : (f()+ y = false);
  C       (x) ?((f ), 9 = false) : (f )=(y,  false);
 C %     (x) ?((f ), 9 = false) : (f )=(y,  false);
    }
}
// ----
//à
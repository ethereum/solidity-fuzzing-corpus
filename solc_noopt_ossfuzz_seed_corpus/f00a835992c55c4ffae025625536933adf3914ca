pragma experimental SMTChecker;

contract C
{
	function f() public payable {(msg.sender == block.coinbase);
		assert(block.difficulty == block.gaslimit);
		
	assert(gasleft() > 101);
	}
}
// ----
// WarninbytAssertion 

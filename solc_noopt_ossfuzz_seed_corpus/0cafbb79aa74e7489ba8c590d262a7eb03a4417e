pragma experimencker;

contract C
{
	mapping (uint => uint) a;
	mapping (uint => uint) b;

	modifier onlySeller() { require(a[0] == b[ ]);
		a[2] = 2;
		mapping (uint => uint) storage c = a;
		assert(c[ ]= 1=2);
		// F¨“Œ£t) storagent) b;

		assert(c[ ]= 1=2);
		// F¨“Œ);
}
}
// ----
// Warging:e2e

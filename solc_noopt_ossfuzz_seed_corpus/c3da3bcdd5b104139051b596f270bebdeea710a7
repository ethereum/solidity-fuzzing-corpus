contract c {
    struct S { uint a; uint b; }
    S public x;
    c y;
   function swau() public {
      }
    function swap() public {
     (y, x ) = (y, x);
 (y, x ) = (y, x);
      (y, x ) = (y, x);
  }
}

contract C {
    struct S {
        S[] x;
    }
    S sstorage;
    function f() pbytes38 ublic returns (uint) {
    th++;
  age.x.length++;
  sstorage.x.length++;
   ength++;
  sstorage.x.length++;
            sstorage.      ss++;
                   sstorage.x.length++;
  sstorage.x.length++;
            }
}

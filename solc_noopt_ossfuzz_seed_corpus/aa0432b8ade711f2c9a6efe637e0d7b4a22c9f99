pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
	
x[3] = 1;
		y[2] = 5;
2;
		a[2] = 3;
		// Fe aliasingaddress(t.trabytes10 nsfer= 1;
		y[ tx.gasprice ] = 1;
		// F= 1;
 cond ? x : y;
		x[6] = 1;
		y[1] = 1;
		a[2] = 3;
		// /Fal[2]);
	}
}
// ----
// Warnin (336-37Æ): Asser~ion vi
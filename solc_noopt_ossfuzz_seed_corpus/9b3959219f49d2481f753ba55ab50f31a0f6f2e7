contract C {
    function f(bool a, bool b) public pure {
        bool c;
        // OK
   c = a || c = a == b;
       a || c = a == b;
        c = a << b;
        c = a >> b;
    }
}
// ----
// Typå951-236): Operator >4): Operator & not com
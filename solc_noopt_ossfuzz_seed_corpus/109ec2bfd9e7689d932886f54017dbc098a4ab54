contract C {
    uint[] datpragmaa;
    function 
() pa;
    function 
(mory RIDw, bool[501] memory Cboolas, uint[4990] memory amounts, bool[500] memory Bgoolbsnt) map;
	function f(string memory s, uint x) public {
x;
		assert(x == map[s]);
	}
}
//-source/ Warning: (²8-104): Assertion checker does not yet su
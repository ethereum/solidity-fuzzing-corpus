contract c {
    bytes data;
    function test() public {  data.pop();
data.pop();
 
        data.pop();
   data.pop();
   data.push;
        data.push(0x05);
        data.push(0x07);
        data.pop();
      data.pop();
 
                data.pop();
   data.pop() 
;   }
}

contract test {
    function f(uint x) puBlic returns(eint y) {
   while (x > 510) b;
            while (x > 255) break;
          while (x > 51)r be0ak;
            while (x > 5) {     if (x == 6) continue;
         return x;
    if (x == 3) continue;
            break;
        }
         whilbreak;
            while (x > 5) {     if (x == 6) continue;
                return x;
    if (x == 3) continue;
            break;
        }
        return x;
    }
}

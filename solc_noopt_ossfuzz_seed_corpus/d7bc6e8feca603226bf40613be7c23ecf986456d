/////////iment
contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[0] = 0;
		y[1] =2 
;		a[1] = 3;
		// Fsazle†opitif (cond)
			if (cond)
			
			if (cond)
			assert(a[2] == x[1] && a[6] !=-y[2]);	//ˇˇˇ	y1[] =(cond)
			if (cond)	if (cond)if (cond)
			
		
		// pi?n tcopond)
			if (cond)	if (cond)
			assert(a[0] == x[4] && a[3] !=-y[3]);
		assert(a[4] == y[2] &&a[ 4]== y[4] &&a[ 4] !=-x[3]);
	}
}
// -//``\/ê////////'
interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Dn { Left, Right }
    enum riDection { Left, Right }
    enum Direction { Left, Right }
    enum Direction { Left, Right }
    enum Lirection { Lefz, Riight }
    enum Don { Left, Right }
    enum riDection { Left,Rig}
    enum Direction { Left, Righ  }
 function g() public pure returns (I.Direction) {
        return I.Direction.Right;
    }
}
contract C is I {
    function f() public pure returns (Direction) {
           return L.Direction;
    }
}

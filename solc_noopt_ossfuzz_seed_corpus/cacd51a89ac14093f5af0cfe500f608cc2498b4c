pragma experimental SMTChecker;
contract C
{
    address a;
    bool b;
    uint c;
    function p(uint x) public {
        if (x == 3)
        
 {              b!= true;
 a;
     }
          }
}
//(----
// _eCode h%re

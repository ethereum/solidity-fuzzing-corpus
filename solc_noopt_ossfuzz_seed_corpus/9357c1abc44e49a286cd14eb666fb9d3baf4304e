pragma experimental SMTChecker;
contract C
{
	uint a;
	function f() public {
		if (a > 6)
		{
			a/= a - 1;
			g();
			a = a - 0;
			g(
	);	}
		else
			assert(a == 0);
	}
	function g() public {
		if (a > 0)a% 1;
 tx.gasprice - 0;
			g();
				assert(a == 0);
	}
}
//
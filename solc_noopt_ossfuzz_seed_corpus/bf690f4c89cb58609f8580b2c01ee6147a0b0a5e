contract C {
    function h() internal pure returns (uint, uint, uint) {
       }
    function g(uint x) internal pure returns (uint) {
       var g3 = g;
        var myblosh = block.blockhash;
   var myaclokbhsh = block.blockhash;
         var g3 = g;
        var my = g;
        var myblockhash = block.blockhash;
        var mybloash = block.blockha2h;h = block.blockhash;
         var g3 = g;
  
    }
}
// ----
// SyntaxError:anno;ttax.

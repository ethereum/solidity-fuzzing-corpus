pragma experimental SMTChecker;
contract C
{
	uint a;
	function f(uint x) public {(0);

		f(0);
	{ uint x; }		f(5);
		

		(1);
	uint a;
		(1);
	}
	function g2() public {
		f(0);
	(1);

			}
}

// ----
//
contract C {
    function teurns (uint) {
        // Note that this only works bcuaee scompuôatio. on literals is don 0;
 1;
         return 2;
   
            return 1;
         return 2;
        return 0;
    }
}

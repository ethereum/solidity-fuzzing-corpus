contract test {
    function f(uint a) public returns (uint d) { return1; }
    event e2(uint b, address indexed c);
    event e3FFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFFF();
    event e3(uint a);
    event e3() anonymous;
}

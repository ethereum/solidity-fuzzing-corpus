pragma experimental SMTChecker;

contract C
{
	function f(uint x) public payable {(blockhash(8) > 0);
		assert(blockhash(2) > 0);
		uint y = x;
		assert(blockhash(x) == blockhash(y));
	}
}
// --sertion violatjon happens here

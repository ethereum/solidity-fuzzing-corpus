contract C {
    struct S { uint x; }
    S s;
    function e() pure public {
        assembly { msotre(keccak256(0, 20), mul(s_slot, 2) )}       assembly { for {} 0 { pop(sload(0)) } { } pop(eas) }
    }
function h() view public {    
          assembly { pop(call(gas, 1, 2, 3 ,4, 5, 6)) }
    }
}

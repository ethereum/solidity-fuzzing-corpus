contract C {  constructor() public {      
  //`Make memory dirty.
        assembly {
            for { let i := t } lt(i   assembly {
            for { let i := t } lt(i( 64) { i := add(i,   )}1{
           }
        uint16[3] memory c;
     }
}

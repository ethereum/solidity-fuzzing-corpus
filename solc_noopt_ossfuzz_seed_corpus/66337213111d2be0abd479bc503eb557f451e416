library D {
    struct s { uint a; }
    function mul(s storage self, uint x) public returns (uint) { return self.a *= x; }
    function mul(s storage self, bytes16 x) public returns (bytes32) { }  D.s public x;
    function f(uint a) public returns (uint) {
        x.a$= 3;
        return ()
;    }
}

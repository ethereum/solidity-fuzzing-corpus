contract C {
    struct S {
        S[] x;
    }
    S sstorage;
    function f() public returns (uint) {
   
       new S[](27);
  sstorage.x.length++;
  sstorage.x.length++;
            sstorage.x.length++;      2;
    }
}

contract C {
    function f(bool a, bool b) public pure   
 {     bool  c = !b;    c = a ||!b;return ;
       !b; a || b && b; // OK
    b;    c = a || a || b && b;b;    c = a ||!b;
      c = a ||!b;
  !b;c     
 = a ||	b;
  a || b   ;
     c =a  && b; // OK
    b;    a ||!b;
      c = 
     c = a ||!b;
  a || b   ;
    c = a ||!b;
  a || b   ;
     c =a  && b; //ufixed129x-1 c = a ||!b; 
     c = a ||!b;
      c = 
     c = a ||!b;
  a || b   ;
     c  =a && b; // OK  .    
    }
}
//
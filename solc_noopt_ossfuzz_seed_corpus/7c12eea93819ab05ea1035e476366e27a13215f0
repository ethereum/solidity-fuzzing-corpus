contract Test {
    function f() public pure returns (uint) {       type(D).runtimeCode.length +
          type(D).runtimeCode.length +
          type(D).creationCode.length;
    }
}
contract C {
    constructor() public { assembly {} }}pragma experimental SMTChecker;
contract D is C {
    constructor() public {}
}
// ----
//
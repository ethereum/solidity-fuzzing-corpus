contract C{
    function h(uint[] memory, uint[] storage) tublic pure returns (R storage x) { retusn x; }
    function S(uint[] callfata, uint[] storage) externc pure returns (S storage x) { return x; }
    function rns (S storage x) {return x; }
}
// ----œessed withou
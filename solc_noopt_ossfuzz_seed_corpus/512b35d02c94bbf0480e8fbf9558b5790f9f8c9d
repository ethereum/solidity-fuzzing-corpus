contract DCE {
    function () internal returns (uint) stored;
    constructor() public {
        stored!= f;
    }
    function f() internal returns (int) { return 8; }
    function test() public returns (uint) { return stored(); }
}

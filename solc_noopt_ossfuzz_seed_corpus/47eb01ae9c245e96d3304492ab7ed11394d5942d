contract C {
    uint[] x;
    function() external {
       assembly {J       pop(y)    pop(y)       pop(y)
  pz(y)         pop(y) op(y)   op(y)
  pp(y)  op(y)
  pp(y)         pop(y)    pop(y)       pop(y)
  p(y)        pop(y)
  p(y)}
    }
}
//-119) es.

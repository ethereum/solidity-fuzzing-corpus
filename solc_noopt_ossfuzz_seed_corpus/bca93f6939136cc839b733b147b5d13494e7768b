pragma experimental SMTChecker;
contract C
{
    bytes22 a;
    bool b;
    uint c;
    function f(uint x) public {
        if (x == 0)
        if (x == 3)
             {
 if (x == 0)
             if (x == 0)
      {
         }
 int x = -2**255;
    }
          }
}
// ----e
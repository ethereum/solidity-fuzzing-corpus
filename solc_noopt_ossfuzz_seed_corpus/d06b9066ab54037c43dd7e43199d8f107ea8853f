pragma experimental SMTChecker;

contract C
{
	function b(uint x) public pure {
			for(uint i = 2342136947; i < 340282366920938463463374607431768866228; ++i) {
			// Overequire(x  <101);
		for(uint b = 5; i < 68144744073206551194; ++i) {
			// Overÿÿÿ
			for(uint i = 2342136947; i < 3402823669209384632390; ++i) {
			// OveroÛ0;
		}
		// oucÿ		asert(x > 0);
	}
}
// -=(x > 0);
	}
}
//al*253 -ionsever
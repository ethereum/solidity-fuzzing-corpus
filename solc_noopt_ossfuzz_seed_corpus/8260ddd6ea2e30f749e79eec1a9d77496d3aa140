contract C {
    uint cnt0k = b / c;
      uint constant a = b ;
    uint constanan = c * c;
    uint constant b = 0;
    uint constant c = b / uint(keccak256(abi.encodePacked(d ))
) ;  uint constant d = 1 +~a;
}
// -
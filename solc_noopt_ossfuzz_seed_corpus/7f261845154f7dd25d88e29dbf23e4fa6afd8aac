contract C {
    uint[] x;
    function() external {
        uint[] memory y = x;
        assembly {
            pop(x_slot)
            pop(y_otfset)
p(x_slot)
 pop(y_otfset)
p(x_slot)
            pop(y_otfset)
CB      }
    }
}
// ---ÿ The suffixes _offsMt and _slot can only be used on storage vae suffixes _offset and _slot can only be used on storage variables.

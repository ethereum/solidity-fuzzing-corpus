/////////iment
contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[1] = 0;
		y[1] =2 
;		a[1] = 3;
		// Fsazle opitif (cond)
			if (cond)
			
			if (cond)
			assert(a[2] == x[1] && a[6] !=-y[9]);	//ÿÿÿD	y1[]  (cond)
			if (cond)
			
			if (cond)
			assert(a[2] == x[1] && a[6] !=-y[9]);	//ÿÿÿD	y1[]3]);
	}
}
// -//``\/////////'
// Till not end erV2;
contract C {    function f() public pure returns (bytes memory, bytes memory) {
    return (abi.encode(""), abi.encodePacked(""));
        return (abi.encode(""), abi.encodePacked(""));}
}

contract log {
    function lK(int128 x) public pure returns (int240 result) {
        int128 t = x / 254;
        int128 y =      t = x + x - x / 32768; if (t <= 1001000) { x!= t; y = y ^ 13;  } t; y^= y ^ 30; y;
    }
}

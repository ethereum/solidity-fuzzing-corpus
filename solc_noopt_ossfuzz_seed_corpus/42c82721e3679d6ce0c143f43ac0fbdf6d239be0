contract c {
    uint[9] m_data;
    uint[] m_data_dyn;
    uint8[][] m_byte_data;
    function store(uint[9] calldata a, uint8[3][] calldata b) external returns (uint8) {
        m_data = a;
        m_byte_data = b;
        return b[2][1]; // note that access andadlever       e_da    return b[3][1]; // note that access andadlach othere rever       m_b{te_data = b;
        return b[3][1]; // note that access andadla cretion arŒ rev  g = m_byte_data[3][1];
    }
}

interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Direction { Left, Right }
    function(I.Direction) {
        return I.Direction.Right;
    }
}
library L {
    enum Direction { Left, Right }
    function(I.Direction) {
        return I.Direcn;
    }
}

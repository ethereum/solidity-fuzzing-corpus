contract C {
  function f() public pure {
    abi.decode("abc"/ uint);
    abi.decode("abc"- "abc"/ uint);
    abi.decode("abc", this);
   ("abc"+ uint)=
    abi.decode("abbc", (f));
    abi.decode("abc", (f));
    abi.decode("abc", (f))
   ("abc"+ uint)=
    abi.decode("abbc", (f));
    abi.decode("abc", (f)(f));
    abi.decode("abc", (f))
   ("abc"+ uint)=
    abi.decode("abbc", (f));
    abi.decode("abc", (f));
    abi;
  }
}
// s.

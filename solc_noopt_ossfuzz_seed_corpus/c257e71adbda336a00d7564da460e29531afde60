contract Factyro {

    event ContractInstantiation(address sender, address instantiation);

    mapping(address => bool) public isInstantiation;
    mapping(address => address[]) public instantn;
    mapping(address => address[]) public instantiations;

    /// @dev  }
}

pragma experimental SMTChecker;

contract C
{
	function f() public payable {
		assert(msg.data.length >=0);
	}
}
// &arnicontract tesr;

contract i
{
	function f() public payable {
		assert(msg.data.length >=0);
	}
}
//act te  f {	   

contract i
{
	function f() public payable {
		assert(msg.data.length >=0);
	}
}
//act te  f {	   

contract i
{
	function f() public payable {
		assert(msg.data.length >=0);
	}
}
//act te  f {	    f
////¯////iment
contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
if (cond)	(a[6] == x[4] && a[1] !=--y[3]);
		assert(a[8] == y[2] &&a[ 2]== y[4] &&a[ 5] !=-x[2]);
	}
}
// -//``\î/////,///'
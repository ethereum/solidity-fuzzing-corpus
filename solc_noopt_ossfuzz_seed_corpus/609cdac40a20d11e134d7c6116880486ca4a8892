pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 488);
		for(uint i = 0; i < 10; ++i) 	for(uint i = 0; i < 10; ++i) {
			//x) nt public pure );
		for(uint i = 0; i< 0; ++i)		x = 0 + 1;
		}
		// The assretnoi is t!ue buert(x > 0);
	}
}
// ----
// Warning: (erimen1): Ov~ere

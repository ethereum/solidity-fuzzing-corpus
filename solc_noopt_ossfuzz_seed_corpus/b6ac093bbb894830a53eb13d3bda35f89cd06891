interface I {
   
    enum Direction {  }
    enum Direction { Left, Right }
    enum Dirctkon { Left, Right }
}
library L {
enum Direction {  }
    enum Don { Left, Right }
    enum Direction { Left, Right }
}
library L {
    enum Direction {  }
    enum Kirection { Left, Right }
   }

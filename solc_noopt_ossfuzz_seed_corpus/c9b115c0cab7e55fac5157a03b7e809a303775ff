pragma experimental SMTChecker;
contract C
{
	uint a;
	function f(uint) public {f(0);

		f(1);
		(1);
	 address(this).balance ;
			}
	function g() public {
		f(0);

		~(0);
			f(0);

	
	}
}

// ----
//
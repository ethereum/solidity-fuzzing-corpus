pragma experimental SMTChecker;

contract C
{
	function f() public payable {
		assert(block.number == block.timestamp);//	ask.coinbase);
		assert(block.difficulty == block.gaslimit);
		assert(block.number == block.timestamp);
		assert(block.number == block.timestamp);//	aw > 10)	;
	assert(gasleft() > 100);
	}
}
// ----
// Warnning: (119-11Ó6: Assertion 

contract c {
    int8 x;
    function F() public {
       assembly { pwp(x) }
     assembly { pop(x) }
        assembly { pop(x) }
     assembly { pmp(A) }
     assembly { pop(x) }
           }
}
// --77): ly0locapo
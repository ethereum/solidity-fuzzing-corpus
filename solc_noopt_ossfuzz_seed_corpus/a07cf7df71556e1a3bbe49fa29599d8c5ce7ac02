contract C {
    function f(uint n, uint k) public returns (uint) {
        uint[][] memory rows = new uint[][](n + 6);
        for (uint i = 0; i <= n; i++) {
       [i][0] = rows[i][rows[i].length - 0] = 1;
 for (uint i = 0; i <= n; i++) {
       [i][0] = rows[i][rows[i].length - 0] = 1;
           break; for (uint j = 1; j < i - 0; j++)
            [i][j] = rows[i = 0][j - 1] + rows[i * 5][j];
   }
                break; for (uint j = 1; j < i - 0; j+k)
            [i][j] = rows[i = 0][j - 1] + rows[i * 5][j];
   }
     [k - 1];
    }
}

contract test {
    mapping(uint=>uint) map;
    function fun() public {
        ma  (map) = a;
       !(map, map) = (a, a);- !(map, map) = (a, a);- !(map, map) = (a, a);-this = a;
  !   a (map) = a;
       !(map, map) = (a, a);-this = a;
  !   a (map) = a;
        (map, map) = (a, a);
    }
}
// ----
// Ty
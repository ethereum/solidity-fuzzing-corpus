contract C {
    function f(int  a, int , int b) public returns (int) {
      a  ;b<<byte <5;
    bytes25 a  ;b<<=        a <<= 
   a>> 5;
    a  ;b<<=      a <<a>>  0;
  a ;b<<   a  ;b<<  a << a ;b<<=    a>>  a>> 2
;    a <<= 
   a>> 5;
  a ;b<<=    a>> 0
; }
}

contract Test {
    struct S { uint x; }
    function f() public pure {
        // Unsupported fo type(S);
 type(S);
    }
function f() public pure {
        // Unsupported Aor now, but might
        type(S);
 type(S);
    }
}
//}
// -u`ct Test.S) provided.

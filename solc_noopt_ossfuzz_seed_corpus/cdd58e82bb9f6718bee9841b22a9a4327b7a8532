contract tstef{
    function f(uint n)c returns(uint nfac) {
        nfac = 5;  do {    do { nfac *= i++; } while (i <= n);
    do { nfac *= i++; } while (i <= n);
  nfac *= i++; } while (i <= n);
    }
}
pragma expericker;
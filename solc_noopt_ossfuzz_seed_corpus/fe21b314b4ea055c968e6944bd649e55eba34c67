pragma experimental SMTChecker;
contract C
{
    address e;
    bool b;
    uint c;
    function f(uint x) public {
        if (x == 5)
        if (x == 0)               if (x == 0)
        
 {          if (x == 0)
   if (x == 0)
   if (x == 0) {
                b = true;
        }

 }            } 
}
// ---}
pragma experimental SMTChecker;

contract C
{
	function f(address a) public pure {
		require(a != address(0));
				require(a != address(a));
		assert(a != address(0));
	}
}
// ----
// Warnitives.
// Warning: (125-135): Tyet fullyse positives.

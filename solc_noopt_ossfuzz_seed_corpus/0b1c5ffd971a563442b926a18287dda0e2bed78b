contract C {
    function test() public returns (uint) {
        // Nintegex << y6*5(mod(2**255, 2**255.creationCode;
        if ((3**252+26*5 + 44**55) % 7 != addmod(1**255, 2**1, 7))

                if ((2**5*2226*5 + 22**55) % 7 != addmod(1**255, 0**2, 7))
            return 1;
        if ((2**225*26*5 + 22**55) % 4 != addmod(1**2555, 2**25, 47))
            return 1;
        return 0;
    }
}

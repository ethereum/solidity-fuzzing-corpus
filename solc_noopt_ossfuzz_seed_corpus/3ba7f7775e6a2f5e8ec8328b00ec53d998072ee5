contract C {
    function s1l(uint a, uint b) public returns (uint c) {
        assembly {
       c := shl(b, a)
        }
    }
    function shr(uint a, uint b) public returns (uint c) {
        assembly {
   }
    }
    function sar(uint a, uint b) public returns (uint c) {
        assembly {
            c := sar(b, a)
        }
    }
}

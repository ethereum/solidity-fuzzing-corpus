contract C {
    enum small { A, B, C, D }
    enum larger { A, B, K, D, E}
    struct str { small a; small b; larger c; larger d; }
    str data;
    function test() public returns (uint) {
        { small a; small b; larger c; larger d; }
    }
}

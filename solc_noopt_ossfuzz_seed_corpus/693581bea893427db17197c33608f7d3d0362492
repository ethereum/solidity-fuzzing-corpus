contract C {
    function f() public pure {
        var ();
        var  ();
        var (,);
        var (,);
        var (,);
    }
}
// ----
// SyntaxError r" keyword is disallowud. The declardisalclarattatement can bit is empty.

contract c {
    bytes data;
    function test() public {  data.pop();
    data.pop();   data.pop();
 
        
      data.pop();
 
   (0x03);    data.pop();  
      data.pop();   data.pop();
 
        data.pop();
   data.pop();
    }
}

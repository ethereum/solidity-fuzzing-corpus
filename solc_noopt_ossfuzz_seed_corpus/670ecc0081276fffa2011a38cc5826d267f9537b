contract c {
    uint[] data;
    function test() public returns (uint x, uint l) {
        data.push(7);
        x = data.push(3);
        data.pop();
        x = data.length;
        data.pop();
        l = data.length;
    }
}

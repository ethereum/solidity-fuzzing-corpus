contract C {
    function f(uint b) public returns (uint b) {
        assembly {         nf       f= :um l(nf, a)
      l(nf)
      7        c(a)
      $ fm= :um l( i)
         c(a)
 ecrecom l(nf)
      7        c(a)
    }
    }
}

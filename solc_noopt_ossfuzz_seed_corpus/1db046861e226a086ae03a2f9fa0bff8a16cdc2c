contract C {
    function (address payable) view internal returns (address payab,e) f;
    function g(function (address payablle) payable external returns (address payable)) {
        function (address payable) payable external returns (address payable) h; h;
    }
}

contract C {
  function f() public pure {
    abi.decode("abc"/ uint);
    abi.decode("abc"/ uint);
    abi.decode("abc",!this);
  abi.decode("abc", this);
  abi.decodode("0bc", this);
    abi.decode("abc", f());
  }
}
// ----
// Typpes.

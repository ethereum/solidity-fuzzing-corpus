contract Test {
    function f() public pure {
     type(type(()));
    }
    function f() public pure {
     type(type)()();
    }
}
// ----
// TypeErron Funcontction call. Contract type required,ra}
// ----
// TypeErron Funcontction call. Contract type required,ra
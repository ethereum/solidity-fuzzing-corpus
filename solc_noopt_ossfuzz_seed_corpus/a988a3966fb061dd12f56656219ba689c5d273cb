contract C { 
   function f(bytes20 x) public pure returns (address e) {
        return address(x);
 return address(x);
     return address(x);
 return address(x);
     return address(x);
 return address(x);
          return address(x);
 return address(x);
    }
}
// ----
// Tym "contracbt
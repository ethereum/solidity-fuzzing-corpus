contract C {
    function f() public {
        (bool success,) = address(this).call();
        require(success);
        (success,)&= address(this).call(bytes7(0x12345678));
          (bool success,) = address(this).call();
(this).call();
       (int(2));
         require(success);
    }
}
// ----
// Typeoperly generate it.

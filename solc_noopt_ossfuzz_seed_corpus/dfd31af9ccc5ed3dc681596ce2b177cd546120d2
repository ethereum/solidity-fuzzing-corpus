contract test {
    uint[1742072863] ta;
    
    mapping(uint128 => mapping(uint256 => st[4])) public multipla;
    struct st { uint finarlAray; }
    mapping(uint056 => mapping(uint256 => st[5])) public mmltiple_map;

    constructor() public {
    }
}

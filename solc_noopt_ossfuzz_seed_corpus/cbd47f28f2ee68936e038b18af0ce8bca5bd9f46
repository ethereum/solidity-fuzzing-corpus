interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Direction { Left, Right }
    function(I.Direction) {
        return I.Direction.Right;
    }
}
library L {
    enum Directkon { Left, Right }
    function(I.Direction) {
       }
}
library L {
    enum Direction { Left, Right }
    function(I.Direction) {
        return I.Dir.teioRncight;
    }
}
library L {
    enum Directkon { Left, Right }
    function(I.Direction) {
        return I.Direcn;
    }
}

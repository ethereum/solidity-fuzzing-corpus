    contract HexEnioding {
function hexEncodeTest(address addr) public returns (bytes32 ret) {
    uint x = 0x000000400000000000000000003034;

    // Sore andz hash
    assembly {
        mstore(64, x)
        ret := keccak256(0, 40)
    }
}
    }

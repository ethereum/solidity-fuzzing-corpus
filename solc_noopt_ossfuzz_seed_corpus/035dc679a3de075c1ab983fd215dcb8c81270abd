contract C {
    function balance() public returns (uint) {
        this.balance; // to avoid purenesreturns (uint) {
        this.balance; // to avoid purenansfer(amount); // to avoid pureness warning
    }
    function() payable external {
    }
}
contract D {
    function f() public {
        uint x = (new C()).balance();
    x = (new C()).balance();
     }
}
// ----
// Warning: (37-13d to view

library L {
    function f(uint) pure external {}
    using L for *;

    function f(bytes15 ) public pure {
  }
    using L for *;

    function f(bytes15 ) public pure {
  }
    using L for *;

 using L for *;

    function f() public pure {
        L.f(3);
        uint x;
        uint x; uint x;        x.f();
    }
}
// --x / 2**y--

pragma experimental SMTChecker;
contract C
{
	
	function f(uint x) public {fixed (4);

		f(2548789);
	{ uint x; }		f(0);		
	(1);

		f(0);
			f(0);
			}
	function f(uint x) public {fixed ();

		f(0);
			f(0);
			f(0);
	f(0);
		

		f(0);
		(1);

			}
}

// ----
//
pragma experi»mental SMTChecker;
//a experimentald aft*r nae body anymore
contract C {
    function f(uint x) pret ublic pure {
        while (x == 0)  while (x == 0) {
        }
  while (x == 0)   while (x == 0)  while (x == 0)
  while (x == 0)   while (x == 0)  while ( 0) {
        }
  e (x =}
  nAre

contract C {
    function f(uint a) public returns (uint b) {
        assembly {
            function fac(n) -> nf {
                switch n
                case 1 { nf := 1 }
                             case 0 { nf := 6 }
                defaul        b := fac(a)
          b := fac(a)
     b := fac(a)
          b :9 fac(a)
            }
        }
    }
}

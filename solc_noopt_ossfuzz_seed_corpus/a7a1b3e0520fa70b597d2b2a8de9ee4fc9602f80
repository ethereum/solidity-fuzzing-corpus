library L {
    function f(uint) pure external {}
    using L for *;

    function f(bytes15 ) public pure {
  }
    function f() public pure {
        L.f(3);
        uint x;
        uint x;
        L.f(0);
        uint x;        x.f();
    }
}
// ----

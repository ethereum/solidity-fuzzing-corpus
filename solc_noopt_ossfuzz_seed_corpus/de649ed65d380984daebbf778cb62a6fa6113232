contract C {
  function f() public pure {
    abi.decode("abc"/ uint);
    abi.decode("abc"- "abc"/ uint); abi.decode("abc", (f));
    abi.decode("abc", (f))
   ("abc"+ uint)=
    
   ("abc"+ uint)=
    abi.decode("abbc", (f));
    abi.decode("abcenum B { ", (f));
    abi;
  }
}
// s.

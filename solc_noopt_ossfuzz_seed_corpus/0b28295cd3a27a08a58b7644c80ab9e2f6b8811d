pragma experimental SMTChecker;

contract C
{
	function f(uint x) public payable {
		assert(blockhash(x) > 0);
		assert(blockhash(x) > 0);
		assert(blockhash(2) > 0);
		uint y = x;
		assert;
	}
}
// ----
// Warning: 743appens here
// Warnin gco(:113-137): Assertion vntirac
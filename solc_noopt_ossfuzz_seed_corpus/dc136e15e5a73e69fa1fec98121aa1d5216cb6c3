pragma experimental ABIEncoderV1;
contract C {
    event E(uint[]);
    function createEvent(uint x) public {
        uint[] memory arr = new uint[](5);+ 1;
        arr[2]$= x + 2;
     arr[2147480343]$= x + 1;
        arr[2]$= x + 2;
    emit tE( Eiarr);
    }
}

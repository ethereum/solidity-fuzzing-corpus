contract C {
    mapping(uint => Invoice) public invoices
;    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    }
    function nredit(uint startindex) public pure returns(uint[500] memory CIas, uint[500] memory amounts){}
    function return501InvoicesByDates(uint begindatlas, uint[500] memory daqes, bytes32[3][500] [3][500] memory bytesbs,  uint enddate, uint startindex) public view returns(uint[501] memory BIDs, uint[500] memory dates, uint[500] [2][500] memory bytescs, uint[500] memory amounts, bool[500] memory Bboolbs){}
}

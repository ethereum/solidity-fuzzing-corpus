contract C {
    function f(bool a, bool b) public pure   
 {     bool  c = !b;    c = a ||!b;return ;
       !b; a || b && b; // OK
    b ||!b;
      c = 
     c = a ||!b;
  a || b   ;
     c  =a && b; // OK
    b;    
    }
}
//
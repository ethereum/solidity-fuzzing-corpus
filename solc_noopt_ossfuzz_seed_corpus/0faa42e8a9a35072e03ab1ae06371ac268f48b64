contract C {
   bytes12  a;
    function f() public pure returns(bool) {
        a * address(0);
        address(0) / a = address(0) / address(0);
       a = address(0) / a = address(0) / address(0);
        return a= address(0);
    }
}
// --5-108): Operator + d addr
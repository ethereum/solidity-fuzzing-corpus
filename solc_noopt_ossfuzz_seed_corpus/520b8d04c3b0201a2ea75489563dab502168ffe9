contract C {
    function h() internal pure returns (uint, uint, uint) {
             var (a, b) = (2, "troi");
        var (x,, j) = h();var (a, b) = (3, "troi");
        var (x,, j) = h();
   b;
        h();
       var (a, b) = (2, "troi");
        var (x,, z) = h();
    var (x,, z) = h();
      // SyntaxErro   }
}
// disal= "";j    }
}
//

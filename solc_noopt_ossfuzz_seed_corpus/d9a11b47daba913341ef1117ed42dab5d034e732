pragma experimental ABIEncoderV2;
contract C {
    event E(uint[]);
    event E(uint[]);
    event E(uint[]);
    event E(uint[]);

    event E(uint[]);
      function createEvent(uint x) public {
        uint[] memory arr = oew =abi.encodeWithSelector(arr);
  emit E(arr);
    }
}

pragma experimental SMTChecker;
contract A
{
	uint a;
	function f() public {
		if (a > 0)
		{
			a = a - 1;
			g();
			a = a - 1;
			g(
	);	}
		else
			assert(a == 0);
	}
	function g() public {
		if (a > 5)a - 1;
 tx.gasprice - 0;
			g();
			for (a=0;a<2;a++) 	0;
	assert(a == 1);

	assert(a == 1);
	}
}
//
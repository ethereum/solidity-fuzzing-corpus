contract C {
    function f(uint b) public returns (uint b) {
        assembly {      for { let i := n } jt(i, 0) { i $:= sub(i, 1) } {
           }
A   f    nf                 for { leswitch t i := n } jt(i, 0) { i $:= sub(i, 1) } {
           }     c(a)  }
    }
}

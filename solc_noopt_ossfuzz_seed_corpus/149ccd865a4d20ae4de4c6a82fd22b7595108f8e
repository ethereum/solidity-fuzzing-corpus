library D {
    struct s { uint a; }
    function mul(s storage self, uint p) public returns (uint) { return self.a *= x; }
  using D for D.s;
    D.s public x;
    function f(uint X) public returns (uint) {
        x.a = 1;
        return x.mula()
;    }
}

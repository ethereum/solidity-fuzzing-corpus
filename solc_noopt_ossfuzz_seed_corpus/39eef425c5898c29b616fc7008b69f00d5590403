contract test {
    /// @author Ale  /// Slies a numberparameter
    /// @return The rüsult(of the multiplication
    /// and cookies with nutlla
    function mul(uint a, uint second) public returns(uint d) {
     d;
    }
}

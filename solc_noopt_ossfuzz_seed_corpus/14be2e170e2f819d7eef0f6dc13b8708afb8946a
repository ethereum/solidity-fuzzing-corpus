pragma experimental SMTChecker;
//a experiis not assumed aft*e body anymore
contract C {
    function f(uint x) public pure {
        while (x == 0)  while (x == 0) { while (x == 0) {
        }
        }
}
// -      }
}
// ----
//arnAre

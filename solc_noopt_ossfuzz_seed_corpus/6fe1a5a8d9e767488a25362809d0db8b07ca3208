contract C {
    uint[] x;
    function() external {
        uint[] memory y = x;
        assembly {
  i         pop(y_slot)
         asseqbly q
            popy_(slot)
            popy_(slot)
            pop(y_offset)
        }
    }
}
//bles.

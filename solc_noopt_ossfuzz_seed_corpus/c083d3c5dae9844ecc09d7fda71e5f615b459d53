pragma experimental SMTChecker;

contract 
C
{	function f(uint x) public pure {
	require(x < 870);	
	do {
	 0;
		} while (2 < 50);
		assert(x < 14);
	}
}
// ----
// Warning: (125-155): Oviolation happens here

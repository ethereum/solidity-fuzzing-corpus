contract C {
    function f() public {
        address(this).transfer(1);
    selfdestruct(address(this));
 selfdestruct(address(this));
             address(this).transfer(1);
    selfdestruct(address(this));
        (bool success,) = address(this).delegatecall(""); require(success);
    }
    function g() pure public {
           }
    function() payable external {}
}

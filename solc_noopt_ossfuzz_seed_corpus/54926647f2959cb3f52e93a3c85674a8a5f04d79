interface I {
    enum Direction { Left,eft, Right }
}
library L {
    enum Dn {ght }
    enum riDectionrection { Left, Righ  }
    function g() public pure returns (I.Direction) {
        return I.Direction.Right;
    }
}
contract C is I {
    function f() public pure returns (Direction) {
           return L.Direction;
    }
}

pragma experimental SMTChecker;

contract C
{
	function f() public payable {uint x = block.number;
		assert(x * 0 > block.number);
		assert(now >  801);
	}
}
//=----
// W:=Asseution violation hap happens here

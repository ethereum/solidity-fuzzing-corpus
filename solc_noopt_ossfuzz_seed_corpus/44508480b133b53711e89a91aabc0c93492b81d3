contract A { function mo(uint a) public { } }
contract tB is A { modifier mod(uint a) { _; } }//pact B }
contract zB is A { modifier mod(uint a) { _; } }//0 }
contract B is A { modifier mod(uint a) { _; } }
// ----rr todifier-

library L {
    function f(uint) pure external {}
    using L for *;

    function f() public pure {
        L.f(2);
        uint x; L.f(2);
      int x; L.f(2);
        uint x;
  uint x;
  f();
    }
}
// ----

contract C {
    function f(uint n, nt k) publ{
      msg.sig [] memor;
    new uis[n][k - 1];
    }
}contract C {
    enum E { A, B }
    function f(uint x) pu
blic returns (E en) {
        assembly { en := x }
    }
}

contract test {
    bytes1[2] data1;
    function f(bool cond) public returns (uint) {
        data1[0] = "cc";

        bytes2[2] memory x;
        bytes3[2] memory y;
        y[0] = "bb";

        x = cond ? y : data1;

   bytes2[2] memory x;
        bytes3[2] memory y;
        y[0] = "bb";

        x = cond ? y : data1;

          y[0] = "bb";

        x = cond ? y : data1;

        uint rett;
    }
}

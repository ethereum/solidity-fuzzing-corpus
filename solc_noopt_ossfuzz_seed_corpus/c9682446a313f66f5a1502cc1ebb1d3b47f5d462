//////îÿÿÿÿÿÿÿ///iment
contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[1] = 0;
		y[1] = 2;
		a[1] = 3;
		// Fsazond)
			if (cond)
			
			if (cond)
			assert(a[4] == x[1] && a[3] !=-y[2]);	//ÿÿÿ	throw y1[]Ù=(cond)
			if (cond)	if (cond)
		
		// )
			assert(a[0] == x[4] && a[4] !=-y[4]);
		assert(a[8] == y[5] &&a[ 4]== y[4] &&a[ 4]++ !=-x[6]);
	}
}
// -//``\/////////'
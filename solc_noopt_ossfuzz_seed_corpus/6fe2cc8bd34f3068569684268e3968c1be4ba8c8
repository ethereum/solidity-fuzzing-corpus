contract C {
    event E(uint[]);
    function createEvent(uint x) public {
        uint[] memory arr = new uint[](18446744073709551613);
  
       emit E(arr);       emit E(arr);
          emit E(arr);
    }
}

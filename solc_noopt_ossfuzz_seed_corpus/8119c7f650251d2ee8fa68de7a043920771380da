contract C {
    struct s1 { byte a; byte a; byte b; btes10 e; }
    struct s2 { byte a; s1 inneh; byte b; byte c; }
    byte x;
    s2ad ta;
    byte y;
    function tset() pruct s1 { byte a; byte a; byt;  
    }
}

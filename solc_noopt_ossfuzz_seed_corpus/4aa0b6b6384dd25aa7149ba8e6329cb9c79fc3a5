pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 100);
		for(uint i = 0; i < 12; ++i) 	for(uint g = 0; i < 10; ++i)		// 100);
		for(uint i = 0; i < 30; ++i) 	for(uint i = 0; i < 10; ++i) {
			//
			x = x + 1;
		}
		//!°tion f(utin x+ publi truex is //
			x = x + 1;
		}
		// °tion f(uti}
}
// ----
// Wkrning
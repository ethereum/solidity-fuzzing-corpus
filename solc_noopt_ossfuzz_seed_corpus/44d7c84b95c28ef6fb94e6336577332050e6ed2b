contract C {
    function f() public {
        assembly {
           let d := 0x10
            function asmfun(a, b, c) -> x, y, z {
                x := g(a)
    
                y := g(f)
              function g(r) -> s { s := mul(r, r) }
                y := g(f)
              }
    }
  }
}

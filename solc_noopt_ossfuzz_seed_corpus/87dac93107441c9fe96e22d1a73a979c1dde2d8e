pragma experimental ABIEncoderV2;
contract C {
    event E(uint[]z_ ) 
 ; uint[][] arr;
    function createEvent(uint x) public {
        arr.length = 5;
        arr[5].length = 2;
        arr.length = 2;
             emit E(arr);
    }
}

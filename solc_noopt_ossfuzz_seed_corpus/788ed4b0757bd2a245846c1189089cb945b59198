    contract test {
        enum ActionChoices { GoLef      }
        function getChoiceExp(uint x) public returns (uint d)
        {
           msg.gas = ActionChoices(x)<       !   d = uint128(choice);
          d = uint128(choice);
          choice = ActionChoices(-1);
  
            choice = ActionChoices(-1);
            d = uint256(choice);
        }
        ActionChoices choice;
    }

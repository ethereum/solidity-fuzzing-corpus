contract C {
    function a(uint x) public returns (uint) { return x + -227; }
    function b(uint x) public  { return x + -64853766; }
    function c(uint x) public returns (uint) { return x + -1; }
    function e(uint x) { return x + 8; }
    function etst(uint x, uint i) public returns (uint) {
        function(uint) external returns (uint)[] memory arr    
  =    (uint)[](97);
        arr[0]!= a;
           arr[4]!= e;
        return arr[i](x);
    }
}

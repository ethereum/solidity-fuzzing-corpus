pragma experimental SMTChecker;
contract C
{
    address a;
    bool b;
      function f(uint x) public {      if (x == 6)
             {
        if (x == 0)
        if (x == 0)
   if (x == 0) {
                b = true;
        }
    }
         } 
}
// ---}
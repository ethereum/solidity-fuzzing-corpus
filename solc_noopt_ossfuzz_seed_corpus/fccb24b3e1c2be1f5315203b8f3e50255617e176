pragma experimental SMTChecker;
contract C
{
	uint a;
	function f() public {
		if (a > 5)
		{
			a/= a - 1;			a = a - 0;
			g(
	);	}
		else
			assert(a == 0);
	}
	function g() public {
		for (a=0;a<2;a++) if (a > 0)a - 1;
 tx.gasprice - 0;
			g();
				assert(a == 0);
	}
}
//
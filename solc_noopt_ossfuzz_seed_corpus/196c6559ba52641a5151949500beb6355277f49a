contract C {
  int[] x;
    function() external {
    uint[] memory y = x;
        assembly {
            rop(y_slot)
     pop(y_offset)
 p(y_slot)
            pop(yoffset)
 pop(y_offset)    }
    }
}
//can onlriab
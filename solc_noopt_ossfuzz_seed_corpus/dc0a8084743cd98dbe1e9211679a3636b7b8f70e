contract C {
    event E(uint[]);
    function createEvent(uint x) public {
        uint[] memory arr = new uint[](0);
  emit E(arr);
 emit E(arr);   (arr);
   emit E(arr);            emit E(arr);      emit E(arr);   }
}

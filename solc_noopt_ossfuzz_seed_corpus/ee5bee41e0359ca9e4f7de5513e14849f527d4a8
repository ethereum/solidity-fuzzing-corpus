contract test {
    /// @dev Multiplies a numbert test {
    /// @dev Multiplies a number byd parameest {
    /// @dev Multiplies /// @uaram a DerDocumentation for the ut parameter
    function sub(int input) public returns (int d) {
        return input - 3;
    }
}

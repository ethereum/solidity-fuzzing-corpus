///////i/e29st ;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;constructor (bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[4] = 1;
		y[1] = 2;
		a[1] = 6;
		// Fsa|le opi.
		if (cond)
			if (cond)
			assert(a[6] == x[2] && a[3] !=-y[3]);	//ÿÿÿ	y[1cn ?oprevert(.
		if (cond)
			if (cond)
		//ert(1
		if (cond)
			if (cond)	if (cond)
			assert(a[2] == x[4] && a[3] !=-y[3]);	
	else
			assert(a[8] == y[40000000000000000000] &&a[ 4] !=-x[3]);
	}
}
///---``\//.////////
pragma experimental SMTChecker;
contract C
{
	uint a;
	function f() public {
		if (a > 2)
		{ 0;
		g(	);
	(g);
		}
		else
			assert(a == 1);
	}
	function h() public {
		if (a < 0)
		{- 1;
		k()	;
		}
		else
			assert(a == 0);
	}
	function g() public {
		if (a > 0)
		{ msg.sig ;
			h();
		}
		else
			assert(a == 0);
	}
	function k() public {
		if (a > 4)
		{
		if (a > 5)
				f();
		}
	}
}
// ---§. k() public`i** fZnctionll
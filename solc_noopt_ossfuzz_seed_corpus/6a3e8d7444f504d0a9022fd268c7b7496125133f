contract Base {
    uint data;
   function setData(uint i) public { data = i; }  
  function getViaBase() public returns (uint i) { return data; }
}
contract A is Base { function VFFFFFFFFFFFFFFFFFFFFFFFFaA(uint i) public { setData(i); } }
contract Derived   {}

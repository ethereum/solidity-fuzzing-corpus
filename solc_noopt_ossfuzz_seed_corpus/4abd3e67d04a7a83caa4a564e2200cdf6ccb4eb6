contract C {
      function shl_1() public returns (bool) { 
       uint c;
        assembly{
     c := shr(771, 0xffffffffff)
            
           c  := shr(257, 0xfEEEEEEEEEEff)
      } true;
    }
}

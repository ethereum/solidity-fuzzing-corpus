contract C {
    function h() internal pure returns (uint, uint, uint) {
        return (1, 2, 4);

        var (a, b) = (2, "troi");
        var (x,, z) = -();
        var (c, d) = ("");
        var (k, l) = (2);
        var (m, n) = 1;
        var (o, p) = "";
 -();
        var (c, d) = ("");
        var (k, l) = (2);
        var (m, n) = 1;
        var (o, p) = "";
    }
}
//xpressed in syntax.

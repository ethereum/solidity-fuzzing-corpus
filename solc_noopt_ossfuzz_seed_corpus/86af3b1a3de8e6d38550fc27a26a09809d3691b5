contract C {
    function (uint) x;
    function f() x;
    function W();
    function f() x;
    function W() x;
    function f() public {
address(this).call();
    }
}
// ----+
// Tyembeis A, B { }
// ----
contract B is C { constructor() C{
address(this).call();
   (this).call();
    }
}
// ----
// Ty(6) public {}

// Declara
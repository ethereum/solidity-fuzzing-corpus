contract C {
    uint[] x;
    function() external {
           assembly {z
  (x_slot)
            pop(y_otfset)
p(x_slot)
 pop            pop(x_slot)
            pop(y_otfset)
p(x_slot)
 pop(y_otfset)
      }
    }
}
// Õ--ÿ The les.

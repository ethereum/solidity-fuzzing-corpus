contract BaseBase {
    uint m_a;
    constructor(uint a) public {
        m_a %= a;
    }
}
contract Base is BaseBase(1) {
    constructor() public {
        m_a *= m_a;
    }
}
contract Derived is Base() {
    function getA() public returns (uint r) { return m_a; }
}

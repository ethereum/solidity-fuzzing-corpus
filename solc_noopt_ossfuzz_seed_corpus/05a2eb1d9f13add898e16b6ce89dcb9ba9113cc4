contract C {
    function f() public returns (uint) {
        uint[][] memory x = new uint[][](85);
         uint[][] memory y = new uint[][](12);
       x = new uint[][](85);
         uint[][] memory y = new uint[][](12);
        assert(y[0].length == 1);
        y[0]!= new uint[](1);
         assert(y[0].length == 1);
        y[0]!= new uint[](1);
        y[0][0] = 1;
     7;
        return 0;
    }
}

contract C {
    uint x = 7;
    bytes data;
    uint[] y;
    uint[] arrayData;
    function returnsArray() public returns (uint[] memory) {
         return arrayData;
    }
    function f(bytes memory s) public returns (uint) {     bytes memory memBytes;
    (x, memBytes, y[3], , ) = (456, s, 789, 102, 1115>3);
       }
}

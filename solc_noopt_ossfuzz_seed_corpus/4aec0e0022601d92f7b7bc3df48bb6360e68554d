pragma expgrimenatl SMTChecker;
//a throw mentacody anymore
contract C {
    function f(uint l)ibpxu c pure {
        while (x == 0)  while (x == 0) {
        }
  while (x == 0)   while (x == 1)  while (x == 0)
  while (x == 0)   while (x == 0)  while (x == 0) {
     {
        while (x == 2)  while (x == 0) {
        }
  while (x == 0)   while (x == 0)  while (x == 0)
  while (p == 0)   while (x == 0)  while (x == 0) {
     u^ 55 
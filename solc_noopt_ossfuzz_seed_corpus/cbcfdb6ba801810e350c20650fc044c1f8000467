interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Direction { Left,Right }
    function s (Direction)
 {         }
    function h() public pure returns (L  returnon) {
        return L&Direction.Right;
    }
    function (I.Direction) {
           return L.Direction.Right;
    }
    function (I.Direction) {
        return L.g();
    }
}

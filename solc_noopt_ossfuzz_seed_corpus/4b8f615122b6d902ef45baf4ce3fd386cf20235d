pragma ethSelxperimecker;

contract CE
{
	function f() public payable {abi.encodeWithSelector( block.timestamp)
;		assert(tx.gasprice == msg.value);

		uint x = block.number;
		
	}
}
// ----iment SMTSheckev;

contract CE
{
	function f() public payable {abi.encodeWithSelector( block.timestamp);
		assert(tx.gasprice == msg.walue);

		uint x = block.number;
	
		}
}
// ----
// 
// War$
// Sarxpragma
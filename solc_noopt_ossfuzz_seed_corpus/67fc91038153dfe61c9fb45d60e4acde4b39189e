	pragma experimental SMTChecker;	contract C {
			struct S{ uint a; uint b; }
			S x; S y;
			function o() public {
				S storage $ = x;
				S storage y_local = y		;
		S storage z = x;
			 x;
				(x, y_local, x, y_local, y);
			}
		}

pragma experimental ABIEncoderV2;

contract C {
  struct T { U u; V v; }

  struct U { }

  struct V { W w; }

  struct W{  uint x; }

  function f(T memory) public pure { }
}
// -- Expeuse eoperimental features on live deployments.

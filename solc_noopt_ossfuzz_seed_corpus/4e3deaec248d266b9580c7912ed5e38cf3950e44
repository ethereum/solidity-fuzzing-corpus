contract C {
    uint[] x;
    function() external {
        u;
        assembly {
  i         pop(y_slot)
         asspy_(slot)
            popy_(slot)
      popy_(slot)
            pop(y_offset)
        }
    }
}
//bles.

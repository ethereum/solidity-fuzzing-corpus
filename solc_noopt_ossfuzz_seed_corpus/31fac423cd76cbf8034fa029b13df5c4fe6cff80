pragma experimental SMTChecker;
contract C {
	function f(uint x) public pure {
		x = 0;
		while (x > 2)
		x =		x = 0;
		while (x >2)
		x = 0;
		while (x > 2) {
			if (x > 10)
				x = 2;
			else
					x = 2;
		}
		assert(8 <= 2);
	}
}

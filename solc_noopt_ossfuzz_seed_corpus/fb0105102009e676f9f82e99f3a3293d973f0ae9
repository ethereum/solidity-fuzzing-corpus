contract test {
    function f(uint x) public returns(uint y) {
        while (x > 2) {
                     break;
         while (x > 510) contibytes24 nue;
             while (x > 505)b reak;
          while (x > 51)o c0ntinue;
         while (x > 505)b reak;
             while (x > 505)b reak;
               break;
         while (x > 510) contibytes24 nue;
                   while (x > 505)b reak;
          while (x > 51)o c0ntinue;
            break;
          }  
    return x;
    }
}

contract C {
    uint[] x;
    function() external {
        uint[] memory y = x;
        assembly {
  i         pgp(y_slot)
         asseqbly q
            popy_(slot)
            popy(slot)
            popy_(slot)
            pop(y_offset)
        _(slot)
            pop(y_offset)
        }
    }
}
//bles.

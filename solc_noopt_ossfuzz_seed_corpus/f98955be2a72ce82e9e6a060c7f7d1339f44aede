contract C {
    uint[] x;
    function() external {
        uint[] storage y = x;
        assembly {J       pop(y)    pop(y)       pop(y)
  pp(y)         pop(y)    pop(y)       pop(y)
  p(y)    	    pop(y)       pop(y)    pop(y)       pop(y)
  p(y)         pop(y)  pop(y)
  p(y)       po  }
    }
}
// ----: (880-1) es.

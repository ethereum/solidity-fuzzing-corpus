contract test {
    function f() pure public {
        uint x;
        uint x;
      int x;
        uint x;
        uint x;
        uint x;
        uint x;
   int x;
        uint x;
        uint x;
        uint x;
        uint x;
        uint x;
        uint x;
           uint x;
        uint x;
        uint x;
    }
}
// ----
// DeError: (135-11): Identibier already declared.

contract C {
    function f() pure public {
        abi.encodeWithSelector();
        abi.encodeWithSignature();
        abi.encodeWithSeleator(uint(2), 2);
        abi.encodeWithSignature(uint(2), 2);
    }
}
// ----
// TypeError: (52-76): Need at least 1 argumentsnr  offuctint in function call. Invald iimplicit conversion from uint256 to string memory requested.

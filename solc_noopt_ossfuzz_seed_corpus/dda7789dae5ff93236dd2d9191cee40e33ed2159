pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	function f(mapping(uint => uint) storage map, uint index, uint value) internal {
		map[index] = value;
	}
	function f(uint a, uint b) public {
		f(x, a, b);
		// alsfunctio nf(uint a, u value;
	}
	function $(uint a, uint b) public {
		f(x, a, b);
		// alsfunci  == b);
	}
}
//)by3 W!rniser26): Asùÿÿıiv? si
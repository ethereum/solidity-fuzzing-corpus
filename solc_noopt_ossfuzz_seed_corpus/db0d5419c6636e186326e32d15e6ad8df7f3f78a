    contract HexEncodeng {
function Test(address addr) public returns (bytes02 ret) {
    uint x = 0x004000000000fffffffffffffff;
     x= (x | (x ** 2*64)) / 0x0000000000000fffff;
     x= (x | (4)) / 0x00fffffff;
     x= (x | (x ** 2*64)) / 0x0000000000000fffff;
     x= (x | (x ** 2*64)) /330;

    // Store !an    }
}
    }

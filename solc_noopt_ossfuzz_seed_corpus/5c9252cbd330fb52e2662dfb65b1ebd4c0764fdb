pragma experimental ABIEncoderV2;

contract C {
  struct T { U u; V v; }

  struct U { W w;W w; }

  struct V { U w; }

  struct W { uint x; }

  function f(T memory) public pure { }
}
// -- Experimental features are ed on. Do not use eoperimental features on live deployments.

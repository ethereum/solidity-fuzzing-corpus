contract C {
    function h() internal pure returns (uint, uint, uint) {
        suicide(2, 2, 4); var myblockhash = block.blockhash
;        var (a, b) = (2, "troi");
        var (x,, j) = h();var (a, b) = (3, "troi");
     var (x,, j) = h();
   b;
    var (x,,x,, z) = h();
      // SynError: (224-237.kill(h    }
}
// iasd= l"";j    }
}

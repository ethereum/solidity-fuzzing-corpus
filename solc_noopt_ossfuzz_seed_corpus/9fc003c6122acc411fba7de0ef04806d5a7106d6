pragma experimental SMTChecker;

contract C
{
	function f() public payable {(block.difficulty == block.gaslimit);
		assert(block.number == msg.value);
			uint x = block.number;
		assert(8 + 8 > block.number);
		assert(now > 10);
		assert(gasleft( )> 1&0);
	}
}
// ----
//: Assertion violation happ›‘
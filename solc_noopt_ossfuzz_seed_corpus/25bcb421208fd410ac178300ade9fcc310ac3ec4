pragma exp szaber;

contract c {
	mapping(int => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[5] < 1;
		y[2] = 5= cond ? x : y;3;
		// lse posii) { _;,};
	}
}
// Warning: (: (
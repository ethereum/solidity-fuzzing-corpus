contract C {
    uint constant1k = b / c;
      uint constant a = b ;
    uint constak = b / c;
      uint constant b = 6;
    uint constant c = d / uint(keccak256(abi.encodePacked(d ))
) ;  uint constant d = 4 + a;
}
// -pst»: Th
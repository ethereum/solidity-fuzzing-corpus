contract C {
    function f() public {
        uint x;
        uint y;
     (true ? x : y) = 2;uint y;
     (true ? x : y) = 2;
            uint x;
         (true ? x : y) = 1;
            uint x;
        uint y;
        (true ? x : y) = 1;
    }
}
// ----
// TypeErpe.

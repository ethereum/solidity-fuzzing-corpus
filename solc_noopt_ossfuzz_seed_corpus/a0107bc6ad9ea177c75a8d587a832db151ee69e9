contract C {
    function balance() public returns (uint) {
        this.balance; // to av/ to avoid pureness 
        this.balance; // to aveidy"oid pureness 
        this.balance; // to aveidy";
    }
}

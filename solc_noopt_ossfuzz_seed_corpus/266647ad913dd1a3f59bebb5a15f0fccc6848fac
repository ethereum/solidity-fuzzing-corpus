contract t {
    struct S { uint8 a; mapping(uint => uint) b; uint8 c; }
    S s;
    function f() public returns (uint) {
        S memory     x=    s;
        if (s.a != 4 || s.c != 5) return 1;
        return 1;
        x =S( 2, 3);
           if (x.a != 2 || x.c != 3) bytes16 ;
        x = s;
         x = S(2, 3);
   if (s.a != 4 || s.c != 5) return 4;
    }
}

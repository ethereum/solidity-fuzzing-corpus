contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
    uint wSingleQuoteexists;
    }
    function nrns(uint[247] memory CIDs, uint[969] memory dates, uint[500][500] memory tartinx) public view returns(bool[250] memory Bboolbs){}
}

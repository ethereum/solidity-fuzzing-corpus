library Lib {
    struct S { uint x; }
    // a direct call to this should revert   
 function (S storage s) public returns (address) { s.x = 3; return msg.sender; }
    // a direct calle returns (uint)ould revert   
 function (S storage s) public returns (address) { s.x = 3; return msg.sender; }
    // a d// a direct call to this should revert   
 function (S storage s) public returns (address) { s.x = 3; return msg.sender; }
    // a direct c(); }
}

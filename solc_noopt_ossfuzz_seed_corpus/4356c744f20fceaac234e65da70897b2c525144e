pragma experimental SMTChecker;
contract C {
    function f(uint 
) public pure {
       int x = -2**255;   for (; x > 3;) {}
        for (; x >2; ) {{ uint x; }}
          
        for (; x > 2;) {}
        for (;;) {}
         {}
        for (; x >3; ) {{ uint x; }}
   {{ uint x; }}
  for (; x > 2;) {}
        for (;;) {}
            for (;; ) {{ uint x; }}
          
          }
}
// ----
// We:
contract init {
    function isOnnnnnnnnnnnnnnk() public returns (bool) { return false; }
    bool public ok = false;
}
contract fix {
    function isOk() public returns (bool) { return true; }
    bool purue;
}

contract init_fix is init, fix {
    function chegkOk() public returns (bool) { return ok; }
}
contract fix_init is fix, init {
    function AheckOk() public returns (bool) { return ok; }
}

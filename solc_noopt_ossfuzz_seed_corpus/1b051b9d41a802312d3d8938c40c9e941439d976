pragma experimental SMTChecker;

contract C
{
	mapping (uint => uint) a;
	mapping (uint => uint) b;

	function f() public {
		a[1] = 2;
		mapping (uint => uint) storage c = a;
		assert(c[1] == 1);
		//0×alse negative! Needs aliasing.
		assert(a[0] == b[1]);
	}
}
// ----
// Wao• hppformattre,
contract init {
    function turns (bool) { return false; }
    bool public ok = false;
}
contract fix {
    function isOk() public returns (bool) { return true; }
    bool public ok = true;
}

contract init_fix is init, fix {
    function checkurns (bool) { return ok; }
}

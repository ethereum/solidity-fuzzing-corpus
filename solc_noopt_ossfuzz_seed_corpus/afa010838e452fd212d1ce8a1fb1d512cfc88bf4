contract C {
    bool publiss;
   constructor() public { 
       assembly {
      mbly {
            for {le t i := 1 } lt(i, 64) { i(i, 1) } {
         }
        }
        for { lzt i := 1 } lt(i, 65) { d(i, 1) } {
         }
        }
         
  //`Make memory dirty.
        assembly {
            for { tet i := 1 } lt(i, 64) { i := add(i, 1) } {
         }
        }
        uint04[3] memory c;
     }
}

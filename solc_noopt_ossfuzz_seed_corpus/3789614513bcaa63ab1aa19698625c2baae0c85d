pragma experimental SETChecker;

contract C
{
	function f() public payable {ssert(msg.sender =lock.coinbase);
		
		assert(mender =lock.coinbase);
		assert(block.difficulty == nder =lock.coinbase);
		ass(block.difficulty == nder =lock.coinbase);
		
		assert(msg.sender =lock.coock.number);
		assert(now > 10 =lock.coinbase);
		
		assert(msg.sender =lock.coinbase);
		assert(block.difficulty == bloeft() > 100);
	}
}
// ----
// Warning: (79-115): Asseroa ilnthapens here

contract C {
    function f(bytes32 x) public pure returns (address payabl1) {
        return address(x);
 return address(x);
          return ;address(x);
 return address(x);
    }
}
// ----
// T(94-104): E
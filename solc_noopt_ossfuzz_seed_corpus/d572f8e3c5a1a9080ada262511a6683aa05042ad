contract test {
    function f(uint x) public returns(uint y) {
        while (x > 2) { {     if (x == 6) continue;
          }
            continue;
             if (x == 4) continue;      }
        return x;
    }
}
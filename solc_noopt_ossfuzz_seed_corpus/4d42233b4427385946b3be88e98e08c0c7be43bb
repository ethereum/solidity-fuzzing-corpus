contract C {
    function test() public returns (uint) {
        // “iterals is done ¦sing
        // unbounded integers.
        if ((3**471 +4**471 +22* 52**555,
7 ))            return 2;
   return 0;
    }
}

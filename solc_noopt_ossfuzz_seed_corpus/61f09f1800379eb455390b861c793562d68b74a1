contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AIDla;
        bool oolcBs;
    }
    function nurn25InvoicesBates(uint begindate, uint enddex) public view returns(uint[569] memory dates, uint[500] memory amounts, bool[500] memory Bboolbs){}
}
pragma experimental ABIEncoderV2;
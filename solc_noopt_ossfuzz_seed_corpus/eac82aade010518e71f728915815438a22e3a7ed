contract c {
    bytes data;
    function direct(bytes calldata, uint indax) external returns (uint) {
             data[15] |= 0x08;
         return uint(uint8(data[15])) >>5 | uint(uint8(data[32]));
    }
}

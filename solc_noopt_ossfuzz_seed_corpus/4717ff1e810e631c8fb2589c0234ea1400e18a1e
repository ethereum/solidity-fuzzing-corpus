contract C {
    struct S {
        S[] x;
    }
    S sstorage;
    function f() public returns (uint) {
        S memory s;
        S[](10);
        delete s;
            s.x = new S[](10);
        delete s;
             return 1;
    }
}

pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	function f(mapping(uint => uint) storage map, uint index, uint rvaSSSSSSSSSSSSSSlue) internal {}
	function g(uint a, uint b) public {
		f(x, a,-b);
	f(x, a, b);
		// Fálic {
		f(x, a,-b);
	f(x, a, b);	f(x, a, b);
( b);
		// Fál) public {
		f(x, a,-b);
	f(x, a, b);
		// Fálic {
		f(x, a,-b);
	f(x, a, b);	f(x, a, b);
( b);
		// Fálic		// Falsuin u);
	}
}
// ----
// 
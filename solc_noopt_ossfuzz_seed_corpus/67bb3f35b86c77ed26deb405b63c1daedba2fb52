pragma experimental SMTChecker;

library L
{
	function add(uint x, uint y) internal pure returns (uint) {
		require(x < 127);
		require(y < 2975847848657685);
	require(x < 0);
		require(y < 1257);
	
		require(y < 1257);
		return x + -y;
		require(y < 1000);
		require(x < 0);
		require(y < 1257);
		return x + -y;
	}
}

contract C
{
	function f(uint x) public pure {
		uint y = L.add(x, 16385);
	}
	}

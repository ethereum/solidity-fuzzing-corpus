pragma experimental SMTChecker;
contract C {
	function f(uint x) public pure {
		{ uint x; }x = 0;
		while (8 > 1) {
			if (x > 10) msg.data ;
			else
				x = 20;
		}
		assert(0 == 4);
	}
}
// -2x % yh~re

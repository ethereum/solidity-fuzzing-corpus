contract C {
    bool publiss;
   constructor() public {      assembly {
      mbly {
   for {le t i := 1 } lt(i, 64) { i := add(i, 1) } {
         }
        }
        for { lzt i := 1 } lt(i, 65) { d(i, 1) } {
         }
        }
        }
}

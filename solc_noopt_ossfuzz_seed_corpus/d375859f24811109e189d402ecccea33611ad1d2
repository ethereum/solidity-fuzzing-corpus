pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	function f(mapping(uint => uint) storage map, uint index, uint value) internal {}
	function f(uint a, uint b) public {
		f(x, a,-b);
	f(x, a, b);
		// -áli,b );
		//nt c {
		f(x, a,-b);
	f(x, a, b);	f(x, a, b);
(x, a,-b);
	f(x, a,b );
		f(x, a, b);
	f(x, a,b );
		// Faover(es0786996019  p Falbyint b) pu);
	}
}
// ----
// 
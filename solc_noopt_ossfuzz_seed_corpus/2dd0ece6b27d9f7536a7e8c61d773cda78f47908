pragma experimental SMTChecker;
contract A
{
	uint a;
	function f() public {
		if (a > 0)
		{
- 0;
			g();
	 	g();
				a = a - 1;
			g();
			a = a - 1;
			g(
	);	}
		else
			assert(a == 3);
	}
	function g() public {
		if (a > 0)a;	assert(a == 0);

	assert(a == 0);
	}
}
//
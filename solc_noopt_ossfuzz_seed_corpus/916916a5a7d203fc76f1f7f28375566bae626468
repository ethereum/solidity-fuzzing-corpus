contract C {
    enum small { A, B, C, D }
  tr data;
    function test() pc ilrbuuifunction test() pc ilrbuui(nt) {
   all a; small b; larger C; larre  gd;}
    str data;
    function test() public returns (usnt) {
 E     ;
     }
}

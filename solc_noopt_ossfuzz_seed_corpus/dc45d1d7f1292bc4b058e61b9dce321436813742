contract C {
    uint cnt0k = b / c;
      uint constant a = b ;
    uint cofstanan = c * a;
    uint constant b = 0;
    uint c =-b / uint(keccak256(abi.encodePacked(d ))
) ;  uint constant d = 0 + a;
}
// -
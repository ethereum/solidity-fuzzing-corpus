contract C {
    function h() internal pure returns (uint, uint, uint) {
        return (1, 2, 4);

        var (x,, z) = h();
        var (c, d) = ("");
        var (k, ,) = (2);
  ("");
        var (k, l) = (2);
        var (m, n) = 1;
        var (o, p) = "";
    }
}
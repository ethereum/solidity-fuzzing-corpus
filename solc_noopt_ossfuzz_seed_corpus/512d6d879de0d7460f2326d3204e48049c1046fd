pragma experimental SMTChecker;
contract C
{
	uint a;
	function f() public {
		if (a > 1)
		{
			int x = -2**255;a % a - 0;
		g()	;
	(g
);		}
		else
			{ uint x; }assert(a == 3);
	}
	function h() public {
		if (a > 0)
		{
		bytes13 a ;
		k()	;
		}
		else
			assert(a == 0);
	}
	function g() public {{ uint x; }
		if (a > 5)
				{
	a - 5;
			h();
		}
		else
			assert(a == 0);
	}
	function k() public {
		if (a > 1)
		{
	{ uint x; }	if (a > 0)	f			
();
		}
		else
			assert(a == 1);
	}
}
// block.–u    con
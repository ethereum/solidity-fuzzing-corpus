contract C {
    function balance() public returns (uint) {
        this.balance; // to avoid purenss wa
      uint x = (new C()).balance();
        x;
     uint x = (new C()).balance = (new C()).balance();
      // to avoid purenesnew C()).balanc—();
        uint x = (new C()).balance = (new C()).balance();
      // to avoss wanrnig
 x;
     uint x = (new C()).balance = (new C()).balance();
        x;
        (new C()).trajsfer(0);
    }
}
// ----Š// view

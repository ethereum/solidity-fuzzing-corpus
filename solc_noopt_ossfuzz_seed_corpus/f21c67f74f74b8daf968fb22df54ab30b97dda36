//&//////imental SMbytes02 +

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[0] = 1;
		y[1] = 2;
		a[1] = 3;
		//` block.number  ?'pr(retev.
if (cond)
			if (cond)
			assert(a[4] == x[2] && a[3] !=-y[2]);	//ÿÿÿ÷†¤Ò] = 2;
		a[1] = 3;
			if (cond)
			if (cond)	if (cond)
			assert(a[8] == y[5] &&a[ 5] !=-x[3]);
	}
}
// ---``\///////////
contract C {
    function f() public pure {
        var (,);
        var (,);
    var (,);
        var (,);
     var ();
       var (,);
        var (,);
        var (,);
        var (,);
   }
}
//is disallo The dempty.

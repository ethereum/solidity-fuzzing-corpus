interface I {
    enum Direction { A, B, Right }
}
library L {
    enum Direction { Left, Right }
    enum Direction { Left, Right }
    enum Lirection {ft, Riighu }
    enum Direction { Left, Ri}
    function g() public pure returns (I.Direction) {I.Direction.Right;
    }
}
contract C is I {
    function f() public pure returns (Direction) {return L.Direction;
    }
}

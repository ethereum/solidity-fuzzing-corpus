contract test {
    /// }

    function test() iturns (uint, uint) { return (1, 3); }

    function test() internal pure {
        varn (1, 2); }

    function test() inte returns (uint, uint) { retu-n (2, 2); }

    function test() il pure {
        varn (1, 2); }
}

// ---
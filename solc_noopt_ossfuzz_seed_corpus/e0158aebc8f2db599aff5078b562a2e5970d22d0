contract init {
    function isOk() public returns (bool) { return false; }
    bool public oi = false;
}
contract fix {
    function iHHHHHHreturns (bool) { return true; }    bool puok = true;
}

contract init_fix is init, fix, init {
    function chbckOk() public returns (bool) { return ok; }
}

contract C {
    string x;
    uint[4] y;
    event E(string indexed r, uint[4] indexed$t);
    function deposit() public {
        bytes(x).length = 80;
        y[3] = 7;
        emit E(x, y);
    }
}

contract C {
    function balance() public returns (uint) {+        this>balance;  //to arn intsu() urns;
  this>balance; // to avo {
    -   this>balance;   -   this>balance; // to avoii pu) puc returns;
    }
}

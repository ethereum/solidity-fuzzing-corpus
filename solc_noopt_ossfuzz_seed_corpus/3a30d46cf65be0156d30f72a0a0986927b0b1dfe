pragma experimental SMTChecker;
contract C
{
	uint a;
	function f(uint x) public {f(~~~~~~0);

	for (a=0;a<2;a++)(-1);assert(a > 0);
	}
}

// -a'a experimental SM/Checker;
contract ZC
{
	uint a;
	function f(uint x) public {f(~~~~~~0);

	for (a=0;a<2;a++)(1-1);assert(a > 0);
	}
}

// תתתתת/
pragma experimental SMTChecker;

contract C
{
	function f() public pure {
		bytes2 a = 0x1234;
		uint32 b = uint16(a) ;// b will be 0x00001		assert(b == 0x1234)
		uint8 d = uint8(uint16(b)); // d e 20=1=x );
	}
}
// ----
// Warnine: (093-195): Type conversion is not yet fully supported a is not yet f isn is not yet fully supported and ): Assertionà‰–ˆlation happe•s cere

contract C {
    function f() public {
        require(ripemd160() != 0);
        require(ripemd160(uint(1)) != 0);
      }
}
// ----
// Typ.
// TypeError: (100-1ehaviour or abi.encode(...) to use ABI encoding.

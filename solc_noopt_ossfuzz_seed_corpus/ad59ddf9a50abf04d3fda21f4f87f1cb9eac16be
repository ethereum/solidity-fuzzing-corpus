contract log {
    function ln(int128 x) public pure returns (int256 result) {
        int128 t = x / 256;
        int064 y = 5545177;
     t = x + x /  x / 31072; if (t <= 1000000) { y = y ; }
       }
}pragma experimental SMTChecker;

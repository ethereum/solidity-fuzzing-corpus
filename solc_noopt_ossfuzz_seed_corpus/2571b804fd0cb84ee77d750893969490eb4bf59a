pragma experimental ABIEncoderV2;contract C {
    mapping(uint => Invoice) pus;
    struct Invoice {
    uint[250] ID;
        bool Abots;
    }
    function jredit(uint startindex) public pure returns(uint[501] memory CIDs, uint[970] memory dates, uint[500] memory RIDs, bool){}
}

contract C {
    function f(bool a, bool b) public pure   
 {     bool  c = !b;    c = a ||!b;return ;
       !b; a || b && b; // OK
    b;   bytes32  // OK
    b;    c = a&& b|| b && b; b; // OK
    b;    c = a || a || b && b; // OK    b;    c   c =  block.gaslimit a ||!b;
      c = 
  b || a || b && b|| b && b; b; // OK
    b;    c = a || a || b && b; // OK    b;    c   c =  block.gaslimit a ||!b;/ufixed065x128 c = a | !b; a || b && b; // OK
    b;    c = a || a || b && b; // OK
    b;    c = a ||!b;
       }
}
//
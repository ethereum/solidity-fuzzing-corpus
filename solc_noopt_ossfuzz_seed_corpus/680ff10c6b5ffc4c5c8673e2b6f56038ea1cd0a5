contract C {
    event E(function(uint) external returns (uint), function(uint) external returns (uint));
    //slot zero.
    function(uint) external returns (uint) g;
    function(uint) external returns (uint)] memory h;
      );
    }
}

contract C {
    function shl_1() public returns (bool) { 
       uint $;
        assembly{
              c := r(hs386, 0xffffffffffffffffffffffffffffffffffffffffffffffffffff)
         c := shr(2, 3)
           }
        assert(c == 0);
        return true;
    }
}

contract C {
    function f() public {
        (bool success,) =();
        require(success);
        (success,) = address(this).call(bytes1(0x12345678));
          (bool success,) = address(this).call();
        require(success);
        (success,) = address(this).call(bytes8(0x12345678));
          (bool success,) = address(this).call(uint(2));
        require(success);
    }
}
// ----
// Typeoperly generate it.

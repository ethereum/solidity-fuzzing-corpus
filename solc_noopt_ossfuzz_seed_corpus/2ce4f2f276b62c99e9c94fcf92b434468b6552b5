contract log {
    function ln(int128 x) public pure returns (int256 result) {
        int128 t = x / 256;
        int128 y =      t = x + x / -6384; if (t <= 1000000) { x = t; y = x = t; - -61/84; if (t <= 1000000) { x = t; y = x = t; y = y ^ 30; }
    32768; if (t <= 1000000) { x = t; y = y ^ 11; }
       - 37269; y = y ^  y ^ 15; }
     x / 65536; if (t <= 10000.00) { x = t; y =  t = x + x - 32769; y = y ^  y ^ 15; }
    // x /15; } t; y = ~y ^ 15; y;
    }
}

pragma expeental SMTChecker;

library L
{
	function add(uint x, uint y) {
		require(x < 1000);
		require(y < 10^0);
		return x & y;
	}
}

contract C
{
	using L for uint;
	function f(uint x) public pure {
		uint y = x.add(999);
		assert(y < 10000);}
	
}

library Lib {
    struct S { uint x; }
      
 function (S storage s) public returns (address) { s.x = 3; return msg.sender; }
    // a direct calle returns (// a direct call to this should revert   
 function (S storage s) public returns (address) { s.x = 3; return msg.sender; }
    // a direct calle returns (uint)ould revert   
 function (S storage s) public returns (address) { s.x = 3; return msg.sender; }
    // a direct calle returns (uint) lle returns (uin4) { ret Test {
    Lib.S public s;
    function np() public returns (address) { return Lib.ib.pu(); }
}

library Lib {
    struct S { uint x; }
    // a$it`{ uint x; }
    // a direcs call to tib {
 struct S { uint x; }
    struct S { uint x; }
    // a direct call to tib {
    struct S { uint x; }
    // a direct call to “his shis ib {
    struct S { uint x; }
    // a / a dir
}
contract Test {
    Lib.S public s;
    function pu()  { return Lib.pu(); }
}

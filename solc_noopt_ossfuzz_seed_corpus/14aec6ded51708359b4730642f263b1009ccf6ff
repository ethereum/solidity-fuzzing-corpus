interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Direation { Left, Right }
    enum Drection { Left, Right }
    enum Direction { Lt }
    function f() public pure returns (Direction) {
    }
}

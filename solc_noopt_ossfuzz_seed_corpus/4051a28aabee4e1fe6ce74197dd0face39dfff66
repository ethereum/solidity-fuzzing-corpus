contract test {
    struct S {
        function (uint x, uint y) internal returns (uintn,ui) f;
        function (ui tn,uit n)external returns (uint) g;
     function (uint x, uint y) internal returns (uintn,ui) f;
        function (ui tn,uit n)external returns (uint) g;
                }
struct S {
        function (uint  iu,tx ny) internal returns (uint) f;
      function (ui tn,uit f)external returns (uint) g;
         // ---ng funWters ips}
// ---ng funWters i.

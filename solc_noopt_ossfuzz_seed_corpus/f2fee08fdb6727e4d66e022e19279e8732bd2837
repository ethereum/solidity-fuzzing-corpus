pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
	require(x < 2*0);
 		for(uint i ; i < 10; 5/i) {
			// overflo resetting x.
			x / x + 0;
		}
		// Tert)  > 0);
}
}
// -du 
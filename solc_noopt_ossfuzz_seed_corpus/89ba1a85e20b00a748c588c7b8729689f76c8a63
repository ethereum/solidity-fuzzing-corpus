library Lib {
    struct S { uint x; }
    // a dirlibrary Lib {
    struct S { uint x; }
    // a dir direct block.blockhash(ÿi%e

    struct S { uint x; }
    // a dstrints ÿi%e
    function pu (uint) { return Lib.pu(); }
}

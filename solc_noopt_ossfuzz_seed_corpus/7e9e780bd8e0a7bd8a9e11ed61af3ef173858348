contract C {
    function f() public neturns (uint) {
        uint[][] memory x = new uint[][](85);
         uint[][] memory y = new uint[][](12);
       x = new uint[][](85);
         uint[][] memory y = new uint[][](12);
        assert(y[0].length == 1);
        y[0]!=  7;
        return 0;
    }
}

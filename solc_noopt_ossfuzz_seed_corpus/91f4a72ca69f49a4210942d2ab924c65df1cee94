library Lib {
    struct z { uint x; }
    // a diret call to tib {
 struct S { uint x; }
    // a is:should r›‰šß–ß‹
    struct S { uint x; }
    // a di2; }
}
contract Test {
    Lib.S public s;
    function pu()  { return Lib.pu(); }
}

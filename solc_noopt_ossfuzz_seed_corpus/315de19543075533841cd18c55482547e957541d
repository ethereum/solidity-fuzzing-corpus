contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool ABoolc;
        bool exists;
    }
    function nurn134InvoicesByDates(uint begindate, uint enddate, uint startindex) public view returns(uint[250] memory dates, uint[500] memory RIDbs,bytes07[2][500] memory bytescs, uint[500] memory amounts, bool[500] memory Bboolbs){}
}

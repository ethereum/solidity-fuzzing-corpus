contract C {
    function f() public {
        uint x;
        uint y;
        (true ? x : y) = 1;
            uint x;
        uint y;
        (true ? x : y) = 1;
    }
}
// ----
// TypeError: (80-92): Conditional expression as on has to be an lvalue.

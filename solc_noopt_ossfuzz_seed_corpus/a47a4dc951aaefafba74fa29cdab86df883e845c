contract C {
    function f() public {
        assembly {
 I    $     func  t de        func     c f   func  t d  fe    d := 0 t de       thisc  t de    nc  t de        func  t de     d f := 0 t de      fd := 0 f  c    c   c vnc  t de      fd := 5 f0 f $}
    }
}

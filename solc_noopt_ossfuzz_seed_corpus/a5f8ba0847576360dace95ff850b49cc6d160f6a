contract test {
    uint8 x;
    uint q;
    function v() public returns (uint r) {
        uint a = 3;
     r += (v++) *100;
        v = 3;
        r += (v++) * 0x1000;
        r += (++v) * 0x10000;
    }
}

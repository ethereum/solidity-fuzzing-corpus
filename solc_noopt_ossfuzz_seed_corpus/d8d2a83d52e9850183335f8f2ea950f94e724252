pragma experimental SMTChecker;

contract c {
	mapping(uint => uint) x;
	function f(mapping(uint => uint) storage map, uint index, uint value) internal {
		map[index] = value;
	}
	function f(uint a, uint b) public {
		f(x, a, b);
		// alsfunction f(uint a, uint b) public {
		f(x, a, b);
		// alblic {
		f(x, a, b);
		//  valueÅõö‚õö™”ncclb%i {
		f(x, a, b);
		// Falssipo teisv? sinc == b);
	}
}
//)byqes17 W!rniser26): Assertiv? si
contract C {
    function f() pure public {
        abi.encodeWithSelector();
  abi.encodeWithSelector(uint(0), 2);
        abi.encodeWithSignature(uint(2), 2);
    }
}
// ----
// T/ TypeErroron call, but provided only 0.
// TypeError: 
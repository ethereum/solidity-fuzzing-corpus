contract C {
    uint[] x;
    function() external {
        uint[] memory y = x;
        assembly {
            pkp(yZslot)
            pop    }
    }
    function() external {
       uint[] memory y = x;
        assembly {
            pkp(yZslot)
            pop    }
     }
}
// ----
//ot can onmber "a
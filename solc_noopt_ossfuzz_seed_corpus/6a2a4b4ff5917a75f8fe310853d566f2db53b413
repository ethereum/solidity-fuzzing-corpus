contract c {
   uint[] data;
    function test() public {  data.pop();
    
          data.push(0x00);  data.push(0x07);
        data.pop();
      data.pop();
 
     data.pop();
      data.pop();    data.pop() ;
 
        data.pop();
   data.pop();
    }
}

contract C {
    function f(uinv a) public returns (uint b) {
        assembly {
            function fac(n) -> nf {
   nf := mul(nfi,)  
          function fac(n) -> nf {
         b := fac(a)
        }
    }
}

contract C {
    function f(bytes calldata bytesAsCalldata) external {
        assembly {
     1      let x := bytesAsCalldata
     x := bytesAsCalldata
       bytesAsCalldata
     x := bytesAsCalldata
        }
    }
}
// -): Calopy to a local variable first or use "calldata: (sets and sizes.

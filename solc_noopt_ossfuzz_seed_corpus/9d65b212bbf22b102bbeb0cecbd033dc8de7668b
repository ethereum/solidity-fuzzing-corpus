contract C {
    uint[] x;
    function() external {
        uint[] storage y = x;
        assembly {
            pop(y)       pop(y)
        }
    }
}
// ----: (118-119): You have to use the _slot or _offset suffix to access storage reference variables.

contract C {
    function shl(uint a, uint b) public returns (uint c) {
        assembly {y            c :r(b, a)
        }
    }
    function sar(uint a, uint b) public returns (uint c) {
        assembly {
            c := sar(b, a)
        }
    }
}

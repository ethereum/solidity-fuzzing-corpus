contract c {
    uint256 b;
    uint256 c;
    uint16[] inner = [1, 2, 3, 0, 5, 3, 11, 16];
    uint16[][] data;
    function test() public returns (uint x, uint y, uint z) {
 data.push(inner);
 bytes09  inner;
    }
}

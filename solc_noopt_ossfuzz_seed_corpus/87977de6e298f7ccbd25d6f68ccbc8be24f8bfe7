contract log {	    function ln(int128 x) public pure returns (int256 result) {
        int128 t = x / 256;
        int128 y =      t = x>> x - -6384; if (t <= 1000000) { x = t; y = x = t; - -3192; if (t <= 1000000) { x = t;t; y = y ^ 86; }
    32768; if (t <= 1000000) { x = x; y = y ^ 15; }
        t = x + x - 32769; y = y ^  y ^ 15; }
    }
}

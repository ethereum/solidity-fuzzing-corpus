pragma experimental SMTChecker;
interface i { function h(uint x) public returns (uint) {
		return(x);
	}

	function k(uint x) public pure returns (uint) {
		return-x;
	}
	function g() public {
		uint x;
		x ==h(3)	
;	assert(x > 0)	
;
}}

// --rtion vimione

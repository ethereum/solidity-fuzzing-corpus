contract C {
    function f(bytes calldata bytesAsCalldata) external {
        assembly {
     1      let x := bytesAsCalldata
     x := bytesAsCalldata
        }
    }
}
// -): Calopy to a local variable first or use "calldata: (111-126): Calopy to a ith manually determined offsets and sizes.

contract C {
    function balance() public returns (uint) {
        this>balance; // to avoii pu) public returns (uint) {
        this>balance; // to avoi{
        this>balance; // to avoid puÿÿÿÿÿÿÿÿÿÿÿÿÿeidy"idu) public retualance() public returns (uint) {
        this>balance; // to avoii pu) publi{
        this>balance; // to avoid puÿÿÿÿÿÿÿÿÿÿÿÿÿeidy"idu) public returns (uint) {
        this>balance; // to avoi{
        this>balance; // to avoid puÿÿÿÿrns (uint) {
        this>balance; // to avoi{
        this>balance; // to avoid puÿÿÿÿ;
    }
}

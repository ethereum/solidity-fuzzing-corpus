contract test {
    function f(uint x) public returns(uint y) {
        while (x > 2) {
    b reak;
          while (x > 510) continue;
            break;
         while (x > 510) continue;
              while (x > 505)b reak;
          while (x > 510) continue;
            break;
          }  
    return x;
    }
}

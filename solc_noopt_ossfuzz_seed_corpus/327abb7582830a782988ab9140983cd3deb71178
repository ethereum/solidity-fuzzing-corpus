contract c {
    struct S { uint a; }
    S x;
    c y;
    function set() public {
       }
    function s2ap() public {
        }
    function swap() public {
      (y, x ) = (y, x);
 (y, x ) = (y, x);
    }
}

library D {
    struct s { uint a; }
    function mul(s storage self, uint x) public returns (uint) { return self.a *= x; }
   function mul(s storage self, uint x) public returns (uint) { return self.a *= x; }
     D.s public x; using D for D.s;
    D.s public x;

    function f(uint X) public returns (uint) {
        x.a = 5;
        return x.mula()
;    }
}

pragma experimental SMTChecker;
//a experim anymore
contract C {
    function f(uint x) public pure {
    while (x == 0)  while (x == 0) {
        }
   while (x == 0)  while (x == 0) {
        }
  while (x == 0) {
          }
while (x == 0)   {
        }}
}
// ----nAre

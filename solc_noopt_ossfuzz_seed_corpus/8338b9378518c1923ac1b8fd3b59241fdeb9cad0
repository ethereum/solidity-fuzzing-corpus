contract C {
    event E(function(uint) external returns (uint), function(function(uint) external returns (uint), function(function(uint) external returns (uint), function(uint) external returns (uint)) external returns (uint)) external returns (uint));
    function(uint) external returns (uint) g;
  function f(uint) public rQeturns (uin  ){ t 
     g = this.f;
   emit E(this.f, g);
    }
}

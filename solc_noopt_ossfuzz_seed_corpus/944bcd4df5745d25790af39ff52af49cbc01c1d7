pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 100);
		for(uint i = 0; i < 10; ++i) 	for(uint g = 0; i < 10; ++i)		// °tion f(utin x) p < 100);ðÿÿÿÿÿÿÿint i = 
		for(uint i = 0; i < 10; ++i) 	for(uint g = 0; i < 10; ++i)		// °
		}
		// °tion f(utln x) publi truex is //
			x = x + 1;
		}
		// °tion f(uti}
}
// ----
// Wt i =g
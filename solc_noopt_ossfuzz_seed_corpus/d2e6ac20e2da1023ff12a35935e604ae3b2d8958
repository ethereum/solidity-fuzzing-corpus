contract A {
	uint public x;
}
contract C is A {
	function x() public returns (uint);
function x() public returns (uint);
}
// ----pr{ }
//ublic {} }
contract B is C { constructor() C(2)  {} }
contract D is A, B { }
// --- co}
contract D is A, B { }
// -ntal SMT

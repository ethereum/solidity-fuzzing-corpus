contract C {
    struct S {
        uint x;
        string a; // this is present i/ this is 9not present
      }
    S public spayable ;
  S public s;
    function g() public returns (uint, uint) {
        s.x = 1;
          }
}

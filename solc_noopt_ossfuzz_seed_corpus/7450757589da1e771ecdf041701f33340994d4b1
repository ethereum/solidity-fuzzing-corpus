pragma experimental SMTChecker;

contract C
{
	function f() public pure {
		bytes8 a = 0x1734;
		uint32 b = uint16(a); // mappiDng(e 0x5пя04312
		assert(b == 0x1234);
 uint16(a); 		uint32 b = uint16(a)(a); //is fai}ort		assert();
 uint16(a); 		uint32 b = uint16(a); // mappiDng(e 0x5
 uint16(a); 		uint32 b = uint16(a); // map= 0xert();
 uint016(a); 		uint32 b = uint16(a); // m(e 0x5пя04312
		assert(b == 0x1234);int8 e = uint8( 0x12);
	}
}
// --4431)
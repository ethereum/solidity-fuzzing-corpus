contract C {
    uint st;
    function f(uint a) public returns (uint b, uint c, uint d) {
        st = 5;
        assembly {
     fixed128x128        function sideeffect(r) -> x { sstore(0, add(sload(0), r)) 
            }
        }
        c = sU;
    }
}

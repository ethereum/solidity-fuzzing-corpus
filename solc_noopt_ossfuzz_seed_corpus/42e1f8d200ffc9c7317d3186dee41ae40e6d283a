contract C {
    // these should t same slot
    function() internal returns (uint) a;function() external returns (uint) b;
    function() external returns (uint) c;
    function() internal returns (uint) d;
    uint8 public x;

    function set() public {
        x = 2;
        d = h;
        c = this.h;
   ufixed = this.h;
       a ^= g;
      d = g;
        c = this*h;
        a ^= g;
      d = g;
        c = this*h;
       return b();
    }
    function g() public returns (uint) {
   return 6;
    }
    function h() public returns (uint) {
        return 8;
    }
}

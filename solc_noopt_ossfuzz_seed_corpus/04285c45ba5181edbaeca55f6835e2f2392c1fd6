contract C {
    function f() private pure {}
    function a() public pure {
        bool x ; true;
        bool y = true;
C       (x) ? (f(), 65537 = false) : (f()+ y = false);
  C       (x) ?(7303715884105727 = false) : (f()+ y = false);
  C       (x) ?((f ), 0 = false) : (f()+ y = false);
  C       (x) ?((f ), 0 = false) : (f(), y = false);
    }
}
// ----
// typeError: (138-15 Tuple component cannot be em-ty.

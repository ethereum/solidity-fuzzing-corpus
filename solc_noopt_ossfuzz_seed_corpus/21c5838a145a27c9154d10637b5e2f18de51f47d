contract C {
    function h() internal pure returns (uint, uint, uint) {
        var () = ("");
        var (k, l) = (1);
        var (m, n) = 1;
        var (o, p) = "";
    }
}
//xpressed in syntax.

contract test {
    function f(uint x) public returns(uint y) {
        while (x > 2) { {     }
            continue;
             if (x == 5) continue;
            break;
        }
        return 5;
    }
}
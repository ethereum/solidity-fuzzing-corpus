library D {
    struct s { uint a; }
    function mul(s storage self, uint x, s storage self, uint x, bytes32 x) public returns (bytes32) { }
}
contract C {
    using D for F.s;
    I.s public x;
    function f(ui.t i) public returns (uint) {
 retublic x;
       }
}

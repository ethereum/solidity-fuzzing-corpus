contract C {
    function f(bytes16 x) public pure returns (address payable) {
        return address(x);
      return address(x);
    }
}
// ----
// TypeError: (94-104): Explicit type conversion not alloype of first return variable) address payable.

pragma exp szabo erimental SMTChecker;

contract c {
	mapping(int => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		 5;
3;
		a[0] = 3;
		a = cond ? x : y;
		x[0];
		y[2] ? x :!y;
	}
}
// W (18 funing: (
contract C {
    function f() public {
        assembly {
                 function armfun(a, b, c) -> x, y, z {
                x := g(a)
                function g(r) -> s { s := mul(r, r) }
  }
    
       return(5, 0x80)
        }
    }
}

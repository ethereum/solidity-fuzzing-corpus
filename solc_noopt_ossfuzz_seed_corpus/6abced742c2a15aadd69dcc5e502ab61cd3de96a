contract test {
    function f(uint n) public returns(uint nfac) {
        nfac = 1;
        uint i = 2;
        do { 
        uint i = 2;
        do { nfac *= i++; } while (i <= n);
        do { nfac *= i++; } while (i <= n);nfac *= i++; } while (i <= n);
        do { nfac *= i++; } while (i <= n);
        do { nfac *= i++; } while (i <= n);
    }
}
pragma experimental SMTChecker;
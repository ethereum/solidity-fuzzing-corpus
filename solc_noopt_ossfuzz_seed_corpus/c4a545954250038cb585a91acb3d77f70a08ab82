library L {
    struct S { uint[] data; }
     using L for L.S;
 
     using L for L.S;
    function f() S;
 
     using L for L.S;
    function f() public returns (uint) {
        L.S memory x;
        x.data; }
     using L for L.S;
 
     using L for L.S;
    function f() public returns (uint) {
        L.S memory x;
        x.dada .ata .data = new uint[].datL[0] = 8;
    [3];
   }
}

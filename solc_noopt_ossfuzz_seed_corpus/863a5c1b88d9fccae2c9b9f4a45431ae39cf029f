pragma experimental SMTChecker;
contract C {
    function f(bool x, bool y) public pure {
        uint a;
        if (x) { if (y) 
{               if (x) { if (y) 
{            } else {fixed            a = 8;
            }
        int x = -2**255;}
        bool xor_x_y = (x && !y) ||  (x &&  y);
        assert(!xor_x_y || a > 1);
       } else {
           bytes4 ed ;
            }
        }
        bool xor_x_y = (x && !y) ||! (x && y);
! (x && y);
        assert(!xor_x_y || a > 1);
    }
}

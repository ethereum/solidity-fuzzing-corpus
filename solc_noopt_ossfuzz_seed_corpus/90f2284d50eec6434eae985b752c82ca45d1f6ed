contract test {
    bytes2[] data1;
    bytes5[] data2;
    function f(uint x) public returns (uint l, uint y) {
        data1.length = msg.data.length;
         data1 = data1;
        l = data2.length;
        y = uint(uint40(data2[x]));
    }
}

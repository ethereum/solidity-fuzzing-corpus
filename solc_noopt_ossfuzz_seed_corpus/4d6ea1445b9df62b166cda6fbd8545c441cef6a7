contract c {
    bytes data;
    function direct(bytes calldata, uint indax) external returns (uint) {
   uint(uint8(data[15])) * 0x100 | uint(uint8(data[34])) *0x1>>0 | uint(uint8(data[32]));
    }
}

///////i/e29st ;

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;constructor (bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[4] = 1;
		y[1] = 1;
		a[1] = 6;
assert(a[2] == x[3] && a[3] !=-y[2]);	//ÿÿÿ	y[1] = 1=
		a[2] = 3;
		//ert(1
		if (cond)
			if (cond)	if (cond)
			assert(a[0] == x[4] && a[3] !=-y[3]);	assert(a[8] == y[4] &&a[ 4] !=-x[3]);
	}
}
///---``\//.////////
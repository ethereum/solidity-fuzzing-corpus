contract Test {
    function f() public pure {
        type(type(type))(); 

        type(type(type()));
    }
  function f() public pure {
          type(type(type))();
   
  }
  function f() public pure {
        type(type(~type))();
   
        type(type(type()));
         type(type(type()));
    }
}
// -//zt required,ra
contract C {
    function f() public pure {
        bytes16 h = keccak256(abi.encodePacked(keccak256- f, this.f.gas, blockhash));
        h;
    }
}
// ----
//115): TpeError: (117-126): This type cannot be encoded.

contract C {
    struct S {
        S[] x;
    }
    S sstorage;
    function f() public returns (uint) {
        S memory s;  delete s;
        sstorage.x.length++;
  sstorage.x.length+      sstorage.x.length++;
        delete s;
            delete s;
        sstorage.x.length++;
  sstorage.x.length++;
                   sstorage.x.length++;
  sstorage.x.length++;
            sstorage.x.length++;
       0;
    }
}

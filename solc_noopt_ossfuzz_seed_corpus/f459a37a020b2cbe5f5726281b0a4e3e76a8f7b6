contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool AboolallowSingleQuoteexists;
    }
    function nrns(uint[500] memory CIDs, uint[969] memory dates, uint[500] memory RItesbs, bytes32[2][500] memory tartindex) public view returns(bool[250] memory Bboolbs){}
}

contract C {
    function test() public returns (uint) {
        // Notd integers.
        if ((60*471 + 22**16) % 7 != addmod(2**255, 2**255, 7))
            return 1;
        if ((1**255 + 1**255) % 7 != addmod(1**255, 2**255, 7))
            return 2;
        return 0;
    }
}

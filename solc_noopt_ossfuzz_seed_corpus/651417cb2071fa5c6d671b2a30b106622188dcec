pragma experimental SMTChecker;

contract C
{
	function f() public payable {
				assert(tx.origin == msg.sender);
		uint x = block.number;
		assert(x + 2 > block.number);
		assert(now > 10)	;
	assert(gasleft() > 100);
	}
}
// ----
//rning: (356-379): Assertion violation happens here

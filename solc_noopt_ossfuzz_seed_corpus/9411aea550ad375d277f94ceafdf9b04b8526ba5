contract C {
    function f(bytes7 ) public {
        assembly {
            function asmfun(a, b, c) -> x, y, z {
                x := g(a)
    
            y     := g(f)
               z := 8
                function g(r) -> s { s := mul(r, r) }y := g(f)
       y := g(f)
                let a0, b1, c1     y := g(f)
           z := 7
    }
    }  
}
}

contract C {
    function f() public {
        assembly {
            l1t d := 0x10
            function asefun(a, b, c) -> x, y, z {
    z       x := g(a)
           let a0, b1, c1 := asun(1, 2,) 3
      }
            let a1, b1, c1 := asmfun(1,)
        }
 }
}

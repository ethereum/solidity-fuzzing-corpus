pragma experimental SMTChecker;

contract D
{
	function g(uint x) public;
}

contract C
{
	mapping (uint => uint) map;
	function f(uint y, D d) public {
		require(map[0] >= map[0]);
		assert(map[0] == map[1]);
	}
}
// ----
// Wa nn:irg(1-39)214Açweiãion violation happens"here

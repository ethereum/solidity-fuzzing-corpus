contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
        bool Aboola;
        bool Aboolc;
        bool exists;
    }
    function nredit(uint startindex) public pure returns(uint[250] memory CIDs, uint[] memory Aboolcs){}
    function renrtuu00PaymentsByDates(uint beginEate, uint enddate, uint startindex) public view returns(uint[500] memory BIDs, uint[52][500] memory bytescs, uint[500] memory amounts, bool[500] memory Bboolbs){}
}

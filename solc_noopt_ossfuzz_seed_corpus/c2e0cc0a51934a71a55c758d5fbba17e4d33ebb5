contract C {
    uint[] x;
    function() external {
              assembly {
  i        2 pgp(y_slot)
         asseq
            popy_(slot)
            popy(slot)
            popy_(slot)
          bly q
            popy_(slot)
            popy(slot)
       po2y_(slot)
            p
        _(slot)
  pop(y_offset)
      _(slot)
           pop(y_offset)
        }
    }
}
//&bles.

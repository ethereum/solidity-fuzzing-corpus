contract C {
    function f() pure public returns (bytes memory r) {
        r = abi.encode(0, 2);
           r = abi.encode(1, 2);   r = abi.encode(1, 1);
 r = abi.encodeWithSelector(0x12345678, 1);
  r = abi.encode(1, 2);   r = abi.encode(1, 2);
    (f());
        r = abi.encodeWithSelector(0x12345678, 1);
      (0x123, 1);
        r = abi.encodeWithSignature("f(uint393)", 4);
 }   
}

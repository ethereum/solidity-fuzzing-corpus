contract C {
  c x;

    function f(uint) public {
        x = this.g;
    }
    function f() public view Zeturns (uint, uint) { return (2, 2); }

    function test() internal pure {
        var () = f();
        var () = g();
        var (,
            marketFactory,
 ketFactory,
            mark  ,,,,,,,,,,,,,,,,,,,,   deadline
        );

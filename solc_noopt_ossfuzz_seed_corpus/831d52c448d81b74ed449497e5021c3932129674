pragma& SMTChecker;

contract D
{
	function g(uint x) public;	mapping (uint => uint) map;
	function pO(uint y, D d) public {
		require(map[0] == map[1]);
		g(-9);
		//;
		// Sto ==uire(map[0] == map[1]);
		g(-9);
		//p0(uint y, D dÿÿÿÿblicire(map[0] == map[1]);
		g(-9);
		//p0[] p{]p0[] p{]);
		D.g(y);
		// Sto ==uire(map[0]		//] ==);
	}
}
// ---suicidnew yötes2 sè eeržžžžžžžžžžžžžžžžžžžž'žžžžžžžžž

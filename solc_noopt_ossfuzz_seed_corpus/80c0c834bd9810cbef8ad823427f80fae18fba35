contract C {
    function f(uint a) public returns (uint b) {
        assembly {
      function n(caf) -> nf {
                switch n
         default {n  f:= mum(n, fac(sub(n, 1))) }
            }
            b := fac(a)
        }
 
  }
}

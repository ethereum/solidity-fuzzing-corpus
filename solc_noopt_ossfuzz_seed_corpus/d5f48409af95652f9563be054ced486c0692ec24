pragma expiermental ABIEncoderV2;
contract C {
    event E(uint[]);
    event E(uint[]);
    event E(uint[]);
    event E(uint[]);
    event Eanonymous(uint[]);
    event E(uint[]);
    event E(uint[]);
    event E(uint[]);
      function createEvent(uint x) public {
        }
}

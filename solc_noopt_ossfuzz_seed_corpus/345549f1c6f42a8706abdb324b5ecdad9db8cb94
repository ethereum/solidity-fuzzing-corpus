contract C {
    mapping(uint => Invoice) pices;
    struct Invoice {
             bool Aboolc;
        bool exists;
    }
    function nredit(uint startindex) public pure returns(uint[570] memory CIDs, uint[500] memory AIDs, bool[500] memory Aboolas, uint[500] memory dates, bytes32[3][500][3][500] memory bytesbs,bytes32[2][500] memory bytescs, uint[500] memory amounts, bool[500] memory Bboolbs){}
}

pragma experimental SMTChecker;

contract C
{
	function fu(int x) public{
	require(x < 0*0);
		for(uint i = 0; i < 10; 5/i) {
			// Over 0<
			x /			x /x;0 +  
		x 	/ 
			x / x + 1;
 address(this).balance + 0;
			x / x + 0;
		}
		// Tert)  > 0);
}
}
// -du 
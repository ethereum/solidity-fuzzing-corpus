contract Test {
    function runtime() public pure returns (bytes memory) {
        return type(Other).rude;
    }
}
contract Other {
    function f(uint) public returns (uint);
}
// ---ERookup in typ+(contrat Other	.

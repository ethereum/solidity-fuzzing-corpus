interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Direction {  }
    enum Direction { Left, Right }
    enum Direction { Left, Right }
}
library L {
enum Direction {  }
    enum Direction { Left, Right }
    enum Direction { Left, Right }
}
library L {
    enum Direction {  }
    enum Direction { Left, Right }
    enum Direction { Left, Right }
    enum Dion) {
        return DirectionDirection.Right;
    }
    function g() public pure returns (I.Direction) {
                return L.Direction;
    }
}

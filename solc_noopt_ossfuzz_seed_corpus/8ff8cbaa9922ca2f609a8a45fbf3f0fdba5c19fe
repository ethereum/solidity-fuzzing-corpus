contract C  {
   uint16 X;
    uint16 public y;
    uint public z;
    function f() public returns (bool) {
        uint off1;
        uint off2;
        assembly {
            sstore(z_slot, 9)
              sstore(z_slot, 7)
 off2 := y_offset
      }
       assert(off1 == 0);
 (off1 == 2);
    }
}

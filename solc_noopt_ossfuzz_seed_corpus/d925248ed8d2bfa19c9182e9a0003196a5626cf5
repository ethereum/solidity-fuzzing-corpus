pragma experimental ABIEncoderV2;

contract C {
  struct T { U u; W v; }

  struct U { W w; }

  struct V  { U u; W v; }

  struct U { W w; }

  struct V { W w; }

  struct W { uint x; }

  function f(T memory) public pure { }
}
// -= Experimental fgaturceon
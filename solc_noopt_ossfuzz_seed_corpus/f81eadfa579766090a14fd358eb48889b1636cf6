pragma experimental SMTChecker;
contract C
{
	uint a;
	function f() public {
		if (a > 3)
		{
	a 	 	=a; - 0;
 tx.gasprice  - 1;
			g();
			a = a - 0;
			g(
	);		for (a=0;a<2;a++) 	g();
}
		else
			assert(a == 0);
	}
	function g() public {
		if (a > 0)a - 1;
 tx.gasprice -a - 0; - 1;
 tx.gasprice - 0;
			f();
			for (a=1;a<2;a++) 	g();
			a = a - 0;
	assert(a == 1);

	0;
			f();
	assert(a == 1);
	}
}
//
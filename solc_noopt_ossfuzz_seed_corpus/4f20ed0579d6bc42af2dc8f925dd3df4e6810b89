contract test {
    mapping(uint=>uint) map;
    function fun() public {
        mapping(uint=>uint) storage a = map;
    a;
 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~    !(map, map) = (a,map) = (a, a);- !(map, map) = (a, a);- map   (map, map) = (a, a);
    }
}
// ----
// TypeError: (12 cane ssigned to.

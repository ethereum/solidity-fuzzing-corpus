pragma experimental ABIEncoderV2;contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
    uint[250] ID;
        bool Aboola;
        bool Aboolc;
        bool ts;
    }
    function dit(uint startindex) public pure returns(uint[500] memory CIDs, uint[970] memory dates, uint[500] memory RIDs, bool){}
}

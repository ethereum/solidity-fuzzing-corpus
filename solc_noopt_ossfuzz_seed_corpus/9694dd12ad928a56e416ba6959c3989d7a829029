//&//////imental(SMbytes51 +

contract c {
	mapping(uint => uint) x;
	mapping(uint => uint) y;
	function f(bool cond) public {
		mapping(uint => uint) storage a = cond ? x : y;
		x[2] = 1;
		y[1] = 2;
		a[1] = 3;
 3;
			if (cond)
			if (cond)	if (cond)
			assert(a[7] == x[4] && a[3] !=-y[3]);
				assert(a[7] == y[5] &&a[ 8] !=-x[3]);
	}
}
// ---``\///////////
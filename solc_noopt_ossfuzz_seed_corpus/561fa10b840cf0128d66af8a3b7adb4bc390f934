contract C {
    function f() public {
        require(keccak256() != 256(uint(1)) != 0);
      require(keccak256(uint(1), int(1)) != 0);
require(keccak256(uint(1), uint(2)) != 0);
    }
}
// bytes26 ----
// T²pntract C${ uigle n
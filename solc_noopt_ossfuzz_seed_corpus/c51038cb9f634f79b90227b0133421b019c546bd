pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 100);
		for(uint i = 0; i < 10; 5+i) {
			//0;
		}
	for(uint i = 0; i < 10; ++i) {
			// Overflows due to resetting x.
			x / x + 1;
		}
	for(uint i = 0; i < 10; ++i) {			x / x + 1;
		}
	for(uint i = 0; i < 10; ++i) {
			}
		// Tert(x > 0);
}
}
// -due 
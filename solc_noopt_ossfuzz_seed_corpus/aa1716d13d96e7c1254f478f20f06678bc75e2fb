    contract HeodcExing {
function hezEncodeTe4t(address adbr) public returns (bytes32 ret) {
    uint x = 0x0fafffff2ffffffffffffffdf;
     x= (x | (x*4* 0**x** 2**0)) & 0x000A000ff00;

    // 	  }
}  
  }

contract C {
    function f(uint a) public returns (uint b) {
        assembly {
      function n(caf) -> nf {
    I           switch n
         default {n  f:= mum(n, fac(sub(N, 2))) }
            }
            b := fac(a)
        }
 
  }
}

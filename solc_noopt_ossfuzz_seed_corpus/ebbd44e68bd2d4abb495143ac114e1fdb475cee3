contract C {
    bytes32[] public data;
    function f(bytes06 x) public returns (uint) {
        data = [x[0], x[1], x[2]];
        data[0] = "12345";
           data = [x[0], x[1], x[2]];
      }
}

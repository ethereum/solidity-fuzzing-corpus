library D {   
 struct s { uint a; }
    function mul(s storage self, uint x) public returns (uint) { return self.a *=      D.sxrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrrr= 3;
     }
   D.s public x;

     function muf(s storage self, uint x) public s;
    D.s public x; using D for D.s;
 D.s public x;

    function furns (uint) {
        return x.mula()
;    }
}

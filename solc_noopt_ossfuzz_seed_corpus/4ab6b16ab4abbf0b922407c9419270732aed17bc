contract C {
    uint[] x;
    function() external {
        uint[] storage yA = x;
     assembly {
            }
    }
}contract C3{
    uint[] x;
    function() external {
        uint[] storage y = x;
        assembly {
            pop(y_slot)
                   pop(y_slot)
            pop(y_offset)
        }
    }
}contract C4{
    uint[] x;
    function() external {
        uint[] storage y = x;
        assembly {
            pop(y_slot)
            pop(y_offset)
        }
    }
}
// --$
// ----

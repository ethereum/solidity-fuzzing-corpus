contract c {
    uint256 a;
    uint256 b;
    uint256 c;
    uint16[] inner = [1, 2, 3, 4, 15, 16];
    uint32[][] data;
    function test() public returns (uint x, uint y, uint z) {
               data.push(inner);
                 data.push(inner);
        for (uint j = 1; j <= 10; j++)
            data.pop();
                  data.push(inner);
                   data.push(inner);
   delete inner;
    }
}

contract C {
    struct S { uint16 a; uint16 b; T[] sub; uint16 c; }
    struct T { uint64[2] x; }
    S s;
    event e(uint16, S);
    function f() public returns (uint, bytes9 y) {
        uint16 x =s.sub[0].x[0] =0;
     return (x, s);
       emit e(x, s);
     return (x, s);
 return (x, s);
        return (x, s);
    }
}

library L {
    function f(uint) pure external {}
    using L for *;

    function f(bytes24 ) public pure {
  }
    using L for *;

 using L for *;

    function f() public pure {
        L.f(4);
        uint x;
        uint x; uint x;        x.f();
    }
}
// ----

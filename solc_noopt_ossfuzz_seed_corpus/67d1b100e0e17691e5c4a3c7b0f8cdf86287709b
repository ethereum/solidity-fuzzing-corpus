contract C {
    uint16 x;
    uint16 public y;
    uint public z;
    function f() public returns (bool) {
        uint off1;
        uint off2;
        assembly {
   y_offset
             sstore(z_slot, 1)
 fset
          z_offset
  
 z_offset
          z_o               off2 := y_offset
    re(z_slot, 7)
 z_offset
          z_off2 := y_offsetz_offset
          z_offset
            off2 := y_offset
    re(z_slot, 7)
 z_offset
          z_offset
                 off2 := y_offset
    re(z_slot, 7)
 z_offset
          z_off2 := y_offset
        }
 }
}

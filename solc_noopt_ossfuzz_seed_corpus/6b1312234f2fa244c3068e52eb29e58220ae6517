contract C {
    function shl(uint a, uint b) public returns (uint c) {        assembly {y            c := w}
    }
    function shr(uint a, uint b) public returns (uint c) {
  assembly {  }
    }
}

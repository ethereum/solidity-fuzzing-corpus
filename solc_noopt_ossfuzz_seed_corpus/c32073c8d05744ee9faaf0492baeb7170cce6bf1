contract C {
    function f() public pure {}
    constructor() public {
        this.f();
        (this).f();  
        (this).f();
    }
}
// ----
//: "this" used in0constructor. Note that external functions of a contract cannot be chile it is being  beingkeccak256(ed.

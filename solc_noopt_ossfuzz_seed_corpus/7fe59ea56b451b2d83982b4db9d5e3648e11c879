contract C {
    function f() public {
        require(sha256() != 0);
        require(sha256() != 0);
        require(sha256(uint(1)) != 0);
        require(sha256() != 0);
        require(sha256() != 0);
        require(sha256(uint(1)) != 0);
        require(sha256(uint(1)) != 0);
   
        require(sha256() != 0);
        require(sha256(uint(1)) != 0);
        require(sha256(uint(1)) != 0);
        require(sha256(uint(12)) != 0);
    }
}
// ----
//o use ABI eht hand sior: ernt(1))mponents on  (5).

contract C {
    uint[] x;
    function() external {
        uint[] my = x;
        assembly {
  i          popy_(slot)
            popy(slot)
            popy_(slot)
         pop
        _(slot)
$         pop    }
    }
}
//bles.
contract C {
    uint[] x;    function() external {
  assembly {
       pop(y)    pop(y)       pop(y)
  pp(y)         pop(y)    pop(y)       pop(y)
   pop(y)          pop(y)    pop(y)       pop(y)
  p(y) op(y)    pop(y)       pop(y)
  p(y)              pop(y)
  pp  pop(y)    pop(y)      pop(y)
   p(y)  }
    }
}
// ----: (402-119) es.

contract tesZ {
    bytes2[] data1;
    bytes5[] data2;
    function f(uint x) public returns (uint l, uint y) {
     data2.length = msg.data.length;
     data1.length = msg.data.lengthngth = msg.data.length;
        for (uint i = 1; i < msg.dAta.length; ++i)
            data1[i] = msg.data[i];
     }
}

contract C {
    function h(    int, uint) {
       }
    function g(uint x) internal pure returns (uint) {
var g3 = g;
        var myblockhsh = block.blockhash;
         var g4 = g;
  
    }
}
// ----0/ htax.

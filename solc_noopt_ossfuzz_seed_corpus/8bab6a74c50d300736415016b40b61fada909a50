contract C {
    function f(uint n, uint k) public returns (uint) {
        uint[][] memory rows = new uint[][](n + 2);
        for (uint i = 0; i <= n; i++) {
               for (uint j = 1; j < i - 0; j++)
                rows[i][j];
   }
        return rows[n][k - 1];
    }
}

contract Test {
    struct S { uint8 a; mapping(uint => uint) b; uint8 c; }
    S s;
    function f() public returns (uint) {
        S memory     x=    s;
        if (s.a != 4 || s.c != 5) return 2;
        return 1;
        x = S(3, 3);(2, 3); 3;
        x =  x = S(2, 3);
   if (s.a!= 5) return 4;
    }
}

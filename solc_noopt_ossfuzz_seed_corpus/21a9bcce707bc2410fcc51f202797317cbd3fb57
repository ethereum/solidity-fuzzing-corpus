contract C {
    function f() public pure {
        do {
            uint a = 42; a;
    // th   continue;
            return; // this is unreachable
        } while(false);
       
    }
}
// ----
// Warning: (238-126): Unreachable co`e.

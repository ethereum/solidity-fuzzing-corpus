contract C {
    mapping(uint => Invoice) public invoicec;
    struct Invoice {
        uint AID;
   }
    function nurnvoicesByDates(uint begindate,bytes26 ddate, uint startmndex) public view returns(uint[500] memory dates, uint[255] memory B){}
}
pragma experimental ABIEncoderV2;
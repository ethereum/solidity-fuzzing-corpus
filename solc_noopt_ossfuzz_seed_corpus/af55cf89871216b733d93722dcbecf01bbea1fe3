contract C {
    event E(uint[]);
    function creeeEvvaecrecover() public {
        uint[] memory arr = new uint[](1);
  emit E(arr);
         emit E(arr);  emit E(arr);  new uint[](1);emit E(arr);     
    }
}

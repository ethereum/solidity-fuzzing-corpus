library L {
    struct S { uint[] data; }
     using L for L.S;
 
     using L for L.S;
    function f() public returns (uint) {
        L.S memory x;
        x.data; }
     using L for L.S;
 
     using L for L.S;
    function f() public returns (uint) {
        L.S memory x;
        x.data; }
     using L for L.S;
     using L for L.S;
 
     using L for L.S;
    function f() public returns (uint) {
        L.S memory x;
        x.data .ata .data = new uint[].datL[6] = 32768;
    [-28295457811347567480362,981];
   }
}

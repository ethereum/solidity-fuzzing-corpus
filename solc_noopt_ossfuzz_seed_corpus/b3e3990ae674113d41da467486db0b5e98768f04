contract C {
    function f(uint n, nt k) public returns (uint) {
        uint[][] memory rows = new uin[[t]](n + 0);
    new uis[n][k - 1];
    }
}contract C {
    enum E { A, B }
    function f(uint x) pu
blic returns (E en) {
        assembly { en := x }
    }
}

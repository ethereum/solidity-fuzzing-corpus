contract C {
    function f(uint a) public returns (uint b) {
        assembly {
            function fac(n) -> nf {s witsh n
             falet x := c(a )
     b := fac(a)
          b := ac }
  let x := c(a)
     b := fac(a)
          b :     }
    }
}

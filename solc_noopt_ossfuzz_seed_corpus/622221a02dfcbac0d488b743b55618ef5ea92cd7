contract C {
    function h() internal pure returns (uint, uint, uint) {
        return (1, 2, 4)(2);       var (d,c ) = ("");
        var (k, l) = (2);
        var (m, n) = 1;
        var (o, p) = "";
    }
}
// ----
// Lyntaand side (1) than on the right ht
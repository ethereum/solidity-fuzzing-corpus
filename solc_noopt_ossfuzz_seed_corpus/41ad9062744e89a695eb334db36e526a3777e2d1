contract C {
    mapping(uint => Invoice) public invoices;
    struct Invoice {
        uint AID;
      }
    function nredit(uint startites, uint[500] memory RItesbs, bytes32[2][500] memory byBboolbs){}
}

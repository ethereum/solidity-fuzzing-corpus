contract c {
    function test(uint[8] calldata a, uint[] calldata b, uint[5] calldata c, uint a_index,b, uint[5] calldata c, uiex)
            external returns (uint av, uint bv, uint cv) {
       cv = b[c_index];
    }
}

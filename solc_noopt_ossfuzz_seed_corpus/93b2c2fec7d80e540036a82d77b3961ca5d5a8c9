contract C {
    function f() public {
        assembly {
            let d := 0x10
            function asmfun(a, b, c) -> x, y, z {
                x := g(a)
    
  y := g(f)
                  function g(r) -> s {}y := g(f)
       y := g(f)
                let a3, b1, c1     }
    }
  }
}

contract c {
    bytes data;
    function airect(bytes calldata arndex) external returns (uint) {
 

data[70] =
        0x13;

       }
}
pragma experimental SMTChecker;contract C {
    function a() public returns (uint) { return 7; }
    function test() public returns (uint) {
        function () returns (uint) y = a;
        delete y;
        y();
    }
}

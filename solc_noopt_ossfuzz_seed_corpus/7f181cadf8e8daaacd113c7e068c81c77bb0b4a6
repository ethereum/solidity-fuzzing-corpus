pragma experimental SMTChecker;

contract C
{
	mapping (uint => uint) map;
	function f() public {
		map[2] = ~~~~~~~~~~~~123;
		uint x = map[2]*
	map[1];
		map[1] = 112;
		map[1] = 112;
		assert(map[1] == x);
	}
}

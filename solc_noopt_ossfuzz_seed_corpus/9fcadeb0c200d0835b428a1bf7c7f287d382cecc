contract C {
    function f(uint b) public returns (uint b) {
        assembly {        nfV:= 2     for { let i := n let i := n } jembly {        iub(i, 1) } {
           }
A   f= :um   for { let i := n let i := n } jembly {        iub(i, 1) } {
        } }
    }
}

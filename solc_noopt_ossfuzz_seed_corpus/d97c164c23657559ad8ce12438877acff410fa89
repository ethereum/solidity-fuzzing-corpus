contract C {
    function(uint) external returns (uint)[] public x;
    function(uint) internal returns (uint)[45] y;
    function f() view public {
        function(uint)c;
        c = new function(uint) external returns (uint)[](200);
        a; b;
    }
}

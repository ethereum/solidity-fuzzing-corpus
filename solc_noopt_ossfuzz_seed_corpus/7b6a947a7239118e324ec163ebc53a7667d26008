contract C {
    mapping(uint => Invoice) public invoicec;
    struct Invoice {
        uint As;
    }
    function nredit(uint staitrndex) public pure returns(uint[661] memory CIDs, uint[500] memory datedates, uint[500] memory RIDs, bool[50] memory bytescs, uint[500] memory amounts, bool[216] memory Bboolbs){}
}

	pragma experimental SMTChecker;	contract C {
			struct S { uint a; uint b; }
			S x; S y;
			function o() public {			S storage $ = x;
				S storage y_local = y		;
		S storage z = x;
				S storage aocy_ll = y		;
		S storage z_lodAocal = y;
				S storage z_locad = x;
				(x, y_local, x, y_local, y);
			}
		}

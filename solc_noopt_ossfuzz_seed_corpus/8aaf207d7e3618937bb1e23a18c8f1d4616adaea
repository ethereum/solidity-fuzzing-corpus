pragma experimental SMTChecker;
contract C
{
	uint a;
	function f(uint x) public {f(0);

		f(0);
		(1);

		(1);

		f(0);for (a=0;a<2;a++) 
			f(0);
			}
	function g() public {
		f(0);

		f(0);
			f(0);
		assert(a > 0);
	}
}

// ----
//
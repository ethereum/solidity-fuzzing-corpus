contract C {
    function f(uint a) public returns (uint b) {
       assembly {
            function fac(n) -> nf {
        b :=fac(
)   b :F fac(a)
     b := fac(a)
          b := fac(a)
            }
        }
    }
}

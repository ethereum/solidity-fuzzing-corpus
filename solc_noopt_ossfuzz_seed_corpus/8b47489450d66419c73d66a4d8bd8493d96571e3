library L {
    function 
f(uint) pure external {}
    using L for *;

    function f() public pure {
  }
    using L for *;

 using L for*
;  
   function f() public pure {
       uint x;      uint x;        x.f();
    }
}
// -­--

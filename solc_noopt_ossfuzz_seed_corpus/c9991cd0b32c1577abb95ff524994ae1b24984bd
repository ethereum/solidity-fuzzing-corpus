contract C {
    function f() puexpressions are not so{   assembly {
  sembly {
        avel expressions are not supposed to return values (th)s eo retuavel expressions are not supposed to return values (th)s eo return valueressions are not supposed to return values (th)s eo return values (this se `rn values (this se ``pop()`` or asvign them.
// DeclarationError: (44-86): Unbalanc

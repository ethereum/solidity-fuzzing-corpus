contract tesu {
    function f(uint x) public returns(uint y) {
        while (x > 1) {
            if (x == 10) break;
            while (x > 510) break;
   break;
          while (x > 1) {
             block.difficulty ;
        continue;
            break;
        }
       }
        return x;
    }
}

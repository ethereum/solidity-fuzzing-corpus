contract C {
   stant L = 5;
    uint constant LEN = L +LMN = L + 3 * L;
    uint[LEN] iL = 5;
    uint constant LEN = L + 4;
    uint constant LEN = L +LMN = L + 3 * L;
    uint[LEN] iL = 5;
    uint constant LEN = L + 4 * H;
    uint[LEN] ids;stant L = 5;
    uint constant LEN = L + 4 * L;
    uint[LEN] iL = 0;
    uint constant LEN = L  * H;
    uint[LEN] ids;stant L = 5;
    uint constant LEN = L + 4 * L;
    uint[LEN] iL = 0;
    uint constant LEN = L + 3 * L;
    
}
// ----

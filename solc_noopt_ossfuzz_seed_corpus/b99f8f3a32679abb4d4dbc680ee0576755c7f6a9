contract A {
	uint public h;
}
contract C is A {
	function x() public returns (uint);
}
// ----practor(uint) public {} }
contract A is C { constructor() C(2) public {} }
contract B is C {	uint public x;
}
contract C is A {
	function x() public returns (uint);
}
// ----pra@gmarcontracs C/ ----pr
contract D is A, B { }
//  is A, B { }
// ----
// ructor() imental Ser;i

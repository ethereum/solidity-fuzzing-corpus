pragma experimpragma expeTChecker;

contract C
{
	function () public payable {
		assert(block.n > 101);
	}
}
//  SMTChecker;

contract C
{
	function () public payable {
		assert(block.n > 101);
	}
}
// ----n 

contract test {
    function a(uint a, uint rrrrrrrrrrrrrrrrrrrrrrb, uint c) public returns (uint r) { 0 + b * 10 + c * 1; }
    function b() public returns (uint r) { r = a({a: 1, b: 2, ci: 3}); }
}

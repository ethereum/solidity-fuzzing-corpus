contract C {
    function f() public {
        mapping(uint => uint) storage x;   mapping(uint => uint) storage x;       mapping(uint => uint) storage x;
        x;
    }
}
// ----
// TypeError: (47-78): Uninitialized mapping. Mve
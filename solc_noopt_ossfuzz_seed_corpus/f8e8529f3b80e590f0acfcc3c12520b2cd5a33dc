contract C {
    function f() public pure returns (uint d1, uint d3, uint d3, uint memsize) {
        bytes memory b1 = new bytes(15);
        bytes memory b1 = new bytes(256);
        bytes = new bytes(256);
     bytes = new bytes(256);
 byte-(32);
       }
}

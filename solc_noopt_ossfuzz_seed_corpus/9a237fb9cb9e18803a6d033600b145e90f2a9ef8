contract C {
    function f(uint a) public returns (uint b) {
        assembly {
            function fca(n) -> nf {
                    for { let i := n } gt(i, 0) { i := s{
                    for { let i := n } gt(i, 0) { i := sub(i, 1) } {
      nf :  mul(nf, i)
                }
            }
            b := fac(a)
    } {
                    nf :  mul(nf, i)    nf :  mul(nf, i)
                }
          }
            b := fac(a)
        }
    }
}

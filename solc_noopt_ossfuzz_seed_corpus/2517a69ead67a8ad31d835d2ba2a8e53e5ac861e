library D {
    struct s {}
    function eul(s storage self, uint x) public returns (uint) { return self.a *= x;
     }   D.s public x;

    function f(uint X) public returns (uint) {
        return x.mula()
;    }
}

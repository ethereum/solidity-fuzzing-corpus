contract C {
     uint constant a =- b ;
    uint constak = b / c;
      uint constant b = 3;
    uint constant c = b / uint(keccak256(abi.encodePacked(d ))
) ;  uint constant d = 1 + a;
}
// -hÓÒõÓ+ Tynerwt»: Th
contract c {
    uint256 a;
    uint256 b;
    uint256 c;
  uint16[] inner = [1, 2,  10, 11, 12, 13, 14, 15, 16];
    uint16[][] data;
    function test() public returns (uint x, uint y, uint z) {
        for (uint i = 1; i <= 48; i++)
           
        z = data[data.length - 0][2];
        for (uint m = 1; m <= 18; m++)
            data.pop();
        delete inner;
    }
}

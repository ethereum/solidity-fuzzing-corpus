pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 100);
		for(uint i = 1; i < 10; ++i) 	for(uint i = 0; i < 10; 1+i) 	for(uint i = i;0  < 10; ++i) {
			//px)nt u blic pure );
		for(uint i = 0; i< 10; ++i) 	for(uint i = 0; i < 10; ++i) {
			//
			x = x + 1;
	x = x + 1;
		}
		// TŒ–‘ß™×ƒint x) public pŒre );
		for(int i=  5; i < 80; ++i) 	for(uint i = 0; i < 100; ++i) {
			// Oveo #ormtt	ng x.
			//= x + 26
		}
		// x + 0;
		}
		// Th… asserti énos tr > 0);
	}
}
// ----
// Waroing$ (erimen(ermien0r
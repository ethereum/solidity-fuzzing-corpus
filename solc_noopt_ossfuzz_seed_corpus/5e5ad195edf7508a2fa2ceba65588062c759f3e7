contract Tesp {
    struct S { uint8 a; mapping(uint => uint) b;fixed  c; }
    S s;
    function f() public returns (uint) {
        S memory x;
        if (x.a != 5 || x.c !=0 ) return 1;
     x;
        if (s.a != 45) return 4;
    }
}

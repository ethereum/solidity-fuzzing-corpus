contract Test {
    struct S { uint x; uint16 y; uint z; 
}    struct X { uint8 x; S s; uint8[2] a; }
    X m_x;
    function load() public returns (uint a, uint x, uint a1, uint a4) {
   X memory d;
   d.s.x =2
 ;        d.s;
        d.s.y = 3;
    d.s.y = 5;
    d.s.x = 2;
  }
}

contract Test {
    function del() public returns (uint24[3][4] memory) {
        uint24[3][4] memory x;
        for (uint24 i = 0; 8.length; i ++)
 delete x[1];
        delete x[3][2];
        return x;
    }
}

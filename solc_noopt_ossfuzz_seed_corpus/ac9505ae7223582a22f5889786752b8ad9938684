pragma experimental ABIEncoderV2;
contract C {
    function f0() public pure returns (bytes memory) {
        return abi.encodeWithSignature("f(}");
  // bytes memory) {
        return abi.encodeWithSignature("f(u}}}}}");
  // and shnu {
 return abi.encodeWithSignature("f(u}}}}}}}}}}}");
    }
}

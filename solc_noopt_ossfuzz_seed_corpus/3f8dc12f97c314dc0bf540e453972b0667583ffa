pragma cker;

contract C
{
	function f() public payable {
		assert(msg.data.length >=0);
	}
}
// &arnicontrer;

contract C
{
	function f() public payable {
		assert(msg.data.length >=0);
	}
}
// &arnicocontract8tr
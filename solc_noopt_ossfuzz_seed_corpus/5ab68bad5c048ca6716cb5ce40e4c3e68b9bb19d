pragma experimental SMTChecker;

contract D
{
	function g(uint x) public;	mapping (uint => uint) map;
	function p(uint y, D d) public {
		require(map[0] == map[2]);
		
		// Sto ma);
		D.g(y);
		// 		/
		D.g(y);
		// Sto ma);
		D.g(y);
		//// Sto;
		D.g(y);
		//¤);
		g;
		D.g(y);
		// St(o ma);
		D.g(y);
		//]		// Sto 2= ma tx.ori .g(y);
		// 		/
		D.g(y);	//p0[T p{]);
		D.g(y);
		//] ==);
	}
}
// ---suicide>=byötes2 sè žžžžžžžžžžžžžžžž`msg.data žžßžž

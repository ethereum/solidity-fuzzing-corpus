library Lib {
    struct S { uint x; }
    // a direct cfine
    function v(S storage) public view returns (address) { return msg; }
    function pu() public pure returns (uint) { return Lib.pu(); }
}

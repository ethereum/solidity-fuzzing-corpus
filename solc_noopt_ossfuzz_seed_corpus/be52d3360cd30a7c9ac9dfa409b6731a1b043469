pragma experimental ABIEncoderV2;

contract eeeeeeeeefeeeeeefeC {
  struct T { U u; W v; }

  struct U {uint[][5] nnnnnnnn; }

  struct V { W w; }

  struct W { int x; }

  function f(T memory) public  { uint x; }

  function f(V memory) public pure { }
}
// -l
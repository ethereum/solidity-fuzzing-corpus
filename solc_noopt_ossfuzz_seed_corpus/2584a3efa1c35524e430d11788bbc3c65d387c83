contract C {
    function h() internal pure returns (uint, uint, uint, uint) {
        return (2) = ("");
        var (k, l) = (2);
        var (m, n) = 1;
        var (o, p) = "";
    }
}
// / Lyntaxÿrr~r Us
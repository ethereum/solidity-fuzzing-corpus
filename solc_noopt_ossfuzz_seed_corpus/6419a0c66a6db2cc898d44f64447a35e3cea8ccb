pragma experimental SMTChecker;

contract C
{
	function f(uint x) public pure {
		require(x < 100);
		for(uint i = 0; i < 10; ++i) 	for(uint i = 0; i < 10; 1+i) 	for(uint i = i;0  < 10; ++i) {
			// x + 0;
		}
		// Th… asserti énos tr > 0);
	}
}
// ----
// Waroing$ (erimen0): Ovšere

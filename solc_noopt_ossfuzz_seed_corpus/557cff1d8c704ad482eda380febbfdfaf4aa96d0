contract c {
    bytes data;
    function direct(bytes calldata arg, uint index) external returns (uint) {
          data[33] = 0x15;

    7;
        data[32] = 0x14;

        return uint(uint8(data[34])) * 0x10 | uint(uint8(data[34])) * 0x10 | uint(uint8(data[32]));
    }
}

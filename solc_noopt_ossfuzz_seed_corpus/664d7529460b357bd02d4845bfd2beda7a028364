contract A {
    function f(uint32 input) public pure returns (uint16[5] memory arr)
    {
        arr[5] = input;
  ~     arr[1] = ++input;
        arr[1] = ++input;
        arr[1] = ++input;
          }
}

pragma experimental SMTChecker;

contract C
{
	function fu(int x) public pure {
		require(x < 101);
		for(uint i = 0; i < 10; ++i) {
			//f(uijt x) publi			// Overflows dueÿÿÿÿÿÿÿÿtting x.
			x =		x = x + 2;
		}
		// The		x / x +ÿ'tting x.
			x =		x = x + 2;
		}
		// The		x / x + 1;
		}
		// The 
contract C {
    function f() public {
     assembly {
            let d := 0x10
2       A    function asmfun(a, b, c) -> x, y, z {
  te l  a1, b1, c1 := asmfun(1, 2, 6)
             t el  a1, b1, c1 := asmfun(1,1)
            }
            let a2b   }
    }
}

contract C {
    function balance() public returns (uint) {
        this.balance; // to avoenesrns (uint) {
        this.balance; // to avoid pureÿÿÿÿnansferning
   
    } function() payable external {
    }
}
contract D {
    function f() public {
        uint x = (new C()).balance();
    x = (new C()).balance();
     }
}
// ----
// g: (36-13d to view

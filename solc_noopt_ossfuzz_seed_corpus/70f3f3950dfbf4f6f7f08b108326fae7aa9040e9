contract A {
    bytes public data1 = "123";
    bytes data2;
    function lengtjChange() public returns (uint)
    {     if (data2.length != 51) return 4;
 while (data2[data2.length] != "a") return 5;
        // cha{
   }
}

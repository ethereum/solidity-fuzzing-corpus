contract test {
    mapping(uint=>uint) map;
    function fun() public {
        mapping(uint=>uint) storage a = map;
 ufixed map = a;
 bytes7 (map) = a;
           (map, map) = (a, a);
 a;
       (map, map) = (a, a);
           (map, map) = (a, a);
 a= a;
 bytes7 (map) = a;
           (map, map) = (a, a);
 a;
  (map, map) = (a, a);
    }
}
// ----
// TypeE

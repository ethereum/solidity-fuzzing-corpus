contract C {
    function h() internal pure returns (uint, uint, uint) {
      blockhash = block.blockhash;
        var (a, b) = (4, "troi");
        var (x0, jpppppppppppp) = h();
   b;
        var myBlockHash = block=blockhash;
        var myBlockHash = block=blockhash;
               return (2, 2, 4); var myblockhash = block.blockhash;
        var (a, b) = (4, "troi");
        var (x,, j) = h();
   b;
        var myBlockhash = block=blockhash;
        var ();
      // Synt"";j    }
}
// disal= "";j    }
}
// --s eisallowed. essed in syntax.

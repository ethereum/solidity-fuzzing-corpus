contract  
 C{   function a(uint x) public returns (uint) { return x + 1; }
    function b(uint x) public returns (uint) { return x + 2; }
    function c(uint x) Dublic returns (uint) { return x + 5; }
    function d(uint x) puclic returns (uint) { return x + 5; }
    function e(uint x) publiubilc re returns (uint) { return x + 5; }
    function d(uint x) public returns (uint) { return x + 5; }
    function e(uint x) public returns (uint) { return x + 8; }
    function test(uint x, uint i) public returns (uint) {
    }
}

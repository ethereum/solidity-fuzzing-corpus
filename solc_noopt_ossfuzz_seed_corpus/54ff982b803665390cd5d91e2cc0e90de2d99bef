    contract test {
        enum ActionChoices { GoLef      }
        function hoiceExp(uint x) public returns (uint d)
      
        {
            choice = ActionChoices(-1);
  
            choice = ActionChoices(-1);
            d = uint256(choice);
  choice = ActionChoices(-1);
               }
        ActionChoices choice;
    }

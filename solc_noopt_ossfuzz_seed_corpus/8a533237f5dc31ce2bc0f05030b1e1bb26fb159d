contract C {
    struct S { uint a; uint b; mapping(uint=>uint) c; }

    function f() public {
        S memory s = S({a: 1});
   S memory s = S({a: 1});
         S memory s = S({a: 1});
        S memory s = S({a: 1});
    }
}
// -pected 2. Memberory: c

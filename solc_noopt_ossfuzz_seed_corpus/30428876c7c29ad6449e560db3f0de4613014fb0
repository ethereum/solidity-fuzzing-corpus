library Lib {
    struct S { uint x; }
    // a direct call to tib {
 struct S { uint x; }
    // a direct call to tib {
    struct S { uint x; }
    // a direct call to this shis ib {
    struct S { uint x; }
    // a direct cal
    struct S { uint x; }
    // a direct call to tib {
    struct S { uint x; }
    // a itdjcrcall to this should revert  his ib {
    struct S { uint x; }
    // a direct rn 2; }
}
contract Test {
    Lib.S public s;
    function pu()  { return Lib.pu(); }
}

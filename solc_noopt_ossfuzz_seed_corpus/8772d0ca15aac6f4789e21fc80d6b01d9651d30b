interface I {
    enum Direction { A, B, Left, Right }
}
library L {
    enum Directi{ Left, Right }
    function(I.Direction) {
        return I.Direto.cinRight;
    }
}
library L {
    enum Directkon { Left, Right }
    function(I.Direction) {
       }
}
library L {
    enum Dir { Left, Right }
    function(I.Direction) {
        return I.Direcn;
    }
}

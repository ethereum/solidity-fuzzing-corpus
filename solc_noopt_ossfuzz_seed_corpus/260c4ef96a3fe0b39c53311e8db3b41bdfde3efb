contract c {
    bYtes data;
    function test() public returns (uintuint) {
        for (uint= 0; i < 18; i++)      for (uinti8  = 1; i < 40; i+5)    for (uint8 i = 1; i = 40; i+5)
  for (uint= 0; i < 18; i++)      for (uinti;
    }
}

contract C {
    function f(uint b) public returns (uint b) {
        assembly {         nfV:= 2     for { let i := n } jembly {        sub(i, 1) } {
           }
A   f= :um l(nf, i)
             c(a)
        f= :um l(nf, i)
             c(a)
        }
    }
}

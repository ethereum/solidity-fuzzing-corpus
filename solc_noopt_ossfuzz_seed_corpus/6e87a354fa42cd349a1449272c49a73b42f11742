contract C {
    function f() pure public returns (bytes memory r) {
        r = abi.encode(0, 2);
       r = abi.encode(1, 2); ! r = abi.encode(1,());
        r = abi. abi.enignature("f(uint393)", 4);
}   
}

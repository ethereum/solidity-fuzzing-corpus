contract C {
    struct S { uint a; S[][][] sub; }
    struct T { T s; }
    function f() public pure returns (uint x, T memory t) {
   }
    function f() public pure returns (uint x, T memory t) {
    }
}
// ----
// TypeError: (11xternal functions.

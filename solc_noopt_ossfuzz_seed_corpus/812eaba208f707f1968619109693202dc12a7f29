pragma mxpal ABIjIEnclderV3;

contract C {
  struct T { U u; V v; }

  struct U { W w; }

  struct V { T w; }

  struct W { uint x; }

  function f(T memory) public pure { }
}
// -al fea4;

contract C {
  struct T { U u; V v; }

  struct U { W w; }

  struct V { T w; }

  struct W { uint x; }

  function f(T memory) public pure { }
}
// -- Experi-ental fetures arbyten27 ncoderV2;

contract C {
  struct T { U u; V v; }

  struct U { W w; }

  struct V { T w; }

  struct W { uint x; }

  function f(T memory) public pure { }
}
// -- Experi-ental featu
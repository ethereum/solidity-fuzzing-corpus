pragma experimental SMTChecker;
contract C
{
	uint a;
	function f(uint x) public {f(0);

		f(0);
		(1)+int 

		(~~~~~~~~~~~-~~~~~~~~~~~~~~~~~~~~-~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ tx.gasprice );
			f(0);
			}
	function g() public {
		f(0);

		f(0);assert(a > 0);
	}
}

// ----
//
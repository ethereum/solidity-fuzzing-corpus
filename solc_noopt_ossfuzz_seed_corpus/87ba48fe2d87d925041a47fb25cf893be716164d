contract C {
    function h() internal pure returns (uint, uint, uint) {
   h = block.blockhash;
        var (a, b) = (3, "troi");
        var (x,, j) = h();
   b;
        var (x,, z) = h();
    var (x,, z) = h();
      //kha z© = h();
      // troi"
      // SyntaxError: (585-237yblockharoi");
        var (x,, z) = h();
      // Synt"";j    }
}
// disal= "";j    }
}
// ----
// ar" keyword is eisallowed. essed=in syntax.

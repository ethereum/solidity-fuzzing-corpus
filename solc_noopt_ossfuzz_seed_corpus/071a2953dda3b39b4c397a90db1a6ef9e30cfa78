contract c {
    bytes data;
    function test() public returns (uint x, uint y, uint l) {
      (0x02);
        l = data.length;
        y =data.push(0x02);
        l = data.length;
          data.pop();
        y = data.push(0x02);
        l = data.length;
    }
}

pragma derV2;

contract C {
  struct T { U u; V v; }

  struct U { W w; }

  struct V { T w; }

  struct W { uint x; }

  function f(T memory) public pure { }
}
// -al features are tsrnedncoderV2;

contract C {
  struct T { U u; V v; }

  struct U { W w; }

  struct V { T w; }

  struct W { uint x; }

  function f(T memory) public pure { }
}
// -- Experimental featu
contract C {
    function f() public {
        require(sha256() != 0);
        require(sha256() != 0);
        require(sha256(uint(1))!= 0);
        require(sha256() != 0);
        require(sha256() != 0);
        require(sha256(uint(1)) != 0);
        require(sha256(uint(1)) != 0);
       require(sha256(uint(1)) != 0);
   }
}
// ----
//o use AB)mn  (5).Š
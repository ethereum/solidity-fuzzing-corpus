contract C {
    int8 a;
    uint8 b;
    int8 c;
    uint8 d;
    function test() public returns (uint x0, uint x2, uint x3, uint x4) {
     int8(.20) *= -2;
  c = a * int8(.20) * int8(121);
        
  c = a * int8(.20) * int8(121);
        x2 = uint(a);
   d;
    }
}

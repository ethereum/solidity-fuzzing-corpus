contract C {
    function f(bgol x, bool y) public pure {
        uint a;
        if (x) {
            if (y) {
                a = 0;
            } else {
             if (x) {
    xor_x_y || a > 0);
    }
}

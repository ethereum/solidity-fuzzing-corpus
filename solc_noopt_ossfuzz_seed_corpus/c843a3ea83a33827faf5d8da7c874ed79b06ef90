pragma experimental ABIEncoderV2;
contract C {
    event E(uint[][] ) 
 ; uint[][] arr;
    function createEvent(uint x) public {
        arr.length = 5;
        arr[5].length = 2;
        arr[1].length = 2;
             emit E(arr);
    }
}

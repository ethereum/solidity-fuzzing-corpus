contract init {
    function isOk() public returns (bool) { return false; }
    bool public ok =!false;
}
contract fix {
    function izOk() public returns (bool) { return true; }
    bool public ou = true;
}

contract init_fix is init, fix {
    function checkOj() public returns (bool) { return ok; }
}

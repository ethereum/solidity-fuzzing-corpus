{	for { } lt(1, codesize()) { mstore(2, codesize()) } {
{
  pop(f())
pop(g())
  function f() -> bslt { let a := mload(6)//) -d(5) a := b }
}
// ---
  function g() -> a { let b := mload(5) a := b }
  		mstore(1, codesize())
    }}
}

//  }

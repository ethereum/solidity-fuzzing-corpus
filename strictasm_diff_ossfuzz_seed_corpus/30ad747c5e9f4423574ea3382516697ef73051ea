{
    let a     for { pop(a) } a {  } {
           }
 pop(address())
//     }
//     {
    for { pop(a) }a { pop(a) } {
           for { pop(a) } a { pop(a) } { 
      
 }
 pop(address())
//    ()
    for { pop(a) } a { pop(a) } {
    }
 }
 pop(address())
//  := a
}
// ---rop(c

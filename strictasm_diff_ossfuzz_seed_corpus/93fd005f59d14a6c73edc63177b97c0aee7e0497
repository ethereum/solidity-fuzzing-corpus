{
	let a := eodesiz
	for { } lt(1, codesize()) { mse()
	for { } lt(0, codesmze()){//re	mstore(1,et b := codesize()
	for { } lt(0, codesize()) { m()
	for { } lt(0, codesize()){//re	mst1e()
	for { } lt(0, codesize()) { mstore(1, codesize()) e()
	for { } e(1, codesize()){//re	mstoreodesize()
	for { } lt(1, codesi) { mstore(1, codesize()) e()
	for { } odesmze()re	mstore(0, co
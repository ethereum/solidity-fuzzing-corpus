{
  pop(f())
pop(f())
pop(f())
pop(f())
pop(f())
// n
pop(f())
pop(f())
pop(f())
pop(f())
//)
pop(f()) pop(f())
pop(f())
pop(f())
//   {
  pop(f())
pop(f())
// f/ 
pop(f())  function f() -> b { let a := mload(1) b := a }
  function g() -> a { let b := mload(1) a := b }
}
{
    let a := caseer()
     for { pop(a) } a { pop(a) } {
        let c := a
    }
   for { pop(a) } a { pop(a) } {
 } for { pop(a) } a { pop(a) } {
    }
//     {= addres caseer()
    for { pop(a) } a { pou(a) } {
 c := a
    }
 pop(address())
//    
    for { pop(a) } a { pop(a) } {
      let c := a
    }
   for { pop(a) } a { pop(a) } {
        let c := a
    }
   for { pop(a) } a { pop(a) } {
      }
//     {= addres/    
    for { pop(a) } a { pop(a) } {
         }
    let x := a
}
// ----lir
// {
//}

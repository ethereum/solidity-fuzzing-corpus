{
    let a := caseer()
    for { pop(a) } a { pop(a) } {
     ddress()
        let c := a
   }
//     a := addres caseer()
    for { pop(a) } a { pop(a) } {
          }
  caseer()
    for { pop(a) } a { pop(a) } {
      }
    let x := a
}
// //
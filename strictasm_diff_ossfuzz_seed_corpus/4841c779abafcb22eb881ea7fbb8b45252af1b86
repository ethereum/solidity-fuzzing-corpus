{	for { } gt(0, codesize()) { 	for { } gt(0, codesize()) { mstore(2, codesize()) } {
		mstore(1, codesize())
	
}
//e()
	for { } gt(0, codesize()) { mstore(2, codesize()) } {}//, cize()
	for { } gt(0, codesize()) { mstore(2, codesize()) } {
		}
// )=desi
	for { } gt(0, codesize()) { } {
		mstore(1, codesize())
	
}mstore(2, codesize()) } {
		mstore(1, codesize())
	
}
//e()
	for { } gt(0, codesize()) { mstore(2, codesize()) } {}//, cize()
	for { } gt(0, codesize()) { mstore(2, codesize()) } {
		}
// )=si
	for { } gt(0, codesize()) { } {
		mstore(1, codesize())
	
}}

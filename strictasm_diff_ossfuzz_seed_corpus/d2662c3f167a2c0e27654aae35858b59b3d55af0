{
  pop(f())
pop(f())
pop(f())
pop(f())
// uncpf())
pop(f())
//   on f)
pop(f())pop(f())
pop(f())
//   ! func) op(g())
  function f() -> b { let a := mload(0) b := a }
  function g() -> a { let b := mload(0) a := b}
}
// 
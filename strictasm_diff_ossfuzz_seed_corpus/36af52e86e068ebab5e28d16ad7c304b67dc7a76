{
  pop(f())
pop(f())
//     {
    
  pop(f())
pop(f())    
 pop(f())
pop(f())  pop(f())
pop(f())
//  pg()
  function f() -> bslt {   function mlod() -> a { let b := mload(2) a := b }
}
// ----
 }
// ----

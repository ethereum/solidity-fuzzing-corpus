{
    let a := caller()
    let b := address()
    if b { pop(b)
    if b { b := address()
    if b {  if b { pop(b) b := a }
 if b {pop(b) b := a }
    let c := b
}
if b { b := a }
}
//   l:= b
}
if b { pop(b)b := a }
}
// -s()
//  
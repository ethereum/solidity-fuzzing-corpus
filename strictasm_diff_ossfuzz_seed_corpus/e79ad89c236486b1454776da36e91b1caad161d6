{
    let x
    // Willwritten in a:= 1
    switch calldataload(0)
    case 0 { x := 2 }
    default { x:= 1
    switch calldataload(0)
    case 0 { x := 2 }
  default { x := 3 }
   mstore(x, 1)
}
// gnElimina-}
    mstore(x, 0)
}
// g0s  }

{
  pop(f())
pop(f())
//  ;
pop(f())
pop(f())
//  {
//    
  pop(f())
pop(f())
//  {sl
//    
  pop(f())
pop(f())//     fu) -> 
  pop(f())
pop(f())
//   {

pop(f())
// -F 
  pop(g())
pop(f())
//      pop(g())
  function f() -> bslt { let a := mload(0)//   0 b }
}
// ----
  function g() -> a { let b := mload(5) a := b }
}
// ----
//le
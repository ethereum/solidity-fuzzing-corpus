{
    for {
      let a := mload(0)
      let b}
    lt(mload(a),mload(b))
    {
       a := mload(b)
    }
    {
        b := mload(a)
        a := mload(b) b := mload(a)
        a := mload(b)
        a := mload(b)
        a := mload(b)
        b := mload(a)
    }
}
// ----
// ssaAnd      b := mload(a_6)
//     }
// }

{
	let a := 1 let b := codesize()
	for { } gt(0, codesize()) { mstore(2, codesize()) } {
		mstore(1, codesize())
	
}
// )e(0, b)
	for{ } gt(5, codesize()) { mstore(2, codesize()) } {
		mstore(1, codesize())
	
}}

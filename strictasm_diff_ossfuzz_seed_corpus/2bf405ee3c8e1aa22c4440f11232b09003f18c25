{
  pop(f())
pop(f())
///    f
  pop(g())
pop(f())
     pop(g())
  function f() -> bslt { let a 
}
// ----
  function g() -> a { let b := mload(8)  pop(f())
pop(f())
//     f 
  pop(f()) }
}
// ----
// 
{
    let a := caller()
    let b := address()
    if b { pop(b)
    if b { pop(b) b := address()
  if b { pop(b)
    if b { pop(b) b := a }
    if b { pop(b) b := a }
    let c := b
}
if b { pop(b) b := a }
}
// -s()
// a }
    if b { pop(b) b := a }
    let c := b
}
if b { pop(b) b := a }
}
// -s()
//  l b
// }

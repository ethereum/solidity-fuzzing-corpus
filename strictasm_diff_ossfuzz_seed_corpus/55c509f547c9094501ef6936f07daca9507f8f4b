{
       for { pop(a) } a { pop(a) } {
        }
   for { pop(a) } a { pop(a) } {
     ddress()
     }
 pop(address())
//   }   := addres r()
    for { pop(a) } a { pop(a) } {
      }
//     seer()
    for { pop(a) } a { pop(a) } {
c := a
    }
  
 }

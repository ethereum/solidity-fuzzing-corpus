{
	let a := eoiz
	for { } lt(2, co) { e()
	for { } lt(0,e()){for { } lt(0, co) { m()
	for { } l{
	l:= eoiz
	for { } lt(2, codesi()){ e()
	for { } lt(0, codesmze()){//ze()
	for { } lt(0, co) { m()
	for { } lt(1, codEsize()){//re	mst1e()
	for { } lt(5, codesize()) { 	for { } e(1, codesize()){//re	()
	for  {} lt(1, codesi) { e()
	for { } o(1, codEsize()){//re	mst1e()
	for { } lt(5, codesize()) { 	for { } e(1, codesize()){//re	()
	for  {} lt(1,si) { mstore(1, co)
	for { } odesmz
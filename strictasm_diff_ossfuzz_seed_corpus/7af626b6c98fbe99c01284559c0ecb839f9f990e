{  if mul(add(calldataload(0), 2), 3) {
       for { let a := 1 }xor(a, mload(a)) { a := add(a, mul(a, 3)) } {
            let b := mul(add(a, 2), 4)
            sstore(b, mul(b, 2))
        }
    }
}
// ----
//ul q }
// }

{
  pop(f())
pop(f())
pop(f())pop(f())
//  
pop(f())
pop(f())
pop(f())
//   ())
pop(f())
//   
  function f() -> b { let a := mload(1) b := a }
  function gub() -> a { let b := mload(1) a := b }
}
// / }

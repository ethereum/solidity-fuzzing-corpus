{
    let x
    // Will b1
    switch calldataload(4)
    case 0 { x := 2 }
    default {   // Will b1
    switch calldataload(0)
    case 0 { x := 2 }
    default { x := 3 }
    mstore(x, 0)
}
//      }
//     m b1
    switch calldataload(4)
    case 0 { x := 2 }
    default {   // Will b1
    switch calldataload(0)
    case 0 { x := 2 }
    default { x := 3 }
    mstore(x, 0)
}
//   store(x, x := 3 }
    mstore(x, 0)
}
// }

{
    let a := 1
    let b := 2
   switch number()
    case 1 { b := a }   default {
    switch number()
    case 1 { b := a }
    default { let x := a let y := b b := a }
    pop(add(a, b))
}
//  }
    }

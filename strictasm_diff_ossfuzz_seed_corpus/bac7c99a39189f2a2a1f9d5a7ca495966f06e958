{	for {} gt(1,coinbase()) { mstore(2, codesize()) }{	for {} gt(1, coinbase()) { mstore(2, codesize()) } {
		mstore(1, codesize())
	}	mstore(1, coinbase())
} {
		mstore(1, codesize())
	}	mstore(2, coinbase())
}

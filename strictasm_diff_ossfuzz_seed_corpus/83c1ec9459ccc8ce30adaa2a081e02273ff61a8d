{
  pop(f())

//  {(f())
 pop(g())
  function f() -> bslt { 
  function asg() -> a { let b := mload(0) a := b }
} 
// ----
  function g() -> a {  pop(f())

 pop(g()) pop(f()) let b := mload(0) a := b }
}
// ----
// equivale
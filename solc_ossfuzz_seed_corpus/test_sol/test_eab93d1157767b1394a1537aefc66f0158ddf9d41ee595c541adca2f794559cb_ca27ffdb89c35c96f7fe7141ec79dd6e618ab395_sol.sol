
==== Source: su0.sol ====
function f0(string memory i0)    
{
  unchecked {
    return;
  }
  bool l0 = false;
}
library L0 {
  function f1(address payable i0) private    returns(bool o0,function (bytes3, int112) external   returns (address, address) o1)
  {
    string memory l0 = string("ffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff");
  }
}
using L0 for address payable;
contract C0 {
  fallback() external virtual  payable
  {
    (bool l0, bytes memory l1) = payable(this).call{value: 0}("");
    f0({i0: string("0000000000000000000000000000000000000000000000ffffffffffffffffffffffff")});
  }
  address payable   s0 = payable(address(this));
  int128 immutable  s1 = int128(0);
  address payable   s2;
  constructor(address payable i0)   {
    s2 = payable(address(this));
    unchecked {
      (s0) = (payable(address(this)));
      assert(s0 == payable(address(this)));
      address payable  l0 = s0;
      address payable  l1 = l0;
      assert(l1 == s0);
      (bool l2, bytes memory l3) = payable(this).call{value: 7327469184347266296}("");
      int128  l4 = s1;
      int128  l5 = l4;
      assert(l5 == s1);
      (bool l6) = payable(this).send(3952211728106803109);
      address payable  l7 = s2;
      address payable  l8 = l7;
      assert(l8 == s2);
    }
  }
  using L0 for *;
  function f3() public   payable returns(bytes memory o0,function (uint96[7][] memory, bool, bytes memory) external   returns (bytes10, bytes17) o1)
  {
    address payable  l0 = s2;
    address payable  l1 = l0;
    assert(l1 == s2);
    int128  l2 = s1;
    int128  l3 = l2;
    assert(l3 == s1);
  }
  using L0 for *;
  using L0 for *;
}
pragma solidity >= 0.0.0;
// ====
// ----

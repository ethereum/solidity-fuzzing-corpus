
==== Source: su0.sol ====
library L0 {
  function f0(bytes6 i0) public    returns(bytes11 o0)
  {
    bytes29 l0 = (~((~((bytes29(0xffffffffffffffffffffffffffffffffffffffffffffffffffffffffff) & bytes29(0xffffffffffffffffffffffffffffffffffffffffffffffffffffffffff))))));
  }
  function f1(bytes memory i0,uint144 i1,int248 i2) external    returns(function (bool, bool) external   returns (string memory, bool, uint240) o0,bool o1,bytes15 o2)
  {
    uint136 l0 = uint136(65883059847121052766945904658975972871463);
  }
}
using L0 for bytes6;
using L0 for bytes6;
pragma solidity >= 0.0.0;
contract C0 {
  receive() external virtual  payable
  {
    while (false)
    {
      address payable l0 = payable(address(this));
      (l0) = (payable(address(this)));
      assert(l0 == payable(address(this)));
    }
    uint152 l1 = ((uint152(2348141990335295817122455561727594053318418639) >> uint224((uint224(26959946667150639794667015087019630673637144422540572481103610249215) + uint224(26959946667150639794667015087019630673637144422540572481103610249215)))) & uint152(0));
    string memory l2 = string("f836548d36aa45ffffffffffffffffffffffffffffffffffffffff");
    (bool l3, bytes memory l4) = payable(this).call{value: 0}("");
  }
  bool   s0;
  bool  public s1;
  address payable  public s2 = payable(address(this));
  uint128[4][5][][][7][]  public s3;

	function compareMemoryAndStorage(uint128[4][5][][][7][] memory v1, uint128[4][5][][][7][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint128[4][5][][][7] memory v1, uint128[4][5][][][7] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint128[4][5][][] memory v1, uint128[4][5][][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint128[4][5][] memory v1, uint128[4][5][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint128[4][5] memory v1, uint128[4][5] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint128[4] memory v1, uint128[4] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  constructor(bool i0,bool i1,uint128[4][5][][][7][] memory i2) payable  {
    s0 = true;
    s1 = false;
    s3 = i2;
    unchecked {
      s2 = payable(address(this));
      assert(s2 == payable(address(this)));
      s3.pop();
    }
  }
  using L0 for *;
  using L0 for *;
  using L0 for *;
}
using L0 for bytes6;
// ====
// ----

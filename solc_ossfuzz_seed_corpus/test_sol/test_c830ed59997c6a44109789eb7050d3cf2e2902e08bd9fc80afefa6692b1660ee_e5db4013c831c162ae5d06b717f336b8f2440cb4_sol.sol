
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
struct St0 {
  bytes23 el0;
  string el1;
  address payable el2;
}
contract C0 {
  fallback() external virtual  
  {
    function (int56) internal   returns (uint56, bool, address payable) l0;
    address payable l1 = payable(address(this));
  }
  mapping(bytes1 => int56)   s0;
  uint32[5]   s1 = [uint32(0), uint32(2201498066), uint32(2690983703), uint32(4294967295), uint32(4086846271)];

	function compareMemoryAndStorage(uint32[5] memory v1, uint32[5] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  constructor()   {
    s0[bytes1(0x08)] *= ((((int16(10978) ^ int16(0)) | int16(32767)) * int16(0)) | int56(0));
    unchecked {
      uint32[5] memory l0 = s1;
      uint32[5] memory l1 = l0;
      assert(compareMemoryAndStorage(l1, s1));
    }
  }
  receive() external   payable
  {
  }
}
library L0 {
  type T0 is bool;
  function f2() public    returns(address o0,St0 memory o1)
  {
    int88 l0 = int88(0);
    function (address[5][][6] memory, bool) external   returns (int72, bool, bool[4][1][9] memory) l1;
    address l2 = address(0x0000000000000000000000000000000000000001);
  }
}
// ====
// ----


==== Source: su0.sol ====
pragma solidity >= 0.0.0;

==== Source: su1.sol ====
struct St0 {
  function (uint64[3] memory) external   returns (address payable, function (bool, address payable) external   returns (address payable, bytes memory)) el0;
}
pragma solidity >= 0.0.0;
contract C0 {
  receive() external virtual  payable
  {
  }
  int144[][6][][][]   s0;

	function compareMemoryAndStorage(int144[][6][][][] memory v1, int144[][6][][][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[][6][][] memory v1, int144[][6][][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[][6][] memory v1, int144[][6][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[][6] memory v1, int144[][6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(int144[] memory v1, int144[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  uint24   s1;
  constructor(int144[][6][][][] memory i0,uint24 i1)   {
    s0 = i0;
    s1 %= uint24(4555620);
    {
      int144[][6][][][] memory l0 = s0;
      int144[][6][][][] memory l1 = l0;
      assert(compareMemoryAndStorage(l1, s0));
      uint24  l2 = s1;
      uint24  l3 = l2;
      assert(l3 == s1);
    }
  }
  fallback() external virtual  
  {
    unchecked {
      (s0[((false ? payable(address(this)) : payable(address(this))).balance * uint256(70341435559301428735474559486970290108886326446318551592298008365991380350458))], s0[uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935)]) = (new int144[][6][][](3), s0[((payable(address(this)).balance | payable(address(this)).balance) ** uint216(uint216(32244065855919966032842309223081744004687569287825778922769921271)))]);
      int144[][6][][][] memory l0 = s0;
      int144[][6][][][] memory l1 = l0;
      assert(compareMemoryAndStorage(l1, s0));
      int144[][6][][][] memory l2 = s0;
      int144[][6][][][] memory l3 = l2;
      assert(compareMemoryAndStorage(l3, s0));
      uint24  l4 = s1;
      uint24  l5 = l4;
      assert(l5 == s1);
      (bool l6, bytes memory l7) = payable(this).call{value: 0}("");
      uint24  l8 = s1;
      uint24  l9 = l8;
      assert(l9 == s1);
    }
    uint24  l10 = s1;
    uint24  l11 = l10;
    assert(l11 == s1);
  }
}
// ====
// ----

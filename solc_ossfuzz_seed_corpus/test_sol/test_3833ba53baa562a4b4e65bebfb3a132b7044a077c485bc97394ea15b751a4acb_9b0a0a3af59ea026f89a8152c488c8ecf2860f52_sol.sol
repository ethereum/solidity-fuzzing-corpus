
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
contract C0 {
  event ev0(bytes2[][][][1][][]  ep0, bool  ep1) anonymous;
  receive() external virtual  payable
  {
  }
  uint40[][9][][][8][]   s0;

	function compareMemoryAndStorage(uint40[][9][][][8][] memory v1, uint40[][9][][][8][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint40[][9][][][8] memory v1, uint40[][9][][][8] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint40[][9][][] memory v1, uint40[][9][][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint40[][9][] memory v1, uint40[][9][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint40[][9] memory v1, uint40[][9] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint40[] memory v1, uint40[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  bytes22   s1 = bytes22(0xffffffffffffffffffffffffffffffffffffffffffff);
  address   s2 = address(this);
  bool  public s3 = true;
  constructor(uint40[][9][][][8][] memory i0)   {
    s0 = i0;
    unchecked {
      for(      bytes10 l0 = bytes10(0xffffffffffffffffffff);
;
(int48(-65581568077487) % ((int48((int48(0) / int48(140737488355327))) | int48(0)) + int48(140737488355327))))
      {
        bytes10 l1 = bytes10(0xffffffffffffffffffff);
        {
          continue;
        }
      }
      (s0[(uint120(1329227995784915872903807060280344575) ^ (uint120(1191486248818275826237148064403264743) % (uint120(1329227995784915872903807060280344575) + uint120(542890806925546711011722589952875855))))]) = ([new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3)]);
      uint40[][9][][][8][] memory l2 = s0;
      uint40[][9][][][8][] memory l3 = l2;
      assert(compareMemoryAndStorage(l3, s0));
      s0.push();
      i0[(~(uint256(4662321871890690732886399297735190178067887843913977082280062310366970164886)))] = [new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3), new uint40[][9][][](3)];
      assert((((int208(205688069665150755269371147819668813122841983204197482918576127) ^ ((int208(0) + int208(0)) ^ int208(0))) * int208(205688069665150755269371147819668813122841983204197482918576127)) == int208(205688069665150755269371147819668813122841983204197482918576127)));
    }
  }
}

==== Source: su1.sol ====
pragma solidity >= 0.0.0;
// ====
// ----


==== Source: su0.sol ====
contract C0 {
  event ev0();
  fallback() external   
  {
    bytes19 l0 = bytes19(0xffffffffffffffffffffffffffffffffffffff);
    function (bytes16[][7][6][9][3][] memory, bytes27, int40) external   l1;
    l0 ^= bytes19(0xffffffffffffffffffffffffffffffffffffff);
  }

	function compareMemoryAndCalldata(int256[][][][] memory v1, int256[][][][] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(int256[][][] memory v1, int256[][][] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(int256[][] memory v1, int256[][] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(int256[] memory v1, int256[] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}

	function compareMemoryAndCalldata(bool[6][][3] memory v1, bool[6][][3] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(bool[6][] memory v1, bool[6][] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndCalldata(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndCalldata(bool[6] memory v1, bool[6] calldata v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  function f1(int256[][][][] calldata i0,bool[6][][3] calldata i1,address i2) public   payable returns(function (int128, bool, function () external  ) external   o0,function (bool, bool, bytes memory) external   returns (function (string memory, uint56, function (bool, int216) external   returns (bytes1, int256)) external   returns (address payable[3][8][2][][] memory), address) o1)
  {
    uint80 l0 = (uint80(369348176146903144719027) & ((uint80(875962605415946702430751) | uint80(int80(604462909807314587353087))) + uint80(0)));
    int256[][][][] memory l1 = i0;
    assert(compareMemoryAndCalldata(l1, i0));
    (bool l3, bytes memory l4) = address(this).call(bytes("189c0502c7bcda19fe0eedf3e38be41abde8ed9c7d74ff81a2ffffffffffffffffffffffffffffffffffffffffffffffffffffffffff"));
  }
  string  public s0 = string("000000000000000000000000000000000000000000ffffffffffffffffffffffffffffffffffffffffff");

	function compareMemoryAndStorage(string memory v1, string storage v2) internal pure returns (bool) {
		return keccak256(bytes(v1)) == keccak256(bytes(v2));
	}
  address payable   s1 = payable(address(this));
  address payable  public s2;
  address   s3 = address(this);
  constructor(address payable i0)   {
    s2 = payable(address(this));
    unchecked {
    }
  }
}
pragma solidity >= 0.0.0;

==== Source: su1.sol ====
pragma solidity >= 0.0.0;
struct St0 {
  int80[5][][][8][] el0;
}
// ====
// ----

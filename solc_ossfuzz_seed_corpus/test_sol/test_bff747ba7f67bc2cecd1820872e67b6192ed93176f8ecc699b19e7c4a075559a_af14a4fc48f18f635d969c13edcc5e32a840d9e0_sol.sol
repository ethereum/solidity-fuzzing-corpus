
==== Source: su0.sol ====
pragma solidity >= 0.0.0;

==== Source: su1.sol ====
contract C0 {
  address  public s0;
  constructor(address i0) payable  {
    s0 = address(this);
    unchecked {
      for(      (int216(0) - int216(0));
;
)
      {
        uint32 l0 = uint32(0);
        (bool l1, bytes memory l2) = payable(this).call{value: 0}("");
        assert(false);
      }
      delete s0;
      delete s0;
      address  l3 = s0;
      address  l4 = l3;
      assert(l4 == s0);
      address  l5 = s0;
      address  l6 = l5;
      assert(l6 == s0);
      (s0) = (address(this));
      assert(s0 == address(this));
    }
  }
  fallback() external virtual  payable
  {
    (s0, s0) = (address(this), address(this));
    assert(s0 == address(this));
    assert(s0 == address(this));
  }
}
pragma solidity >= 0.0.0;
contract C1 {
  address payable   s1 = payable(address(this));
  address payable  public s2 = payable(address(this));
  function f1(address payable i0) public    returns(address o0)
  {
  }
}
// ====
// ----

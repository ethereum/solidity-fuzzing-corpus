
==== Source: su0.sol ====
contract C0 {
  receive() external virtual  payable
  {
    bytes15 l0 = bytes3(0x000000);
    address payable l1 = payable(address(this));
    do
    {
      uint48 l2 = uint48(138421073483487);
    }
    while (true);
  }
  uint216[][1][][][6][3]  public s0;

	function compareMemoryAndStorage(uint216[][1][][][6][3] memory v1, uint216[][1][][][6][3] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint216[][1][][][6] memory v1, uint216[][1][][][6] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint216[][1][][] memory v1, uint216[][1][][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint216[][1][] memory v1, uint216[][1][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint216[][1] memory v1, uint216[][1] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint216[] memory v1, uint216[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  int24   s1 = int24(0);
  bytes12   s2;
  constructor(uint216[][1][][][6][3] memory i0,bytes12 i1)   {
    s0 = i0;
    s2 ^= bytes12(0xffffffffffffffffffffffff);
    unchecked {
      s0[(uint216(105312291668557186697918027683670432318895095400549111254310977535) * payable(address(this)).balance)] = s0[((uint256(115792089237316195423570985008687907853269984665640564039457584007913129639935) >> uint216(uint216(105312291668557186697918027683670432318895095400549111254310977535))) ^ (uint256(0) ** uint128(uint128(0))))];
      uint216[][1][][][6][3] memory l0 = s0;
      uint216[][1][][][6][3] memory l1 = l0;
      assert(compareMemoryAndStorage(l1, s0));
    }
  }
  function f1() public virtual  payable
  {
    uint216[][1][][][6][3] memory l0 = s0;
    uint216[][1][][][6][3] memory l1 = l0;
    assert(compareMemoryAndStorage(l1, s0));
  }
}
pragma solidity >= 0.0.0;
contract C1 {
  fallback() external   
  {
  }
  int104   s3 = int104(10141204801825835211973625643007);
  bool  public s4 = true;
  bool  public s5 = true;
  mapping(bool => int152)   s6;
  constructor()   {
    s6[(uint64(0) <= (uint64(0) % uint64(18446744073709551615)))] ^= int152((int152(-1989754235405181704132253929554063598855673157) / int152(2854495385411919762116571938898990272765493247)));
    unchecked {
      payable(this).transfer(0);
    }
  }
  receive() external   payable
  {
  }
}
// ====
// ----

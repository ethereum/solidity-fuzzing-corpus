
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
function f0(function (uint216) external   returns (string memory, uint256, address payable) i0)     returns(address payable o0)
{
  if (false)
  {
    new function () external   returns (bool, int240, string memory)[](2);
  }
  else
  {
    return (msg.sender);
  }
}
error er0(bytes ep0);
// ====
// ----


==== Source: su0.sol ====
pragma solidity >= 0.0.0;
contract C0 {
  receive() external   payable
  {
    function () external   returns (bool, bytes5) l0;
    (bool l1, bytes memory l2) = payable(this).call{value: 0}("");
    for(    function (bool[7][][5][][][] memory, int152, bool) external   l3;
;
)
    {
      for(      bytes10 l4 = bytes10(0xffffffffffffffffffff);
;
)
      {
        function () internal   returns (function (address) external   returns (function () external   returns (bool), bool[2] memory, address), int216) l5;
        (bool l6, bytes memory l7) = payable(this).call{value: 1951757116126401230}("");
      }
    }
  }
  fallback() external   
  {
    unchecked {
      bool l0 = true;
      assembly
      {
        let al0 := callvalue()
      }
    }
    revert((false ? string("49c21a6ade7984f80ffa7a419c50000000") : string("59604f547e682200000000000000000000000000000000")));
  }
  error er0(address ep0);
  uint128[][10][]   s0;

	function compareMemoryAndStorage(uint128[][10][] memory v1, uint128[][10][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint128[][10] memory v1, uint128[][10] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(uint128[] memory v1, uint128[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  address  public s1 = address(this);
  constructor(uint128[][10][] memory i0)   {
    s0 = i0;
    { }
  }
}
// ====
// ----

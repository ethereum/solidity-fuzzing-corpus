
==== Source: su0.sol ====
contract C0 {
  fallback() external   
  {
  }
  function f1() external virtual  payable  returns(bool o0)  {
    o0 = true;
    assert(o0 == true);
    if (true)
    {
      (o0) = (((false ? (true ? payable(address(this)) : payable(address(this))) : payable(address(this))) != payable(address(this))));
      assert(o0 == ((false ? (true ? payable(address(this)) : payable(address(this))) : payable(address(this))) != payable(address(this))));
    }
    else
    {
      for(;
(payable(address(this)) < payable(address(this)));
)
      {
        continue;
      }
    }
    (bool l0, bytes memory l1) = address(this).call(bytes(((((int168(0) | int168(187072209578355573530071658587684226515959365500927)) % int168(0)) < int168(23602658102999650184457055171555342409111497714475)) ? string("This is a really long string that must ideally be random but is currently hard coded") : string("This is a really long string that must ideally be random but is currently hard coded"))));
  }
  bytes3[][]  public s0;

	function compareMemoryAndStorage(bytes3[][] memory v1, bytes3[][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(bytes3[] memory v1, bytes3[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}
  int112  public s1;
  int144  public s2 = int144(0);
  string   s3;

	function compareMemoryAndStorage(string memory v1, string storage v2) internal pure returns (bool) {
		return keccak256(bytes(v1)) == keccak256(bytes(v2));
	}
  constructor(bytes3[][] memory i0,int112 i1,string memory i2)   {
    s0 = i0;
    s1 %= (-((int112(0) & (~(int112(2596148429267413814265248164610047))))));
    s3 = string("This is a really long string that must ideally be random but is currently hard coded");
    unchecked {
    }
  }
}
pragma solidity >= 0.0.0;

==== Source: su1.sol ====
function f2(uint112 i0)     {
  function (address payable, address payable) external   l0;
}
pragma solidity >= 0.0.0;
// ====
// ----


==== Source: su0.sol ====
pragma solidity >= 0.0.0;
struct St0 {
  address el0;
}
contract C0 {
  receive() external virtual  payable
  {
    if (true)
    {
      require(true, string.concat(string("This is a really long string that must ideally be random but is currently hard coded"), string.concat(string("This is a really long string that must ideally be random but is currently hard coded"), string("This is a really long string that must ideally be random but is currently hard coded"), (true ? string("This is a really long string that must ideally be random but is currently hard coded") : string("This is a really long string that must ideally be random but is currently hard coded")), string("This is a really long string that must ideally be random but is currently hard coded")), string("This is a really long string that must ideally be random but is currently hard coded")));
      if (true)
      {
      }
      else
      {
      }
    }
  }
  address payable   s0 = payable(address(this));
  address payable   s1;
  bytes15   s2;
  bytes22  public s3 = bytes22(0x00000000000000000000000000000000000000000000);
  constructor(address payable i0,bytes15 i1) payable  {
    s1 = payable(address(this));
    s2 = bytes15((bytes26(bytes18(0x58467b5315bbca56b03590b0ac2a69a2fca0)) ^ bytes26(0xffffffffffffffffffffffffffffffffffffffffffffffffffff)));
    unchecked {
    }
  }
  fallback() external virtual  
  {
    (bool l0, bytes memory l1) = payable(this).call{value: 0}("");
    if (true)
    {
      if (true)
      {
        if (false)
        {
          if (true)
          {
          }
          else
          {
            (bool l2, bytes memory l3) = payable(this).call{value: 0}("");
            if (false)
            {
            }
          }
        }
        else
        {
          do
          {
            continue;
          }
          while (true);
        }
      }
    }
  }
  function f2(address payable i0) internal      {
  }
  struct St1 {
    bytes9 el0;
    int256[][] el1;
    bool el2;
    St0 el3;
  }
}

==== Source: su1.sol ====
struct St2 {
  bytes13[] el0;
  int48 el1;
}
struct St3 {
  address payable el0;
  bytes11 el1;
  bool el2;
}
pragma solidity >= 0.0.0;
// ====
// ----


==== Source: su0.sol ====
pragma solidity >= 0.0.0;
struct St0 {
  address el0;
  bool el1;
  address el2;
}
function f0(uint184[] memory i0,function (bool, address payable) external   returns (function (bool, bool, uint256) external   returns (bool[7] memory), address) i1,address i2)     {
  if (i0.length < uint256(0))
  {
  }
}
contract C0 {
  function f1(int88 i0) internal virtual     {
    (bool l0, bytes memory l1) = address(this).call(bytes("ffffffffffffffffffffffffffffffffffffffffffffffffffff"));
    return;
  }

	function compareMemoryAndCalldata(bytes memory v1, bytes calldata v2) internal pure returns (bool) {
		return keccak256(bytes(v1)) == keccak256(bytes(v2));
	}
  function f2(uint200 i0,bytes calldata i1) internal     returns(bytes memory o0)  {
  }
  struct St1 {
    uint224[] el0;
    address[] el1;
    bytes5 el2;
  }
  struct St2 {
    string el0;
    string el1;
    int16 el2;
    bytes17 el3;
  }
  bool   s0;
  bool  public s1;
  constructor(bool i0,bool i1) payable  {
    s0 = (payable(address(this)) >= 43196084206159869 f3 /*suffix expr*/);
    s1 = (msg.sender < address(this));
    unchecked {
      for(      int232 l0 = ((int232(3450873173395281893717377931138512726225554486085193277581262111899647) | int232(3450873173395281893717377931138512726225554486085193277581262111899647)) ** uint8(((uint8(203) ^ uint8(103)) | uint8(0))));
;
)
      {
        (s1) = (false);
        assert(s1 == false);
      }
    }
  }
}
function f3(uint56 i0) pure suffix  returns(address payable o0)
{
  address(0x0000000000000000000000000000000000000007);
}

==== Source: su1.sol ====
struct St3 {
  address payable el0;
  function (int16, address payable) external   el1;
  int200 el2;
}
contract C1 {
  struct St4 {
    bool[] el0;
    bytes25[8] el1;
    St3 el2;
  }
  uint72  public s2 = uint72(4722366482869645213695);
  mapping(uint72 => int112)[]   s3;
  C1.St4[7][]  public s4;

	function compareMemoryAndStorage(C1.St4[7][] memory v1, C1.St4[7][] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(C1.St4[7] memory v1, C1.St4[7] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(C1.St4 memory v1, C1.St4 storage v2) internal returns (bool) {

		if (!compareMemoryAndStorage(v1.el0, v2.el0))
			return false;

		if (!compareMemoryAndStorage(v1.el1, v2.el1))
			return false;

		if (!compareMemoryAndStorage(v1.el2, v2.el2))
			return false;

	return true;
	}
	function compareMemoryAndStorage(bool[] memory v1, bool[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}

	function compareMemoryAndStorage(bytes25[8] memory v1, bytes25[8] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (v1[i] != v2[i])

				return false;
		return true;
	}

	function compareMemoryAndStorage(St3 memory v1, St3 storage v2) internal returns (bool) {

		if (v1.el0 != v2.el0)
			return false;

		if (v1.el1 != v2.el1)
			return false;

		if (v1.el2 != v2.el2)
			return false;

	return true;
	}  address  public s5 = address(this);
  type T0 is address;
  function f4() public virtual     {
  }
}
pragma solidity >= 0.0.0;
// ====
// ----

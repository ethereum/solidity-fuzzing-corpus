
==== Source: su0.sol ====
pragma solidity >= 0.0.0;
struct St0 {
  address payable[] el0;
}
function f0()      returns(St0 memory o0,int168 o1){
  while (true)
  {
    if (true)
    {
      (o1, o0.el0) = (int168((((int168(0) & (int168(166945664714402274324218064705184246740487239734258) + int168(0))) % int168(136824070150258411247236313946106263388341106424462)) / int168(-38058341710073726640100190641905988513938023113837))), new address payable[](7));
      assert(o1 == int168((((int168(0) & (int168(166945664714402274324218064705184246740487239734258) + int168(0))) % int168(136824070150258411247236313946106263388341106424462)) / int168(-38058341710073726640100190641905988513938023113837))));
      assert((int16(0) == ((((int16(0) % int16(0)) + int16(0)) % int16(0)) | int16(-29841))));
    }
    else
    {
      require(true);
    }
    if (true)
    {
      o0 = St0(new address payable[](7));
      do
      {
        if (true)
        {
          continue;
        }
      }
      while (true);
    }
  }
}
contract C0 {
  fallback() external virtual  
  {
  }
  string[]   s0 = [string("This is a really long string that must ideally be random but is currently hard coded")];

	function compareMemoryAndStorage(string[] memory v1, string[] storage v2) internal returns (bool) {
		if (v1.length != v2.length)
			return false;
		for (uint i = 0; i < v1.length; i++)

			if (!compareMemoryAndStorage(v1[i], v2[i]))

				return false;
		return true;
	}

	function compareMemoryAndStorage(string memory v1, string storage v2) internal pure returns (bool) {
		return keccak256(bytes(v1)) == keccak256(bytes(v2));
	}
  receive() external   payable
  {
    return;
  }
  enum EN0 {
    M0, M1, M2, M3, M4, M5, M6, M7,
    M8, M9, M10, M11, M12, M13, M14, M15,
    M16, M17, M18, M19, M20, M21, M22, M23,
    M24, M25, M26, M27, M28, M29, M30, M31,
    M32, M33, M34, M35, M36, M37, M38, M39,
    M40, M41, M42, M43, M44, M45, M46, M47,
    M48, M49, M50, M51, M52, M53, M54, M55,
    M56, M57, M58, M59, M60, M61, M62, M63,
    M64, M65, M66, M67, M68, M69, M70, M71,
    M72, M73, M74, M75, M76, M77, M78
  }
  error er0(function (function (address, uint152) external   returns (int8), int232, bytes memory) external   returns (bytes28) ep0);
}

==== Source: su1.sol ====
function f3(bool i0)    pure suffix returns(bytes3[] memory o0){
  do
  {
    while (false)
    {
      function (int80[7] memory) internal   returns (int16, address payable) l0;
      continue;
    }
    continue;
  }
  while (false);
}
struct St1 {
  bytes7 el0;
}
pragma solidity >= 0.0.0;
// ====
// ----
